# Double-Take Exit 监控进度

> 监控任务：2026-08-10-reward-fill-double-take-exit-design.md 实现效果
> 创建时间：2026-08-10 19:50 BJT
> 最新监控：2026-08-11 22:35 BJT，第 105 轮（scheduled task double-take-exit-monitor）

---

## 第 105 轮（2026-08-11 22:35 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 104 轮一致，reward-exit 路径冻结约 50 小时

#### 事件统计（Aug 11 当日完整数据）

| 事件类型 | Aug 10 日志 | Aug 11 日志 | 总计 |
|---------|------------|------------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |

**Aug 11 日志（5,802 行）分析**：
- 仅 1 次 BATCH_TAKE_SUBMITTED（05:40 重启，重放 reward-exit-1e5af08f）
- 0 次 BATCH_TAKE_EXECUTED — Aug 11 没有任何 reward-exit FAK 实际成交
- 全天 19 次重启（加载 4 banned 市场），无新 reward-exit 批次

**余额耗尽详情（Aug 10 23:30-23:31 致命故障）**：
- 余额: 5,046,679（USDC 最小单位），做市权益 $286.14
- batch 02cafdd7（MrBeast, size=88, complement=273612511135 @ 0.99）: 需 86,963,450（超出余额 17 倍）
- batch 1e5af08f（Ralph Norman, size=7, complement=788668683504 @ 0.99）: 需 7,252,890（超出余额 44%）
- 08-10 23:31 退出后，此后所有 BATCH_TAKE_SUBMITTED 全部因余额不足失败

#### 数据库状态

DB 仍被运行中 bot 锁定，无法直接读取（与第 102-105 轮一致）。

#### 异常信号

- 🔴 两个 batch 冻结 ~50h，从未完成完整生命周期
- 🔴 BATCH_TAKE_COMPLETED / BATCH_SELL_PLACED / REWARD_EXIT_BATCH_CLOSED 全部 0 次
- 🔴 BATCH_TAKE_EXECUTED remaining_before 始终不递减（代码 bug）
- 🔴 Fill-to-batch 链接丢失（reward_exit_fills.batch_id=""）
- 🔴 reward_exit_orders 为 0（FAK 订单未持久化）
- 🔴 MARKET_REWARD_EXIT_UNLOCKED 从未触发（靠 banned_markets 阻止重入）
- 🟡 余额不足持续阻止 reward-exit 补单（$286 权益 vs ~$87 需求）
- 🟢 自动退出后无新奖励成交（仅 FIFA 市场报价中）
- 🟢 Ban 机制正常：4 banned 市场，restart 后正确跳过
- 🟢 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED（reward-exit 路径）

#### 结论

与第 104 轮一致。Aug 11 全天仅 1 次重放 BATCH_TAKE_SUBMITTED（05:40 restart），无任何新进度。reward-exit 模块从 Aug 10 23:31 冻结至今约 50 小时。余额不足是根本经济原因；remaining_before 计数器不递减是代码层面的次因（即使有足够的余额完成部分 fill，batch 也不会检测到 completion）。

已知问题优先级不变：**余额校验 > fill credit 写入 > orphan batch_id 修复 > remaining 计数器 bug > 时间戳记录**。

---

## 第 104 轮（2026-08-11 22:25 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 103 轮一致，reward-exit 路径冻结约 49 小时

#### 事件统计（与第 103 轮完全相同）

| 事件类型 | Aug 10 日志 | Aug 11 日志 | 总计 |
|---------|------------|------------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |

#### 数据库状态

DB 仍被运行中 bot 锁定（disk I/O error），无法直接读取。与第 102-103 轮一致：2 batches CLOSED（take_filled_size=0.0），2 fills（orphan batch_id=""），0 orders。

#### 关键观察（与第 103 轮一致 + 1 个新发现）

1. **reward-exit 冻结 ~49h**：自 Aug 10 23:31 起无新活动。两个 batch 从未完成完整生命周期（TAKE_COMPLETED → SELL_PLACED → BATCH_CLOSED 全部缺失）。

2. **Aug 11 仅 1 次 BATCH_TAKE_SUBMITTED**（05:40 重启重放 reward-exit-1e5af08f），立即因余额不足失败。之后全天无任何 reward-exit 事件。

3. **Bot 正常做市**："Will another city host the final of the 2030 FIFA World Cup?" 市场稳定报价中（reward pool $135/天，评分 ~13.858-14.100）。

4. **P1 recovery 状态**（旧路径，与 reward-exit 无关）：
   - Anthropic AI Agent 市场：manual_hold terminal stage，unpaired=-40，已 elapsed 3900+s，reason=exceeds_loss_budget
   - Baka President 市场：manual_hold terminal stage（mid 0.998 极高，cost_basis 0.400 已深度偏离）
   - 无新 RECOVERY_EXECUTED（Aug 11 log 中无）

5. **🔴 新发现 — BATCH_TAKE_EXECUTED remaining_before 不更新 bug**：
   所有 12 次 BATCH_TAKE_EXECUTED 都显示 `remaining_before=7`（batch 1e5af08f）或 `remaining_before=88`（batch 02cafdd7），从未递减。即使有实际的 LIVE FILL 成交（如 BUY NO 175.1 @ 0.500 = complement token for MrBeast market），日志中的 remaining_before 始终保持初始值。这表明 batch 内部的 remaining 计数器可能未被正确更新，或者日志取的是初始值而非当前值。这解释了为什么 BATCH_TAKE_COMPLETED 从未触发 — even if fills came in, the batch never detected completion because remaining was stuck.

#### 余额分析

Bot 余额不足是 reward-exit 无法工作的根本原因：
- batch 1e5af08f（Ralph Norman）：需 $6.93（7 股 @ 0.99，但实际 FAK fill 出现在 0.49 价位，累计 ~$14.80= 溢买）
- batch 02cafdd7（MrBeast）：需 $87.12（88 股 @ 0.99，实际 FAK fills 在 0.50 价位，累计 $170-$175= 溢买且远超预算）
- 余额耗尽（balance ~$56M USDC 基础单位, order amount 需要 ~$87M），bot 从 23:13 起余额不足，23:31 日志结束前仍有 3 次 balance not enough 报错

#### 异常信号检测

- 🔴 两个 batch 冻结约 49h，从未完成正常生命周期
- 🔴 take_filled_size=0.0（DB 未更新）
- 🔴 Fill-to-batch 链接为空（reward_exit_fills.batch_id=""）
- 🔴 reward_exit_orders 为 0（FAK 订单未持久化）
- 🔴 MARKET_REWARD_EXIT_UNLOCKED 从未触发（靠 banned_markets.json 阻止重入）
- 🔴 **新** BATCH_TAKE_EXECUTED remaining_before 始终不变（可能阻止 BATCH_TAKE_COMPLETED 检测）
- 🟡 余额不足持续阻止 reward-exit 补单
- 🟢 无重复 fill_id、无 MANUAL_HOLD（reward-exit 路径）、无 BATCH_LOSS_LOCKED
- 🟢 Ban 机制正常：两个市场在 banned_markets.json 中，restart 后正确跳过
- 🟢 Bot 做市正常，仅 reward-exit 路径静默

#### 结论

第 104 轮与第 103 轮完全一致。reward-exit 模块自 Aug 10 23:31 起冻结约 49 小时。新增一个关键发现：BATCH_TAKE_EXECUTED 日志中 remaining_before 始终不递减，可能是 BATCH_TAKE_COMPLETED 从未触发的代码层面原因（除了余额不足的经济原因）。

已知问题优先级（与上轮相同）：**余额校验 > fill credit 写入 > orphan batch_id 修复 > remaining 计数器 bug > 时间戳记录**。

---

## 第 103 轮（2026-08-11 22:00 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 102 轮完全一致，reward-exit 路径冻结超过 24 小时

#### 事件统计

| 事件类型 | Aug 10 日志 | Aug 11 日志 | 总计 |
|---------|------------|------------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |

#### 数据库状态

DB 副本读取（原库仍被 bot 锁定）：2 batches CLOSED（take_filled_size=0.0），2 fills（orphan batch_id=""），0 orders。与上轮一致。

| 表 | 行数 | 关键异常 |
|----|------|---------|
| reward_exit_batches | 2 | 两个 batch 均 CLOSED 但 take_filled_size=0.0, exit_filled_size=0.0, paired_size=0.0 |
| reward_exit_fills | 2 | 两个 fill 的 batch_id 均为空字符串（orphan），fill_id 仅包含 UUID 不含 batch 前缀 |
| reward_exit_orders | 0 | 零订单记录（FAK 订单可能未持久化） |

#### Aug 11 日志变化（总行数 4967 → 5610，+643 行，18:36 至 21:55）

- Bot 多次重启（加载 4 个 banned 市场 + 6-7 tripped 市场被轮转排除）
- **仅 1 次 reward-exit 活动**：05:40 重启时 batch reward-exit-1e5af08f 重放 1 次 BATCH_TAKE_SUBMITTED（price=0.99，立即被余额不足拒绝）
- P1 recovery 按预期工作：Grok 4.6 市场有 2 次 RECOVERY_EXECUTED（buy_complement 路径，escalated stage，已成交）
- CLOB balance refresh 偶尔失败（SSL EOF / timeout）

#### 异常信号检测

- 🔴 两个 batch 冻结超 ~24h，从未经过完整生命周期（BATCH_TAKE_COMPLETED → BATCH_SELL_PLACED → REWARD_EXIT_BATCH_CLOSED 全部缺失）
- 🔴 take_filled_size=0.0 — DB 未反映日志中的 12 次 BATCH_TAKE_EXECUTED
- 🔴 Fill-to-batch 链接为空（reward_exit_fills.batch_id=""）— 可能是写入逻辑 bug
- 🔴 reward_exit_orders 为 0 — FAK 订单未持久化
- 🔴 MARKET_REWARD_EXIT_UNLOCKED 从未触发 — 两个市场靠 banned_markets.json 阻止重入，而非正常解锁
- 🟡 余额不足持续阻止 batch 补单完成（Aug 11 有 balance 报错）
- 🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED
- 🟢 Ban 机制正常：4 个市场在 banned_markets.json 中
- 🟢 Aug 11 仅 05:40 重启时 1 次 BATCH_TAKE_SUBMITTED 重放，之后全部静默

#### 结论

第 103 轮与第 101-102 轮完全一致。reward-exit 模块自 Aug 10 23:31 起冻结 ~24.5 小时，零新活动。两个 batch 的 DB 记录显示 CLOSED 但填充指标全为零，说明关闭逻辑可能不是通过正常 BATCH_TAKE_COMPLETED → REWARD_EXIT_BATCH_CLOSED 路径完成，而是由 bot 重启时直接标记为 CLOSED（绕过完整状态机）。

P1 recovery（旧路径）正常工作 — Grok 4.6 市场有 escalated buy_complement 执行。但 reward-exit 模块因余额耗尽无新 batch 打开，且 DB 持久化数据存在三个已知问题：fill batch_id 为空、orders 表为空、take_filled_size 未更新。

---
## 第 102 轮（2026-08-11 21:23 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 101 轮一致，reward-exit 路径完全静默（第 2 天无新活动）

#### 事件统计

| 事件类型 | Aug 10 日志 | Aug 11 日志 | 总计 |
|---------|------------|------------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| ORDER_FILL | 0 | 0 | 0 |

#### 数据库状态

DB 仍被运行中 bot 锁定（disk I/O error），使用副本读取：2 batches CLOSED（take_filled_size=0.0），2 fills（orphan batch_id=""），0 orders。与上次可读时一致。

#### Aug 11 日志变化

Aug 11 日志从 4652 行增至 4967 行（+315 行）。新增行来自 21:06-21:23 时段：
- Bot 正常报价 "Will another city host the final of the 2030 FIFA World Cup?" 市场
- 跳过 "Will András Baka be the next President of Hungary?"（mid 0.998 超出范围）
- 两个 P1 recovery episode 保持 manual_hold（Baka terminal + Anthropic AI Agent escalated）
- 重新扫描奖励市场（7 tripped 被轮转排除）
- **无任何 reward-exit 相关新事件**（0 REWARD_EXIT_BATCH_OPENED, 0 BATCH_TAKE_SUBMITTED）

#### 异常信号检测

- 🔴 两个 batch 冻结 ~47.5h，无正常生命周期完成（REWARD_EXIT_BATCH_CLOSED 从未触发）
- 🔴 take_filled_size=0.0（DB 未更新）
- 🔴 Fill-to-batch 链接为空（reward_exit_fills.batch_id=""）
- 🔴 MARKET_REWARD_EXIT_UNLOCKED 从未触发（靠 banned_markets.json 阻止重入）
- 🟡 余额不足持续阻止补单（balance ~$5 vs order ~$7+$87），已在第 101 轮充分分析
- 🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED
- 🟢 Ban 机制正常：两个市场仍在 banned_markets.json 中
- 🟢 Aug 11 仅 1 次 BATCH_TAKE_SUBMITTED（05:40 重启重放），之后日间多次重启均不再重放

#### 结论

第 102 轮与第 101 轮完全一致。reward-exit 路径自 Aug 10 23:31 起冻结 ~47.5 小时，零新活动。bot 正常运行做市和 P1 recovery（manual_hold 路径），但 reward-exit 模块因余额耗尽而彻底静默。

下次有余额入金后需重点观察：新 trigger 的 batch 能否走完完整生命周期（BATCH_TAKE_COMPLETED → BATCH_SELL_PLACED → REWARD_EXIT_BATCH_CLOSED → MARKET_REWARD_EXIT_UNLOCKED）。

---

## 第 101 轮（2026-08-11 21:06 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 100 轮一致，reward-exit 路径完全静默

#### 事件统计

| 事件类型 | Aug 10 日志 | Aug 11 日志 | 总计 |
|---------|------------|------------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_STALE_CLEANUP / TERMINAL | 0 | 0 | 0 |

#### 数据库状态

DB 仍无法读取（disk I/O error，59MB，可能被运行中 bot 锁定）。上次可读时状态：2 batches CLOSED（take_filled_size=0.0），2 fills（orphan batch_id=""），0 orders。

#### Aug 11 日志变化

Aug 11 日志从 4385 行增至 4652 行（+267 行）。新增行来自 21:00-21:06 时段正常做市活动：
- Bot 正常报价 "Will another city host the final of the 2030 FIFA World Cup?" 市场
- 跳过 "Will András Baka be the next President of Hungary?"（mid 0.997 超出范围）
- Baka 市场的 P1 recovery episode 持续 manual_hold（terminal stage，exceeds_loss_budget）
- 重新扫描未找到新奖励市场（6 个 tripped 被轮转排除）
- **无任何 reward-exit 相关新事件**

#### 关键观察

1. **reward-exit 静默进入第 2 天**：自 Aug 10 23:31 起无新活动（~21.5 小时）。两个 batch 自创建以来已冻结 ~47 小时。

2. **Aug 11 唯一的 BATCH_TAKE_SUBMITTED**（05:40:07）是 bot 重启重放，余额不足导致失败后被清理。

3. **核心异常不变**：
   - 🔴 take_filled_size=0.0（DB 未更新）
   - 🔴 fills 表 batch_id=""（orphan）
   - 🔴 closed_ts=None（静默关闭路径未记录）
   - 🔴 MARKET_REWARD_EXIT_UNLOCKED 从未触发（通过 banned_markets.json 间接阻止重入）
   - 🟢 ban 机制正常
   - 🟢 当前 bot session 稳定报价中

4. **余额不足根因（已知）**：batch-1 需 $6.93，batch-2 需 $87.12，bot 余额 ~$5。

#### 结论

第 101 轮与第 100 轮完全一致。reward-exit 系统在最近 ~47 小时内仅有 2 个 batch 创建+失败，之后无新 reward fill 触发。bot 当前正常运行做市（1 个活跃市场），等待新 reward fill 来观察完整生命周期。

修复优先顺序不变：余额校验 > fill credit 写入 > orphan 修复 > 时间戳记录。

### 状态：🟡 与第 99 轮完全一致，无任何新变化

#### 事件统计（与上一轮相同）

| 事件类型 | Aug 10 日志 | Aug 11 日志 | 总计 |
|---------|------------|------------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_STALE_CLEANUP / TERMINAL | 0 | 0 | 0 |

#### 数据库状态（无变化）

| 表 | 行数 | 说明 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，take_filled_size=0.0，exit_filled_size=0.0，closed_ts=None |
| reward_exit_fills | 2 | intent=normal_reward，batch_id=""（空字符串 — orphan） |
| reward_exit_orders | 0 | FAK 订单不在此表记录 |

**重复数据验证：** 无重复 fill_id、无 orphan fills（fills 表两行 batch_id 均为空字符串，关联不到 batches）。

#### Aug 11 日志变化

Aug 11 日志共 4385 行（上次 4378，+7 行），其中 249 行来自 20:xx 时段。这些新增行均不包含任何 reward-exit 相关事件——bot 在 20:18 重启后正常做市，但无 reward fill 触发。

#### 关键观察

1. **自 Aug 10 23:31 起无新 reward-exit 活动**（~31 小时静默）。最后一次事件仍是 BATCH_TAKE_SUBMITTED 对 batch `reward-exit-1e5af08f`。

2. **Aug 11 的 1 次 BATCH_TAKE_SUBMITTED**（05:40:07）来自 bot 重启（05:39）后 persistent batch 的重放——bot 从 `reward_exit_batches` 表中加载了状态为 TAKE_PENDING 的 batch（此时还未被 startup cleanup 关闭），提交了一次 FAK 后清理为 CLOSED。

3. **核心异常仍未修复**：
   - ✅ 已知：余额不足导致 FAK 全失败（248 次 "not enough balance"）
   - ✅ 已知：BATCH_TAKE_EXECUTED 有成交但 `take_filled_size=0.0`（数据库未更新）
   - ✅ 已知：fills 表 batch_id 为空字符串（orphan，关联断裂）
   - ✅ 已知：closed_ts=None（静默关闭路径未记录时间戳）
   - ✅ 已知：没有 BATCH_TAKE_COMPLETED / SELL / CLOSED 日志事件（状态机未推进到后续阶段）

4. **Bot 当前状态**：运行中，报价 1 个市场（2030 FIFA World Cup），无 reward-exit locked 市场。之前的两个 batch 市场（Ralph Norman、MrBeast）已被 ban 或在 restart 时清理，不会再触发。

#### 与上一轮对比

与第 99 轮完全一致——无新增事件、无新 batch、无数据变化。reward-exit 功能处于搁置状态，等待上述 5 个异常的修复。

#### 与上一轮对比

与第 94-98 轮完全一致，无任何实质变化。reward-exit 功能处于搁置状态，等待修复上述 5 个异常后才能产生新的有意义的数据。

## 第 94 轮（2026-08-11 20:55 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🔴 发现异常 — 余额不足导致两个 batch 全部 FAK 失败，零 Take 成交却显示 CLOSED

#### 关键发现

**余额不足导致 take 阶段全部失败：**

- `reward-exit-1e5af08f`（Ralph Norman 市场）：7.32 股互补 YES @ $0.99 → 需要 $7.25，余额仅 $5.05
- `reward-exit-02cafdd7`（MrBeast 市场）：87.8 股互补 NO @ $0.99 → 需要 $86.96，余额仅 $5.05
- Aug 10 日志中有 **248 次 "not enough balance" 错误**，Aug 11 还有 2 次
- 两个 batch 在数据库中状态均为 **CLOSED**，但 `take_filled_size=0.0`（零 take 成交）
- `closed_ts=None`，`updated_ts=created_ts`（一次性写入后就从未更新）

#### 异常分析

**异常 1：两个 batch 全部零 take 成交但状态为 CLOSED（严重）**

batch_id=reward-exit-02cafdd7 在其 OPENED 消息（23:12:59）后仅 2 秒就有 EXECUTED 消息，报告 `filled=170.49 remaining_before=88`，说明实际上有成交。但数据库 `take_filled_size=0.0`。

两个 batch 的 `closed_ts=None` 且 `updated_ts=created_ts`（从未 update），说明 `close_reward_exit_batch()` 从未被正常调用。这些 batch 可能是通过 STALE_CLEANUP 路径关闭的——在 bot 重启时 `_init_reward_exit()` 中：

```python
stale_secs: int = int(risk_cfg.get("reward_exit_stale_batch_after_secs", 600))
```

如果 stale_secs 配置很小或未设置，新启动时立即将所有旧 batch 标记为 CLOSED。但 Aug 11 日志中实际上没有 REWARD_EXIT_BATCH_STALE_CLEANUP 或 TERMINAL_TIMEOUT 日志，说明这些 batch 在 bot 进程内就已经标记为 CLOSED 了，而不是通过启动清理。

**异常 2：BATCH_TAKE_EXECUTED 报告了成交但数据库中无对应记录**

- BATCH_TAKE_EXECUTED 对 batch `reward-exit-02cafdd7` 报告 filled=170.49、175.12（> target 88）
- BATCH_TAKE_EXECUTED 对 batch `reward-exit-1e5af08f` 报告 filled=14.80（> target 7）
- 但 `reward_exit_fills` 表中只有 2 行，batch_id 均为空字符串，intent=normal_reward（不是 bach_take）
- `reward_exit_orders` 表为 0 行

这说明 take 成交的记录路径与 origin fill 的记录路径不同。BATCH_TAKE_EXECUTED 报告的 filled 来自 userfeed 实时成交，但 `_credit_take_fills()` 可能没有正确地将这些成交写入 `reward_exit_fills` 表。

**异常 3：余额不足预期校验缺失**

batch `reward-exit-02cafdd7` 的 origin 成交是 43.9 股 YES @ $0.57（$25.02）。`take_target_size=87.8`（2× origin），需要 $86.96 @ $0.99 价格购买 NO 互补 token。但 bot 余额仅 ~$5。

这说明在 `_advance_take_pending()` 中没有对可用余额做校验——committed_balance 机制存在但似乎没有阻止余额不足的 FAK 订单提交。Aug 10 的 248 次 "not enough balance" 错误表明 bot 反复提交相同的无法填满的订单。

**异常 4：TAKE 狂打同一单无冷却**

两个 batch 共 205 次 BATCH_TAKE_SUBMITTED：
- batch `reward-exit-02cafdd7`：115 次，持续约 88 分钟（23:12~23:31）
- batch `reward-exit-1e5af08f`：90 次，持续约 72 分钟（22:19~23:31）

平均每 ~20-30 秒提交一次，没有任何退避机制。当余额不足导致 FAK 失败后，下一次报价循环立即重试。

**异常 5：BATCH_TAKE_EXECUTED 显示 filled 超过 take_target_size**

batch `reward-exit-02cafdd7`：`take_target_size=88`，但 EXECUTED 报告 filled=170.49、175.12。这暗示 userfeed 的成交可能是累计的，或包含了该 market 中不属于 batch 的成交。batch `reward-exit-1e5af08f`：`take_target_size=7.32`，但 filled=14.80（12 次 EXECUTED，每次都是 14.80）。

#### 数据库状态

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，take_filled_size=0.0，exit_filled_size=0.0，closed_ts=None，updated_ts=created_ts |
| reward_exit_fills | 2 | intent=normal_reward，batch_id=""（空字符串） |
| reward_exit_orders | 0 | FAK 订单不在此表记录 |

#### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_SUBMITTED | BATCH_TAKE_EXECUTED | 余额不足错误 | status |
|----------|------|----------|-------------|----------------------|---------------------|-------------|--------|
| reward-exit-1e5af08f | Ralph Norman | NO 3.66@$0.52 | 7.32 | 90 | 12 | ~124 | CLOSED (零成交) |
| reward-exit-02cafdd7 | MrBeast views | YES 43.9@$0.57 | 87.8 | 115 | 2 | ~124 | CLOSED (零成交) |

#### 建议修复方向

1. **TAKE 前校验余额**：`_advance_take_pending()` 提交 FAK 之前检查 `collateral >= order_notional`
2. **TAKE 退避机制**：连续 N 次 "not enough balance" 失败后延长重试间隔
3. **Batch close 逻辑检查**：当前两个 batch 明明是 TAKE_PENDING 且 take_filled=0，为何状态变为 CLOSED？需检查是否有代码路径在零成交时直接关闭 batch
4. **BATCH_TAKE_EXECUTED 时记录到 reward_exit_fills 表**：`_credit_take_fills()` 应该写入库
5. **batch_id 关联**：reward_exit_fills 中 batch_id 为空字符串，`open_reward_exit_batch` 用 origin_fill_id 防重，但 batch 本身可能通过 startup cleanup 提前关闭

### 状态：🟡 与第 92 轮完全一致，reward-exit 路径无变化 ~20.4h+

#### 关键发现

**与第 92 轮完全一致 — 零变化：**

| 指标 | 第 92 轮 | 第 93 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 10 事件总数 | 205 SUBMITTED + 12 EXECUTED | 205 + 12 | 无 |
| Aug 11 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Aug 11 ERROR/CRITICAL | 55 | 55 | 无（bot 启动中，末条 18:38:14） |
| 重复 fill_id | 0 | 0 | 无 |

#### Bot 运行状态

- Bot 在 18:37:41 退出 → 18:38:05 重新加载启动
- 18:38:07 出现 1 次 API key 创建 400 错误（与第 92 轮同一时间点）
- 18:38:14 LIVE 客户端就绪，日志截止于此（约 3 分钟前），bot 可能仍在初始化
- 本轮监控在 bot 启动后约 3 分钟执行，仍在启动阶段，reward-exit 无活动

#### 事件统计（累计，零变化 vs 第 92 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 数据库状态（无变化）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | CLOSED, take_filled_size=0.0, exit_filled_size=0.0, updated_ts=created_ts |
| reward_exit_fills | 2 | intent=normal_reward, batch_id 均为空字符串 |
| reward_exit_orders | 0 | FAK 失败不记录 |

#### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | SUBMITTED | 创建 | 最新活动 | status |
|----------|------|----------|-------------|-----------|------|----------|--------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 91 | 22:19 | 05:40:07 | CLOSED |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 115 | 23:12 | 23:30:59 | CLOSED |

#### 异常信号检测

🔴 **DB 状态与日志矛盾**：两个 batch 在 DB 中标记为 CLOSED（created_ts ≈ updated_ts，closed_ts=NULL），但日志中从未出现 REWARD_EXIT_BATCH_CLOSED 事件。过去 31 轮（第 62-92 轮）中此状态一直持续。batch 很可能被 bot 启动代码直接设为 CLOSED 而未经过正常生命周期。
🔴 **两个 batch 均从未成功 take**：take_filled_size=0.0，206 次 FAK SUBMITTED 全部失败（价格 0.9900 太激进，卖方深度不足）。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 0 次，锁被设但仍未清除（可能也随 batch CLOSED 被启动代码清除，但无日志记录）。
🔴 **Batch 元数据写入缺失**：first_take_submit_ts、first_take_executed_ts 均为 NULL（即使有 206 次 SUBMITTED + 12 次 EXECUTED）。
✅ 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id

#### 结论

状态完全冻结至第 93 轮。reward-exit 路径的 DB 写入和生命周期管理存在多处缺陷：
1. **填量丢失**：take_filled_size 始终为 0（即使 BATCH_TAKE_EXECUTED 有 12 次）
2. **时间戳缺失**：first_take_submit_ts / first_take_executed_ts 从不更新
3. **batch_id 不传播**：fills 表不关联到 batch
4. **closed_ts 不写入**：batch CLOSED 但没有 closed_ts
5. **解锁无日志**：市场解锁无记录

这些是编码缺陷，非运行状态问题，需要从代码层面修复，非监控层面可解。

---

## 第 92 轮（2026-08-11 18:39 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 91 轮相同，reward-exit 路径无变化 ~20.3h+，Bot 刚重启（18:37 退出 → 18:38 重启）

#### 关键发现

**与第 91 轮完全一致 — 零变化：**

| 指标 | 第 91 轮 | 第 92 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3885 | 3896 | +11（bot 退出+重启） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 (batch1=10, batch2=2) | 12 (同) | 无 |
| Aug 11 ERROR/CRITICAL | 52 | 55 | +3（1×API key 400 + 2×其他非 reward-exit） |
| 重复 fill_id | 0 | 0 | 无 |

#### Bot 运行状态

- Bot 在 18:33:51 主动退出（"正在退出，撤销全部订单"），18:37:48 ORDER_CANCEL_ALL
- 18:38:05 重新加载 4 个 banned 市场
- 18:38:07 1 次 API key 创建错误（`Could not create api key` 400），自动恢复
- 18:38:14 LIVE 客户端就绪，deposit-wallet merging 已启用
- 18:35-18:36 重启中期曾短暂报价 LoL Esports GotY 2026 + FIFA 2030（18:35:56 "开始报价"）
- 18:37:15 **重要**：`adopting held position market 'Will Ralph Norman place second in first round of the South C' into inventory management` — 这是 batch1 的原始市场，bot 重启后通过 Data API 检测到持仓并纳入管理
- 同时 adopted 的还有：Ty Masterson Kansas Governor（18:37:05）和 András Baka President of Hungary（18:37:18）
- 18:37:41 再次退出，本次运行仅存活 1 分 45 秒（18:35:56 → 18:37:41）
- 最新日志为 18:38:14（LIVE 客户端已就绪），bot 仍在启动阶段

#### 事件统计（累计，零变化 vs 第 91 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

**所有 8 个已知异常持续不变：**

1. ⚠️ **持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串）。origin_fill_id 与 reward_exit_fills.fill_id 可匹配但 batch_id 字段未被正确回填。导致 batch CLOSED 但 take_filled_size=0
2. ⚠️ **生命周期不完整** — 2 个 batch 只有 OPENED → TAKE_SUBMITTED（×206）→ TAKE_EXECUTED（×12），缺少 COMPLETED、SELL_PLACED、CLOSED、UNLOCKED
3. ⚠️ **DB 状态矛盾** — status=CLOSED，但 closed_ts=None，take_filled=0，exit_filled=0
4. ⚠️ **BATCH_TAKE_EXECUTED 数据异常** — batch2 的 filled=170.49/175.12 远超 target 88
5. ⚠️ **无 BATCH_TAKE_COMPLETED** — 所有 206 次 SUBMITTED 后 remaining 从未递减
6. 🔴 **两个 batch 永冻（~20.3h+）** — batch1 创建于 8/10 22:19，batch2 创建于 8/10 23:12
7. 🔴 **市场锁永不解锁** — MARKET_REWARD_EXIT_UNLOCKED 仍为 0
8. 🔴 **FORCED_EXIT 而非正常关闭** — bot 在 8/10 23:31:07 退出，248 次余额不足错误后触发

#### 新发现（第 92 轮）

- **Bot 18:33-18:38 重启周期**：18:33:51 退出 → 18:35:06 权益基准设置（$290.30） → 18:35:56 开始报价 LoL Esports GotY + FIFA 2030 → 18:37:41 再次退出（存活仅 1 分 45 秒）→ 18:38:14 再次就绪
- **Ralph Norman 市场仍被 adopted**：bot 重启后发现 Ralph Norman 市场有持仓（held position），通过 `adopting held position...` 纳入库存管理。该市场在 banned_markets.json 中但仍有持仓，bot 的 adoption 逻辑将持仓纳入但不做市
- **净收益缓存刷新失败**：18:36:16 出现 `净收益缓存后台刷新失败（5s timeout）`，非 reward-exit 相关
- **Aug 11 ERROR 增到 55**：+3 vs 第 91 轮，包含 1 次 API key 400 错误
- **与第 91 轮对比**：reward-exit 指标零变化，仅 bot 生命周期事件（退出+重启）增加

#### DB 状态详情（零变化）

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空，但 origin_fill_id 与 fill_id 可匹配)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66 (↔ batch origin_fill_id)
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9 (↔ batch origin_fill_id)
reward_exit_orders: 0
无重复 fill_id
```

#### 结论

第 92 轮与第 91 轮 reward-exit 状态完全一致，零变化。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 50 轮约 12.5 小时。Bot 在 18:33-18:38 经历了一次快速重启周期（存活仅 1 分 45 秒），期间 Ralph Norman 市场（batch1 原市场）的持仓被发现并 adopted 入库存管理。reward-exit 路径仍然静默。

修复优先顺序：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P0**: 修复 fill 写入时 batch_id 回填（当前 fill 记录存在但 batch_id="" ）
4. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
5. **P1**: bot 启动时清理过期 batch + DB 写入 BATCH_TAKE_EXECUTED 补量
6. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 91 轮（2026-08-11 18:24 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 90 轮相同，reward-exit 路径无变化 ~20.1h+，Bot 在线运行中（LoL Esports GotY + FIFA 2030）

#### 关键发现

**与第 90 轮完全一致 — 零变化：**

| 指标 | 第 90 轮 | 第 91 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3771 | 3825 | +54（ORDER_PLACED/CANCELLED） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 (batch1=10, batch2=2) | 12 (同) | 无 |
| Aug 11 ERROR/CRITICAL | 51 | 52 | +1（非 reward-exit） |
| 重复 fill_id | 0 | 0 | 无 |

#### Bot 运行状态

- Bot 在 18:21:03 最后日志（ORDER_PLACED + ORDER_CANCELLED expiry_refresh：LoL Esports GotY 2026 + 2030 FIFA World Cup host city）
- 活跃报价：**LoL Esports GotY 2026** (@ 0.480/0.490) + **2030 FIFA World Cup host city** (@ 0.400/0.570)
- Bot 凌晨 05:39 启动，持续运行超过 12.6 小时
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh)
- 本轮无 P0 guard 历史清理日志（上次清理在 ~18:01-18:02），正常

#### 事件统计（累计，零变化 vs 第 90 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

**所有 8 个已知异常持续不变：**

1. ⚠️ **持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串）。origin_fill_id 与 reward_exit_fills.fill_id 可匹配但 batch_id 字段未被正确回填。导致 batch CLOSED 但 take_filled_size=0
2. ⚠️ **生命周期不完整** — 2 个 batch 只有 OPENED → TAKE_SUBMITTED（×206）→ TAKE_EXECUTED（×12），缺少 COMPLETED、SELL_PLACED、CLOSED、UNLOCKED
3. ⚠️ **DB 状态矛盾** — status=CLOSED，但 closed_ts=None，take_filled=0，exit_filled=0
4. ⚠️ **BATCH_TAKE_EXECUTED 数据异常** — batch2 的 filled=170.49/175.12 远超 target 88
5. ⚠️ **无 BATCH_TAKE_COMPLETED** — 所有 206 次 SUBMITTED 后 remaining 从未递减
6. 🔴 **两个 batch 永冻（~20.1h+）** — batch1 创建于 8/10 22:19，batch2 创建于 8/10 23:12
7. 🔴 **市场锁永不解锁** — MARKET_REWARD_EXIT_UNLOCKED 仍为 0
8. 🔴 **FORCED_EXIT 而非正常关闭** — bot 在 8/10 23:31:07 退出，248 次余额不足错误后触发

#### 新发现（第 91 轮）

- **与第 90 轮零差异**：reward-exit 路径完全静默，所有指标不变
- **Bot 持续运行超 12.6h**：05:39 启动至今持续正常做市，仅 Aug 11 05:40 发出一次 BATCH_TAKE_SUBMITTED 即被中断
- **市场稳定**：LoL Esports GotY 2026 + 2030 FIFA World Cup 已连续多轮不变
- **Aug 11 52 ERROR/CRITICAL**：仅 +1 vs 第 90 轮，均为非 reward-exit 相关

#### DB 状态详情（零变化）

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空，但 origin_fill_id 与 fill_id 可匹配)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66 (↔ batch origin_fill_id)
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9 (↔ batch origin_fill_id)
reward_exit_orders: 0
无重复 fill_id
```

#### 结论

第 91 轮与第 90 轮 reward-exit 状态完全一致，零变化。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 49 轮约 12.2 小时。Bot 在线正常做市（LoL Esports GotY 2026 + FIFA 2030 host city），reward-exit 路径静默。

修复优先顺序：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P0**: 修复 fill 写入时 batch_id 回填（当前 fill 记录存在但 batch_id="" ）
4. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
5. **P1**: bot 启动时清理过期 batch + DB 写入 BATCH_TAKE_EXECUTED 补量
6. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 90 轮（2026-08-11 18:05 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 89 轮相同，reward-exit 路径无变化 ~19.8h+，Bot 在线运行中（LoL Esports GotY + FIFA 2030）

#### 关键发现

**与第 89 轮完全一致 — 零变化：**

| 指标 | 第 89 轮 | 第 90 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3703 | 3771 | +68（ORDER_PLACED/CANCELLED + P0 guard 清理） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 (batch1=10, batch2=2) | 12 (同) | 无 |
| Aug 11 ERROR/CRITICAL | 46 | 51 | +5（1 次 API timeout + 4 次其他） |
| 重复 fill_id | 0 | 0 | 无 |

#### Bot 运行状态

- Bot 在 18:05:39 最后日志（ORDER_PLACED + ORDER_CANCELLED expiry_refresh：LoL Esports GotY 2026 + 2030 FIFA World Cup host city）
- 活跃报价：**LoL Esports GotY 2026** (@ 0.480/0.490) + **2030 FIFA World Cup host city** (@ 0.400/0.570)
- Bot 凌晨 05:39 启动，持续运行超过 12.4 小时
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh)
- P0 guard 历史清理正常（18:01:18-18:02:22：删除 62 + 12 + 10 条），正常维护行为
- 18:01:08 1 次 API request timeout（`The read operation timed out`），非 reward-exit 相关，自动恢复

#### 事件统计（累计，零变化 vs 第 89 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

**所有 8 个已知异常持续不变：**

1. ⚠️ **持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串）。origin_fill_id 与 reward_exit_fills.fill_id 可匹配但 batch_id 字段未被正确回填。导致 batch CLOSED 但 take_filled_size=0
2. ⚠️ **生命周期不完整** — 2 个 batch 只有 OPENED → TAKE_SUBMITTED（×206）→ TAKE_EXECUTED（×12），缺少 COMPLETED、SELL_PLACED、CLOSED、UNLOCKED
3. ⚠️ **DB 状态矛盾** — status=CLOSED，但 closed_ts=None，take_filled=0，exit_filled=0
4. ⚠️ **BATCH_TAKE_EXECUTED 数据异常** — batch2 的 filled=170.49/175.12 远超 target 88
5. ⚠️ **无 BATCH_TAKE_COMPLETED** — 所有 206 次 SUBMITTED 后 remaining 从未递减
6. 🔴 **两个 batch 永冻（~19.8h+）** — batch1 创建于 8/10 22:19，batch2 创建于 8/10 23:12
7. 🔴 **市场锁永不解锁** — MARKET_REWARD_EXIT_UNLOCKED 仍为 0
8. 🔴 **FORCED_EXIT 而非正常关闭** — bot 在 8/10 23:31:07 退出，248 次余额不足错误后触发

#### 新发现（第 90 轮）

- **与第 89 轮零差异**：reward-exit 路径完全静默，所有指标不变
- **API timeout 偶发**：18:01:08 1 次 `request error: The read operation timed out`（py_clob_client_v2），自动恢复，非 reward-exit 相关
- **Bot 持续运行超 12.4h**：05:39 启动至今持续正常做市，仅 Aug 11 05:40 发出一次 BATCH_TAKE_SUBMITTED 即被中断
- **市场稳定**：LoL Esports GotY 2026 + 2030 FIFA World Cup 已连续多轮不变

#### DB 状态详情（零变化）

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空，但 origin_fill_id 与 fill_id 可匹配)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66 (↔ batch origin_fill_id)
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9 (↔ batch origin_fill_id)
reward_exit_orders: 0
无重复 fill_id
```

#### 结论

第 90 轮与第 89 轮 reward-exit 状态完全一致，零变化。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 48 轮约 11.9 小时。Bot 在线正常做市（LoL Esports GotY 2026 + FIFA 2030 host city），reward-exit 路径静默。仅新增 1 次 API timeout（非 reward-exit），自动恢复。

修复优先顺序：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P0**: 修复 fill 写入时 batch_id 回填（当前 fill 记录存在但 batch_id="" ）
4. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
5. **P1**: bot 启动时清理过期 batch + DB 写入 BATCH_TAKE_EXECUTED 补量
6. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 89 轮（2026-08-11 17:56 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 88 轮相同，reward-exit 路径无变化 ~19.6h+，Bot 在线运行中（LoL Esports GotY + FIFA 2030）

#### 关键发现

**与第 88 轮完全一致 — 零变化：**

| 指标 | 第 88 轮 | 第 89 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3619 | 3703 | +84（ORDER_PLACED/CANCELLED + P0 guard 清理） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 (batch1=10, batch2=2) | 12 (同) | 无 |
| Aug 11 ERROR/CRITICAL | 46 | 46 | 无（本轮无新增 ERROR） |
| 重复 fill_id | 0 | 0 | 无 |

#### Bot 运行状态

- Bot 在 17:53:14 最后日志（P0 guard 历史清理: 删除 4 条 10 小时前决策）
- 活跃报价：**LoL Esports GotY 2026** (@ 0.480/0.490) + **2030 FIFA World Cup host city** (@ 0.400/0.570)
- Bot 凌晨 05:39 启动，持续运行超过 12.2 小时
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh)
- P0 guard 历史清理正常（17:53），正常维护行为
- 本轮无新增 ERROR/CRITICAL

#### 事件统计（累计，零变化 vs 第 88 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

**所有 8 个已知异常持续不变：**

1. ⚠️ **持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串）。origin_fill_id 与 reward_exit_fills.fill_id 可匹配但 batch_id 字段未被正确回填。导致 batch CLOSED 但 take_filled_size=0
2. ⚠️ **生命周期不完整** — 2 个 batch 只有 OPENED → TAKE_SUBMITTED（×206）→ TAKE_EXECUTED（×12），缺少 COMPLETED、SELL_PLACED、CLOSED、UNLOCKED
3. ⚠️ **DB 状态矛盾** — status=CLOSED，但 closed_ts=None，take_filled=0，exit_filled=0
4. ⚠️ **BATCH_TAKE_EXECUTED 数据异常** — batch2 的 filled=170.49/175.12 远超 target 88
5. ⚠️ **无 BATCH_TAKE_COMPLETED** — 所有 206 次 SUBMITTED 后 remaining 从未递减
6. 🔴 **两个 batch 永冻（~19.6h+）** — batch1 创建于 8/10 22:19，batch2 创建于 8/10 23:12
7. 🔴 **市场锁永不解锁** — MARKET_REWARD_EXIT_UNLOCKED 仍为 0
8. 🔴 **FORCED_EXIT 而非正常关闭** — bot 在 8/10 23:31:07 退出，248 次余额不足错误后触发

#### 新发现（第 89 轮）

- **Aug 10 余额耗尽确认**：总共 248 条 `balance is not enough` 错误（bot 余额仅 $50.47，两个 batch 同时对价格 $0.99 的 complement token 提交 FAK BUY）
- **Aug 11 干净（无 ERROR）**：本轮区间（第 88 轮 17:36 → 第 89 轮 17:56）无 ERROR/CRITICAL，bot 运行平稳
- **Bot 运行超 12h**：05:39 启动至今，持续正常运行，仅 Aug 11 05:40 发出一次 BATCH_TAKE_SUBMITTED 即被中断
- **市场稳定**：LoL Esports GotY 2026 + 2030 FIFA World Cup 已连续多轮不变

#### DB 状态详情（零变化）

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空，但 origin_fill_id 与 fill_id 可匹配)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66 (↔ batch origin_fill_id)
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9 (↔ batch origin_fill_id)
reward_exit_orders: 0
无重复 fill_id
```

#### 结论

第 89 轮与第 88 轮 reward-exit 状态完全一致，零变化。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 47 轮约 11.8 小时。Bot 在线正常做市（LoL Esports GotY 2026 + FIFA 2030 host city），reward-exit 路径静默。本轮重新确认了 Aug 10 的 248 次余额不足错误是导致 FORCED_EXIT 的直接原因。

修复优先顺序：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P0**: 修复 fill 写入时 batch_id 回填（当前 fill 记录存在但 batch_id="" ）
4. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
5. **P1**: bot 启动时清理过期 batch + DB 写入 BATCH_TAKE_EXECUTED 补量
6. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

### 状态：🟡 与第 87 轮相同，reward-exit 路径无变化 ~23.3h+，Bot 在线运行中（LoL Esports GotY + FIFA 2030）

#### 关键发现

**与第 87 轮完全一致 — 零变化：**

| 指标 | 第 87 轮 | 第 88 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3565 | 3619 | +54（ORDER_PLACED/CANCELLED + P0 guard 清理） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 (batch1=10, batch2=2) | 12 (同) | 无 |
| Aug 11 ERROR/CRITICAL | 44 | 46 | +2（PolyApi 400 error，非 reward-exit） |
| 重复 fill_id | 0 | 0 | 无 |

#### Bot 运行状态

- Bot 在 17:36:48 最后日志（P0 guard 历史清理: 删除 10 条 10 小时前决策）
- 活跃报价：**LoL Esports GotY 2026** (@ 0.480/0.490) + **2030 FIFA World Cup host city** (@ 0.400/0.570)
- Bot 凌晨 05:39 启动，持续运行超过 12 小时
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh)
- P0 guard 历史清理密集（17:30-17:36：删除 94 + 52 + 12 + 14 + 20 + 24 + 10 条），正常维护行为
- Aug 11 ERROR/CRITICAL 增强至 46（全部为 API key 400 错误等非 reward-exit 异常），bot 自动恢复

#### 事件统计（累计，零变化 vs 第 87 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

**所有 8 个已知异常持续不变：**

1. ⚠️ **持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串）。但验证发现 origin_fill_id 与 reward_exit_fills.fill_id 匹配（batch1 origin=1e5af08f ↔ fill=1e5af08f），说明 fill 记录存在但 batch_id 字段未被正确回填。导致 batch CLOSED 但 take_filled_size=0
2. ⚠️ **生命周期不完整** — 2 个 batch 只有 OPENED → TAKE_SUBMITTED（×206）→ TAKE_EXECUTED（×12），缺少 COMPLETED、SELL_PLACED、CLOSED、UNLOCKED
3. ⚠️ **DB 状态矛盾** — status=CLOSED，但 closed_ts=None，take_filled=0，exit_filled=0
4. ⚠️ **BATCH_TAKE_EXECUTED 数据异常** — batch2 的 filled=170.49/175.12 远超 target 88
5. ⚠️ **无 BATCH_TAKE_COMPLETED** — 所有 206 次 SUBMITTED 后 remaining 从未递减
6. 🔴 **两个 batch 永冻（~23.3h+）** — batch1 创建于 8/10 22:19，batch2 创建于 8/10 23:12
7. 🔴 **市场锁永不解锁** — MARKET_REWARD_EXIT_UNLOCKED 仍为 0
8. 🔴 **FORCED_EXIT 而非正常关闭** — bot 在 8/10 23:31:07 退出

#### 新发现（第 88 轮）

- **fill-to-batch 链接确认**：origin_fill_id ↔ reward_exit_fills.fill_id 可以匹配，但 reward_exit_fills.batch_id 字段为空字符串。这说明 fill 记录在创建时 batch_id 未被正确设置，而非 fill 记录缺失。
- **P0 guard 清理密集**：17:32-17:36 段连续出现 7 次 P0 guard 历史清理（总计删除 226 条），是正常维护行为（清理 10 小时前的旧决策），但频率略高，可能与 bot 做市时间长有关（运行超 12 小时）
- **市场稳定**：LoL Esports GotY 2026 + 2030 FIFA World Cup 已连续多轮不变，无市场轮换

#### DB 状态详情（零变化）

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空，但 origin_fill_id 与 fill_id 可匹配)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66 (↔ batch origin_fill_id)
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9 (↔ batch origin_fill_id)
reward_exit_orders: 0
无重复 fill_id
```

#### 结论

第 88 轮与第 87 轮 reward-exit 状态完全一致，零变化。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 46 轮约 11.5 小时。Bot 在线正常做市（LoL Esports GotY 2026 + FIFA 2030 host city），reward-exit 路径静默。本轮确认了 fill-to-origin 的链接存在但 batch_id 字段为空。

修复优先顺序：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P0**: 修复 fill 写入时 batch_id 回填（当前 fill 记录存在但 batch_id="" ）
4. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
5. **P1**: bot 启动时清理过期 batch + DB 写入 BATCH_TAKE_EXECUTED 补量
6. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 87 轮（2026-08-11 17:24 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 86 轮相同，reward-exit 路径无变化 ~23.1h+，Bot 在线运行中（LoL Esports GotY + FIFA 2030）

#### 关键发现

**与第 86 轮完全一致 — 零变化：**

| 指标 | 第 86 轮 | 第 87 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3507 | 3565 | +58（ORDER_PLACED/CANCELLED + P0 guard 清理） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 (batch1=10, batch2=2) | 12 (同) | 无 |

#### Bot 运行状态

- Bot 在 17:22:33 最后日志（P0 guard 历史清理: 删除 12 条 10 小时前决策 + ORDER_PLACED/CANCELLED expiry_refresh）
- 活跃报价：LoL Esports GotY 2026 + 2030 FIFA World Cup host city
- Bot 凌晨 05:39 启动，持续运行超过 11.7 小时
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh)
- P0 guard 历史清理密集（17:17-17:22：删除 46 + 46 + 62 + 10 + 8 + 12 条），正常维护行为

#### 事件统计（累计，零变化 vs 第 86 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

**所有 8 个已知异常持续不变：**

1. ⚠️ **持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串），导致 batch CLOSED 但 take_filled_size=0
2. ⚠️ **生命周期不完整** — 2 个 batch 只有 OPENED → TAKE_SUBMITTED（×206）→ TAKE_EXECUTED（×12），缺少 COMPLETED、SELL_PLACED、CLOSED、UNLOCKED
3. ⚠️ **DB 状态矛盾** — status=CLOSED，但 closed_ts=None，take_filled=0，exit_filled=0
4. ⚠️ **BATCH_TAKE_EXECUTED 数据异常** — batch2 的 filled=170.49/175.12 远超 target 88
5. ⚠️ **无 BATCH_TAKE_COMPLETED** — 所有 206 次 SUBMITTED 后 remaining 从未递减
6. 🔴 **两个 batch 永冻（~23.1h+）** — batch1 创建于 8/10 22:19，batch2 创建于 8/10 23:12
7. 🔴 **市场锁永不解锁** — MARKET_REWARD_EXIT_UNLOCKED 仍为 0
8. 🔴 **FORCED_EXIT 而非正常关闭** — bot 在 8/10 23:31:07 退出

#### DB 状态详情（零变化）

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9
reward_exit_orders: 0
```

#### 结论

第 87 轮与第 86 轮 reward-exit 状态完全一致，零变化。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 45 轮约 11.3 小时。Bot 在线正常做市（LoL Esports GotY 2026 + FIFA 2030 host city），reward-exit 路径静默。

修复优先顺序：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

---

## 第 86 轮（2026-08-11 17:09 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 85 轮相同，reward-exit 路径无变化 ~22.8h+，Bot 在线运行中（LoL Esports GotY + FIFA 2030）

#### 关键发现

**与第 85 轮完全一致 — 零变化：**

| 指标 | 第 85 轮 | 第 86 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3441 | 3507 | +66（ORDER_PLACED/CANCELLED + P0 guard 清理） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 (batch1=10, batch2=2) | 12 (同) | 无 |

#### Bot 运行状态

- Bot 在 17:07:10 最后日志（P0 guard 历史清理: 删除 10 条 10 小时前决策）
- Bot 凌晨 05:39 启动，持续运行超过 11.5 小时
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh)
- 活跃报价：LoL Esports GotY 2026 + 2030 FIFA World Cup host city
- 17:06:53 P0 guard 历史清理（删除 18 + 10 条），正常维护行为

#### 事件统计（累计，零变化 vs 第 85 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

**所有 8 个已知异常持续不变：**

1. ⚠️ **持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串），导致 batch CLOSED 但 take_filled_size=0
2. ⚠️ **生命周期不完整** — 2 个 batch 只有 OPENED → TAKE_SUBMITTED（×206）→ TAKE_EXECUTED（×12），缺少 COMPLETED、SELL_PLACED、CLOSED、UNLOCKED
3. ⚠️ **DB 状态矛盾** — status=CLOSED，但 closed_ts=None，take_filled=0，exit_filled=0
4. ⚠️ **BATCH_TAKE_EXECUTED 数据异常** — batch2 的 filled=170.49/175.12 远超 target 88
5. ⚠️ **无 BATCH_TAKE_COMPLETED** — 所有 206 次 SUBMITTED 后 remaining 从未递减
6. 🔴 **两个 batch 永冻（~22.8h+）** — batch1 创建于 8/10 22:19，batch2 创建于 8/10 23:12
7. 🔴 **市场锁永不解锁** — MARKET_REWARD_EXIT_UNLOCKED 仍为 0
8. 🔴 **FORCED_EXIT 而非正常关闭** — bot 在 8/10 23:31:07 退出

#### DB 状态详情（零变化）

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9
reward_exit_orders: 0
```

#### 结论

第 86 轮与第 85 轮 reward-exit 状态完全一致，零变化。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 44 轮约 11 小时。Bot 在线正常做市（LoL Esports GotY 2026 + FIFA 2030 host city），reward-exit 路径静默。

修复优先顺序：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 85 轮（2026-08-11 16:52 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 84 轮相同，reward-exit 路径无变化 ~22.6h+，Bot 在线运行中（LoL Esports GotY + FIFA 2030）

#### 关键发现

**与第 84 轮完全一致 — 零变化：**

| 指标 | 第 84 轮 | 第 85 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3385 | 3441 | +56（ORDER_PLACED/CANCELLED + P0 guard 清理） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 (batch1=10, batch2=2) | 12 (同) | 无 |

#### Bot 运行状态

- Bot 在 16:50:59 最后日志（ORDER_PLACED + ORDER_CANCELLED expiry_refresh：LoL Esports GotY 2026 + 2030 FIFA World Cup）
- P0 guard 历史清理正常（16:49-16:50：删除 12 + 18 条 10 小时前决策）
- Bot 凌晨 05:39 启动，持续运行超过 11.2 小时
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh)

#### 事件统计（累计，零变化 vs 第 84 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

**所有 8 个已知异常持续不变：**

1. ⚠️ **持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串），导致 batch CLOSED 但 take_filled_size=0
2. ⚠️ **生命周期不完整** — 2 个 batch 只有 OPENED → TAKE_SUBMITTED（×206）→ TAKE_EXECUTED（×12），缺少 COMPLETED、SELL_PLACED、CLOSED、UNLOCKED
3. ⚠️ **DB 状态矛盾** — status=CLOSED，但 closed_ts=None，take_filled=0，exit_filled=0
4. ⚠️ **BATCH_TAKE_EXECUTED 数据异常** — batch2 的 filled=170.49/175.12 远超 target 88
5. ⚠️ **无 BATCH_TAKE_COMPLETED** — 所有 206 次 SUBMITTED 后 remaining 从未递减
6. 🔴 **两个 batch 永冻（~22.6h+）** — batch1 创建于 8/10 22:19，batch2 创建于 8/10 23:12
7. 🔴 **市场锁永不解锁** — MARKET_REWARD_EXIT_UNLOCKED 仍为 0
8. 🔴 **FORCED_EXIT 而非正常关闭** — bot 在 8/10 23:31:07 退出

#### DB 状态详情（零变化）

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9
reward_exit_orders: 0
```

#### 结论

第 85 轮与第 84 轮 reward-exit 状态完全一致，零变化。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 43 轮约 10.8 小时。Bot 在线正常做市（LoL Esports GotY 2026 + FIFA 2030 host city），reward-exit 路径静默。

修复优先顺序：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 84 轮（2026-08-11 16:37 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 83 轮相同，reward-exit 路径无变化 ~22h+，Bot 在线运行中（LoL Esports GotY + FIFA 2030）

#### 关键发现

**与第 83 轮完全一致 — 零变化：**

| 指标 | 第 83 轮 | 第 84 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3313 | 3385 | +72（ORDER_PLACED/CANCELLED + P0 guard 清理） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 (batch1=10, batch2=2) | 12 (同) | 无 |

#### Bot 运行状态

- Bot 在 16:37:54 最后日志（P0 guard 历史清理: 删除 4 条）
- 活跃报价：League of Legends Esports GotY 2026 + 2030 FIFA World Cup host city
- Bot 凌晨 05:39 启动，持续运行超过 11 小时
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh)

#### 事件统计（累计，零变化 vs 第 83 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

**所有 8 个已知异常持续不变：**

1. ⚠️ **持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串），导致 batch CLOSED 但 take_filled_size=0
2. ⚠️ **生命周期不完整** — 2 个 batch 只有 OPENED → TAKE_SUBMITTED（×206）→ TAKE_EXECUTED（×12），缺少 COMPLETED、SELL_PLACED、CLOSED、UNLOCKED
3. ⚠️ **DB 状态矛盾** — status=CLOSED，但 closed_ts=None，take_filled=0，exit_filled=0
4. ⚠️ **BATCH_TAKE_EXECUTED 数据异常** — batch2 的 filled=170.49/175.12 远超 target 88
5. ⚠️ **无 BATCH_TAKE_COMPLETED** — 所有 206 次 SUBMITTED 后 remaining 从未递减
6. 🔴 **两个 batch 永冻（~22h+）** — batch1 创建于 8/10 22:19，batch2 创建于 8/10 23:12
7. 🔴 **市场锁永不解锁** — MARKET_REWARD_EXIT_UNLOCKED 仍为 0
8. 🔴 **FORCED_EXIT 而非正常关闭** — bot 在 8/10 23:31:07 退出

#### DB 状态详情（零变化）

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9
reward_exit_orders: 0
```

#### 结论

第 84 轮与第 83 轮 reward-exit 状态完全一致，零变化。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 42 轮约 10.5 小时。Bot 在线正常做市（LoL Esports GotY 2026 + FIFA 2030 host city），reward-exit 路径静默。

修复优先顺序：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 83 轮（2026-08-11 16:21 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 82 轮相同，reward-exit 路径无变化 ~20h+，Bot 在线运行中

#### 关键发现

**与第 82 轮完全一致 — 零变化：**

| 指标 | 第 82 轮 | 第 83 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3196 | 3313 | +117（全为 ORDER_PLACED/CANCELLED + P0 guard 清理） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 (batch1=10, batch2=2) | 12 (同) | 无 |

#### Bot 运行状态

- Bot 在 16:21:43 最后日志（活跃状态：League of Legends Esports GOTY + FIFA 2030 host 市场）
- **新观察**：16:20-16:21 出现 P0 guard 历史清理日志（删除 2 条 → 10 条 → 18 条，逐批清理），正常维护行为
- Bot 凌晨 05:39 启动，持续运行超过 10.7 小时
- **重要**：Aug 11 BATCH_TAKE_SUBMITTED（05:40:07）是 bot 重启后立即发出的，紧接着 05:40:09 出现「正在退出，撤销全部订单」，说明 bot 在启动 2 秒后又被关闭——BATCH_TAKE_SUBMITTED 在这个周期里只发了 1 次就退出了，没有后续 TAKE_EXECUTED

#### 事件统计（累计，零变化 vs 第 82 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

**所有 8 个已知异常持续不变：**

1. ⚠️ **持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串），导致 batch CLOSED 但 take_filled_size=0
2. ⚠️ **生命周期不完整** — 2 个 batch 只有 OPENED → TAKE_SUBMITTED（×206）→ TAKE_EXECUTED（×12），缺少 COMPLETED、SELL_PLACED、CLOSED、UNLOCKED
3. ⚠️ **DB 状态矛盾** — status=CLOSED，但 closed_ts=None，take_filled=0，exit_filled=0
4. ⚠️ **BATCH_TAKE_EXECUTED 数据异常** — batch2 的 filled=170.49/175.12 远超 target 88
5. ⚠️ **无 BATCH_TAKE_COMPLETED** — 所有 206 次 SUBMITTED 后 remaining 从未递减
6. 🔴 **两个 batch 永冻（~20h+）** — batch1 创建于 8/10 22:19，batch2 创建于 8/10 23:12
7. 🔴 **市场锁永不解锁** — MARKET_REWARD_EXIT_UNLOCKED 仍为 0
8. 🔴 **FORCED_EXIT 而非正常关闭** — bot 在 8/10 23:31:07 退出

#### 新增观察

- **Aug 11 05:40 bot 启动/退出时序**：bot 在 05:39-05:40:07 启动并加载持仓，发出 1 次 BATCH_TAKE_SUBMITTED 后，在 05:40:09 立即执行了「正在退出，撤销全部订单」，又于 05:55:14 重新启动。05:40 的那次启动仅存活了 ~2 秒就被关闭——BATCH_TAKE_SUBMITTED 还没来得及等 TAKE_EXECUTED 返回就被中断了。
- Aug 10 的 BATCH_TAKE_EXECUTED 分布：batch1 (1e5af08f) = 10 次，batch2 (02cafdd7) = 2 次。Batch2 target=88 但 2 次 EXECUTED filled=170.49 和 175.12——每次执行都超过了 target 数量且是 taker BUY，说明 FAK 订单被部分成交且超量填充（可能是订单簿深度导致的）。
- Batch1 (1e5af08f) 在所有 10 次 EXECUTED 中 remaining 都是 7，从未减少——说明虽然 FAK BUY 被提交且成交了，但代码认为 remaining 没有减少（可能是 FAK 订单成交后剩余数量没有正确更新到 batch 状态中）。
- reward_exit_orders 表始终为 0（没有 order 记录），FAK 订单未被记录到此表。

#### DB 状态详情（零变化）

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0, paired_loss=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9
reward_exit_orders: 0
```

---

## 第 82 轮（2026-08-11 16:06 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 81 轮相同，reward-exit 路径无变化 ~20h+，Bot 在线运行（FIFA 2030 + 逆向选择防护日志）

#### 关键发现

**与第 81 轮完全一致 — 零变化：**

| 指标 | 第 81 轮 | 第 82 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3196 | 3196 | 无（日志已到16:06但行数未变——可能日志被截断） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |

#### Bot 运行状态

- Bot 在 16:06:22 最后日志（16:05-16:06 段密集输出 P0 逆向选择防护日志，market="Will another city host the final of the"）
- **新观察**：Aug 11 日志最后几行全是「P0 逆向选择防护：NO=pull 决策已记录但报价不在 desired 中 (pull_cooldown)」，说明 FIFA 2030 host city 市场有活跃的逆向选择检测
- Bot 凌晨 05:39 启动，持续运行超过 10.5 小时
- 无 reward-exit 相关的任何新事件

#### 事件统计（累计，零变化 vs 第 81 轮）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

**所有 8 个已知异常持续不变：**

1. ⚠️ **持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串），导致 batch CLOSED 但 take_filled_size=0
2. ⚠️ **生命周期不完整** — 2 个 batch 只有 OPENED → TAKE_SUBMITTED（×206）→ TAKE_EXECUTED（×12），缺少 COMPLETED、SELL_PLACED、CLOSED、UNLOCKED
3. ⚠️ **DB 状态矛盾** — status=CLOSED，但 closed_ts=None，take_filled=0，exit_filled=0
4. ⚠️ **BATCH_TAKE_EXECUTED 数据异常** — batch2 的 filled=170.49/175.12 远超 target 88
5. ⚠️ **无 BATCH_TAKE_COMPLETED** — 所有 206 次 SUBMITTED 后 remaining 从未递减
6. 🔴 **两个 batch 永冻（~20h+）** — batch1 创建于 8/10 22:19，batch2 创建于 8/10 23:12
7. 🔴 **市场锁永不解锁** — MARKET_REWARD_EXIT_UNLOCKED 仍为 0
8. 🔴 **FORCED_EXIT 而非正常关闭** — bot 在 8/10 23:31:07 退出

#### DB 状态详情（零变化）

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9
reward_exit_orders: 0
```

#### 新观察

- Aug 11 日志最后段落（16:05-16:06）密集出现「P0 逆向选择防护：NO=pull 决策已记录但报价不在 desired 中」日志，市场为 "Will another city host the final of the"（FIFA 2030 host city 市场）。0.00 score (pull_cooldown) 表示逆向选择评分很低，正常行为。
- 与第 81 轮对比，Aug 11 日志行数从 3075 增至 3196（+121 行），全部为 ORDER_PLACED/CANCELLED 和逆向选择防护日志。

#### 结论

第 82 轮与第 81 轮 reward-exit 状态完全一致，零变化。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 40 轮约 10 小时。Bot 在线正常做市 + 逆向选择防护活跃（FIFA 2030 host city 市场有 pull_cooldown 检测），reward-exit 路径静默。所有已知问题的修复优先级不变。

修复优先顺序：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 81 轮（2026-08-11 15:55 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 80 轮相同，reward-exit 路径无变化 ~19.7h+，Bot 在线运行

#### 关键发现

**与第 80 轮完全一致 — 零变化：**

| 指标 | 第 80 轮 | 第 81 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 3018 | 3075 | +57 行（ORDER_PLACED/CANCELLED） |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |

#### Bot 运行状态

- Bot 运行超过 10.2 小时（05:39 启动），15:51 仍在运行（最后日志）
- 市场：LoL Esports GotY 2026 + 2030 FIFA World Cup
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh)
- SSL 错误 3 次（13:37, 13:37, 15:04），auto-recovered
- API key 错误 2 次（14:09, 14:37），auto-recovered
- 上午补单活动正常（Phase 1）：Ralph Norman（敞口=152）+ Grok 4.6（敞口=-36）

#### 事件统计（累计）

| 事件 | Aug 10 | Aug 11 | 合计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |

#### 异常分析

1. **⚠️ 持续异常：fill-to-batch 关联缺失** — 2 个 reward_exit_fills 记录的 `batch_id=""`（空字符串），导致 batch CLOSED 但 take_filled_size=0，说明生命周期事件（BATCH_TAKE_COMPLETED、CLOSED）没有被记录到数据库。所有 205 次 BATCH_TAKE_SUBMITTED 都显示 `size=7 remaining=7`（batch1）或 `size=88 remaining=88`（batch2），从未递减。

2. **⚠️ 生命周期不完整** — 2 个 batch 在日志中只有 OPENED → TAKE_SUBMITTED（×205）→ TAKE_EXECUTED（×12），缺少 BATCH_TAKE_COMPLETED、BATCH_SELL_PLACED、REWARD_EXIT_BATCH_CLOSED、MARKET_REWARD_EXIT_UNLOCKED。这些状态在代码中可能存在但从未被触发。

3. **DB 状态矛盾** — DB 中 status=CLOSED（2 个 batch），但 closed_ts=None，take_filled=0，exit_filled=0。说明 batch 在代码中被标记为 CLOSED 但未写入数据库（只在创建时写了 INSERT with status=CLOSED?）

4. **BATCH_TAKE_EXECUTED 数据异常** — batch 02cafdd7 的两次 EXECUTED 显示 `filled=170.49` 和 `175.12`，但 target 只有 88 股。这可能意味着 `filled` 字段是**累计填充数**（overfill FAK fill）而非实际买入量。

5. **无 BATCH_TAKE_COMPLETED** — 两个 batch 的目标量都很小（7 股和 88 股），但 205 次 TAKE_SUBMITTED 后仍显示 `remaining=7` 和 `remaining=88`，说明 FAK BUY 从未成交。原因可能是 complement token 没有卖方流动性（价格 0.99 几乎触及上限）。

#### DB 状态详情

```
reward_exit_batches: 2 (均 CLOSED)
  batch=reward-exit-1e5af08f: take_target=7.32, take_filled=0, exit_filled=0
    → 市场: Will Ralph Norman place second in first round of the South C
  batch=reward-exit-02cafdd7: take_target=87.8, take_filled=0, exit_filled=0
    → 市场: Will MrBeast's next video get between 60 and 70 mi
reward_exit_fills: 2 (batch_id="" 均为空)
  fill=1e5af08f: intent=normal_reward side=BUY price=0.52 size=3.66
  fill=02cafdd7: intent=normal_reward side=BUY price=0.57 size=43.9
reward_exit_orders: 0
```

#### 建议

- 需要 review `_close_reward_exit_batch()` 代码路径，确认 lifecycle 事件（COMPLETED/UNLOCKED）是否正确绑定
- 需要确保 fill 写入时 `batch_id` 字段被正确填充（当前为空字符串）
- 考虑在数据库中单独记录 batch lifecycle transitions（event log）
- BATCH_TAKE_EXECUTED 的 `remaining_before` 始终输出 `7` 和 `88`，疑似采用原始 target 而非实时 remaining（代码中可能取了 `take_target_size` 而非 `remaining = take_target_size - take_filled_size`）

---

## 第 80 轮（2026-08-11 15:36 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 Bot 在线正常运行，市场轮换（LoL Esports GotY 取代 Ülle Madise），reward-exit 路径完全冻结，零变化 ~19.4h+

#### 与第 79 轮对比

| 指标 | 第 79 轮 | 第 80 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 2965 | 3018 | +53 行：ORDER_PLACED/CANCELLED |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |
| Bot 市场 | Ülle Madise + 2030 FIFA World Cup | **LoL Esports GotY 2026** + 2030 FIFA World Cup | **Ülle Madise → LoL Esports GotY** |
| Bot 报价状态 | 在线 15:21:32 | 在线 15:34:20（最后日志） | 正常 |

#### Bot 运行状态

- Bot 在 15:34:20 最后日志（ORDER_PLACED + ORDER_CANCELLED expiry_refresh：LoL Esports GotY 2026 + 2030 FIFA World Cup）
- **市场轮换检测**：League of Legends Esports Game of the Year 2026 取代 Ülle Madise President of Estonia，约在 15:23 发生切换。2030 FIFA World Cup 保持不变。
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换，约每 2-3 分钟交替（两个市场）
- 运行持续超过 9.8 小时（05:39 启动），15:34 仍在运行
- 1 次 SSL EOF 错误（15:04:07，auto-recovered）
- 无 reward-exit 相关 ERROR

#### 事件统计（零变化 vs 第 79 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 (所有在 Aug 10) |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（Python sqlite3 直接查询，完全冻结，零变化 ~19.4h+）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 79 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~19.4h+ |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~18.5h+ |

#### 异常信号检测

🔴 **两个 batch 永冻（~19.4h+）**：无变化，自第 73 轮以来完全冻结。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，日志有 12 次 EXECUTED。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 38 轮无解**：自第 42 轮发现以来无变化。
🔴 **FORCED_EXIT 而非正常关闭**：bot 在 8/10 23:31:07 退出，两个 batch 从未进入 exit phase。
🟢 Bot 在线运行正常（15:34+ 报价 LoL Esports GotY 2026 + 2030 FIFA World Cup），reward-exit 路径静默。
🟢 **市场轮换正常**：Ülle Madise President of Estonia → League of Legends Esports GotY 2026，正常 scan 轮换。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟡 1 次 SSL EOF 错误（15:04:07），auto-recovered。

#### 结论

第 80 轮与第 79 轮 reward-exit 状态完全一致，零变化。唯一变化是 bot 报价市场轮换（Ülle Madise → League of Legends Esports GotY 2026），这是正常的 scan 轮换。reward-exit 两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 38 轮约 9.4+ 小时。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 79 轮（2026-08-11 15:22 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 Bot 在线正常运行，市场已轮换（Ülle Madise + 2030 FIFA World Cup），reward-exit 路径完全冻结，零变化 ~19.2h+

#### 与第 78 轮对比

| 指标 | 第 78 轮 | 第 79 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | ~2839 | 2965 | +126 行：ORDER_PLACED/CANCELLED |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |
| Bot 市场 | LoL Esports GotY + Ülle Madise | Ülle Madise + 2030 FIFA World Cup | **市场已轮换** |
| Bot 报价状态 | 在线 14:48+ | 在线 15:21:32 | 正常 |

#### Bot 运行状态

- Bot 在 15:21:32 最后日志（ORDER_PLACED + ORDER_CANCELLED expiry_refresh：Ülle Madise President of Estonia + 2030 FIFA World Cup）
- **市场轮换检测**：从 LoL Esports GotY + Ülle Madise 换为 Ülle Madise + another city host 2030 FIFA World Cup，约在 15:13 发生切换
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换
- 运行持续超过 10 小时（05:39 启动），15:21 仍在运行
- 无 reward-exit 相关 ERROR
- 无 SSL EOF 错误

#### 事件统计（零变化 vs 第 78 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 (所有在 Aug 10) |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（Python sqlite3 直接查询，完全冻结，零变化 ~19.2h+）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 78 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~19.2h+ |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~18.3h+ |

#### 异常信号检测

🔴 **两个 batch 永冻（~19.2h+）**：无变化，自第 73 轮以来完全冻结。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，日志有 12 次 EXECUTED。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 37 轮无解**：自第 42 轮发现以来无变化。
🔴 **FORCED_EXIT 而非正常关闭**：bot 在 8/10 23:31:07 退出，两个 batch 从未进入 exit phase。
🟢 Bot 在线运行正常（15:21+ 报价 Ülle Madise + 2030 FIFA World Cup），reward-exit 路径静默。
🟢 **市场轮换正常**：从 LoL Esports GotY 切换至 2030 FIFA World Cup final，无异常。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 无 SSL EOF 错误。

#### 结论

第 79 轮与第 78 轮 reward-exit 状态完全一致，零变化。唯一变化是 bot 报价市场轮换（LoL Esports GotY → 2030 FIFA World Cup），这是正常的 scan 轮换。reward-exit 两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 37 轮约 9.2+ 小时。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 77 轮（2026-08-11 14:49 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（LoL Esports GotY + Ülle Madise），reward-exit 路径完全冻结，零变化 ~18.5h+

#### 与第 76 轮对比

| 指标 | 第 76 轮 | 第 77 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | ~2750+ | 2839 | +~89 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |
| Bot 报价状态 | 在线 14:40+ | 在线 14:48+（最后日志） | 正常，市场不变 |

#### Bot 运行状态

- Bot 在 14:48:17 最后日志（ORDER_PLACED + ORDER_CANCELLED expiry_refresh：LoL Esports GotY + Ülle Madise President of Estonia）
- **市场与第 76 轮完全相同**：League of Legends Esports GotY 2026 + Ülle Madise President of Estonia，无轮换
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换，约每 2-3 分钟
- 运行持续超过 9 小时（05:39 启动）
- 无 reward-exit 相关 ERROR
- 无 SSL EOF 错误

#### 事件统计（零变化 vs 第 76 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 (所有在 Aug 10) |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（Python sqlite3 直接查询确认，完全冻结，零变化 ~18.5h+）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 76 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~18.5h+ |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~17.6h+ |

#### 异常信号检测

🔴 **两个 batch 永冻（~18.5h+）**：无变化，自第 73 轮以来完全冻结。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，日志有 12 次 EXECUTED。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 36 轮无解**：自第 42 轮发现以来无变化。
🔴 **FORCED_EXIT 而非正常关闭**：bot 在 8/10 23:31:07 退出，两个 batch 从未进入 exit phase。
🟢 Bot 在线运行正常（14:48+ 报价 LoL Esports GotY + Ülle Madise President of Estonia），reward-exit 路径静默。
🟢 **市场无轮换**：与第 76 轮相同的两个市场（LoL Esports GotY + Ülle Madise），正常行为。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 无 SSL EOF 错误。

#### 结论

第 77 轮与第 76 轮状态完全一致，零变化。本监控任务已持续 77 轮（约 35+ 小时）。reward-exit 路径仍完全冻结。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 36 轮约 8.8+ 小时。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 76 轮（2026-08-11 14:40 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（LoL Esports GotY + Ülle Madise），reward-exit 路径完全冻结，零变化 ~17.8h+

#### 与第 75 轮对比

| 指标 | 第 75 轮 | 第 76 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 2732 | ~2750+ | +~18 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |
| Bot 报价状态 | 在线 14:20+ | 在线 14:40+（最后日志） | 正常，市场不变 |

#### Bot 运行状态

- Bot 在 14:40:32 最后日志（ORDER_CANCELLED expiry_refresh：LoL Esports GotY + Ülle Madise President of Estonia）
- **市场与第 75 轮完全相同**：League of Legends Esports GotY 2026 + Ülle Madise President of Estonia，无轮换
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换，约每 2-3 分钟
- 运行持续超过 9 小时（05:39 启动）
- 无 reward-exit 相关 ERROR

#### 事件统计（零变化 vs 第 75 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 (所有在 Aug 10) |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（Python sqlite3 直接查询确认，完全冻结，零变化 ~17.8h+）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 75 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~18.0h+ |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~17.1h+ |

#### 异常信号检测

🔴 **两个 batch 永冻（~18.0h+）**：无变化，自第 73 轮以来完全冻结。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，日志有 12 次 EXECUTED。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 35 轮无解**：自第 42 轮发现以来无变化。
🔴 **FORCED_EXIT 而非正常关闭**：bot 在 8/10 23:31:07 退出，两个 batch 从未进入 exit phase。
🟢 Bot 在线运行正常（14:40+ 报价 LoL Esports GotY + Ülle Madise President of Estonia），reward-exit 路径静默。
🟢 **市场无轮换**：与第 75 轮相同的两个市场（LoL Esports GotY + Ülle Madise），正常行为。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。

#### 结论

第 76 轮与第 75 轮状态完全一致，零变化。本监控任务已持续 76 轮（约 34+ 小时）。reward-exit 路径仍完全冻结。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 35 轮约 8.5+ 小时。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 75 轮（2026-08-11 14:21 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（LoL Esports GotY + Ülle Madise），reward-exit 路径完全冻结，零变化 ~17.8h

#### 与第 74 轮对比

| 指标 | 第 74 轮 | 第 75 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 2716 | 2732 | +16 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |
| Bot 报价状态 | 在线 14:15+ | 在线 14:20+（最后日志） | 正常，市场不变 |

#### Bot 运行状态

- Bot 在 14:20:45 最后日志（ORDER_PLACED + ORDER_CANCELLED expiry_refresh：LoL Esports GotY + Ülle Madise President of Estonia）
- **市场与第 74 轮完全相同**：LoL Esports GotY 2026 + Ülle Madise President of Estonia，无轮换
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换，约每 2-3 分钟
- 运行持续超过 8.7 小时（05:39 启动）
- Aug 11 日志有 44 行 ERROR/CRITICAL（均为非 reward-exit 相关，如 SSL 连接错误、API 异常等）
- 无 reward-exit 相关 ERROR

#### 事件统计（零变化 vs 第 74 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 (所有在 Aug 10) |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（Python sqlite3 直接查询确认，完全冻结，零变化 ~17.8h）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 74 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~18.0h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~17.1h |

#### 异常信号检测

🔴 **两个 batch 永冻（~18.0h）**：无变化，自第 73 轮以来完全冻结。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，日志有 12 次 EXECUTED。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 34 轮无解**：自第 42 轮发现以来无变化。
🔴 **FORCED_EXIT 而非正常关闭**：bot 在 8/10 23:31:07 退出，两个 batch 从未进入 exit phase。
🟢 Bot 在线运行正常（14:20+ 报价 LoL Esports GotY + Ülle Madise President of Estonia），reward-exit 路径静默。
🟢 **市场无轮换**：与第 74 轮相同的两个市场（LoL Esports GotY + Ülle Madise），正常行为。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 本轮无 reward-exit 相关 ERROR。Aug 11 的 44 行 ERROR/CRITICAL 均为其他系统级错误（API 连接等）。

#### 结论

第 75 轮与第 74 轮状态完全一致，零变化。本监控任务已持续 75 轮（约 33+ 小时）。reward-exit 路径仍完全冻结。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 34 轮约 8.5 小时。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 74 轮（2026-08-11 14:20 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（LoL Esports GotY + Ülle Madise），reward-exit 路径完全冻结，零变化 ~17.8h

#### 与第 73 轮对比

| 指标 | 第 73 轮 | 第 74 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 2671 | 2716 | +45 行：ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |
| Bot 报价状态 | 在线 13:51+ | 在线 14:15+（最后日志） | 正常，市场已轮换 |

#### Bot 运行状态

- Bot 在 14:15:23 最后一次日志（ORDER_CANCELLED expiry_refresh + ORDER_PLACED LoL Esports GotY + Ülle Madise）
- **市场完全轮换**：第 73 轮的 Grok 4.6 + FIFA 2030 已被替换为 **League of Legends Esports GotY 2026** + **Ülle Madise President of Estonia**（14:10-14:15+ 活跃报价）
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换，约每 2-3 分钟
- 运行持续超过 8.5 小时（05:39 启动），最新一段从 08:46:11 持续至今 ~5.7h
- 无 ERROR、无 SSL EOF 错误

#### 事件统计（零变化 vs 第 73 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 (所有在 Aug 10) |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（完全冻结，零变化 ~17.8h）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 73 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~17.8h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~16.9h |

#### 异常信号检测

🔴 **两个 batch 永冻（~17.8h）**：无变化，自第 73 轮以来完全冻结。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，日志有 12 次 EXECUTED。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 33 轮无解**：自第 42 轮发现以来无变化。
🔴 **FORCED_EXIT 而非正常关闭**：bot 在 8/10 23:31:07 退出，两个 batch 从未进入 exit phase。
🟢 Bot 在线运行正常（14:15+ 报价 LoL Esports GotY + Ülle Madise President of Estonia），reward-exit 路径静默。
🟢 **市场轮换正常**：Grok 4.6/FIFA 2030 → LoL Esports GotY/Ülle Madise，正常行为。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 本轮无 SSL EOF 错误、无 ERROR。

#### 结论

第 74 轮与第 73 轮状态完全一致，零变化。本监控任务已持续 74 轮（约 32+ 小时）。唯一变化是市场轮换（Grok 4.6 → LoL Esports GotY + Ülle Madise President of Estonia），reward-exit 路径仍完全冻结。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 33 轮约 8.3 小时。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 73 轮（2026-08-11 13:51 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（Grok 4.6 + FIFA 2030），reward-exit 路径完全冻结，零变化 ~17.6h

#### 与第 72 轮对比

| 指标 | 第 72 轮 | 第 73 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 2525 | 2671 | +146 行：ORDER_PLACED/CANCELLED + 2 RECOVERY_EXECUTED (Grok 4.6) |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |
| Bot 报价状态 | 在线 13:35+ | 在线 13:51+（最后日志） | 正常，报价 Grok 4.6 + 其他市场 |

#### Bot 运行状态

- Bot 在 13:51:49 最后一次日志（ORDER_PLACED Grok 4.6 score 1460+ @ 0.570/0.400 + merging 66 pairs Grok 4.6 released by Aug 13）
- **新发现**：13:51:37 出现 2 次 RECOVERY_EXECUTED（Grok 4.6 released by Aug 13 市场，recovery 路径 buy_complement NO 36 @ 0.21，filled=30, expected_loss=0, actual_loss=0, stage=escalated），这是普通 recovery 而非 reward-exit
- Grok 4.6 released by Aug 13 mid 0.790 超出 [0.25, 0.75] 范围，bot 跳过报价
- Bot 正常活动：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换
- 运行持续超过 8 小时（05:39 启动），中间有短暂退出（05:40:09）+ 重启（05:55:14），最新一次运行从 08:46:11 持续至今 ~5.1h

#### 事件统计（Aug 10 + Aug 11 累计，零变化 vs 第 72 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 (所有在 Aug 10) |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（零变化）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 72 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~17.6h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~16.7h |

#### 异常信号检测

🔴 **两个 batch 永冻（~17.6h）**：batch1 创建于 8/10 22:19，batch2 于 8/10 23:12。DB 状态 CLOSED 但无对应 CLOSED/UNLOCKED 日志、closed_ts=None、take_filled_size=0.0。状态冻结不变，持续到第 73 轮监控。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。两个市场均已加入 banned 列表，但 LOCKED/UNLOCKED 日志计数不匹配（4 LOCKED, 0 UNLOCKED）。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，但日志显示 batch1 有 10 次 EXECUTED（filled=14.80），batch2 有 2 次 EXECUTED（filled=170.49/175.12）。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 32 轮无解**：自第 42 轮发现以来无变化。closed_ts=None，无 REWARD_EXIT_BATCH_CLOSED 日志。get_open_reward_exit_batches() 返回空。
🔴 **FORCED_EXIT 而非正常关闭**：bot 在 8/10 23:31:07 以"正在退出，撤销全部订单" 退出，两个 batch 因此从未进入 exit phase。
🟢 Bot 在线运行正常（13:51+ 报价 Grok 4.6 score 1460+ 市场），reward-exit 路径静默（无新 batch 触发）。
🟢 **新增活动**：13:51:37 出现 2 次 RECOVERY_EXECUTED（普通 recovery，Grok 4.6 released by Aug 13），与 reward-exit 无关。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 本轮无 SSL EOF 错误。
🟢 数据完整性无异常：无重复 origin_fill_id，无 orphan fill，无 orphan batch。

#### 结论

第 73 轮与第 72 轮状态完全一致，零变化。本监控任务已持续 73 轮（约 31+ 小时）。两个 reward-exit batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 32 轮约 7.9 小时。Bot 在线正常做市（Grok 4.6 score 1460+ 市场 + FIFA 2030），reward-exit 路径的根因问题仍未修复。本轮新增了 Grok 4.6 市场的普通 recovery 活动，不影响 reward-exit 路径。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 72 轮（2026-08-11 13:40 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（Grok 4.6 $400/天 + FIFA 2030），reward-exit 路径完全冻结，零变化 ~15.3h

#### 与第 71 轮对比

| 指标 | 第 71 轮 | 第 72 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 2475 | 2525 | +50 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12（确认） | 无 |
| Bot 报价状态 | 在线 13:22+ | 在线 13:35+（最后日志） | 正常，报价 Grok 4.6 + FIFA 2030 |

#### Bot 运行状态

- Bot 在 13:35:53 最后一次日志（ORDER_PLACED Grok 4.6 @ 0.680 + ORDER_CANCELLED expiry_refresh）
- **市场轮换**：Team Vitality LEC 2026 被替换为 **Grok 4.6**（奖励池 $400/天，评分 225.366），这是目前最高奖励市场
- 活跃报价：Grok 4.6（@ 0.290/0.680）+ FIFA 2030（@ 0.400/0.570）
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换
- 13:25 市场扫描触发（rotating out 6 tripped），Grok 4.6 加入
- 运行持续超过 7.7 小时（05:39 启动），中间有短暂退出（05:40:09）+ 重启（05:55:14），最新一次运行从 08:46:11 持续至今 ~4.9h

#### 事件统计（Aug 10 + Aug 11 累计，零变化 vs 第 71 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 (所有在 Aug 10) |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（零变化，Python sqlite3 直接查询确认）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 71 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~15.3h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~14.5h |

#### 异常信号检测

🔴 **两个 batch 永冻（~15.3h）**：batch1 创建于 8/10 22:19，batch2 于 8/10 23:12。DB 状态 CLOSED 但无对应 CLOSED/UNLOCKED 日志、closed_ts=None、take_filled_size=0.0。状态冻结不变，持续到第 72 轮监控。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。两个市场均已加入 banned 列表，但 LOCKED/UNLOCKED 日志计数不匹配（4 LOCKED, 0 UNLOCKED）。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，但日志显示 batch1 有 10 次 EXECUTED（filled=14.80），batch2 有 2 次 EXECUTED（filled=170.49/175.12）。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 31 轮无解**：自第 42 轮发现以来无变化。closed_ts=None，无 REWARD_EXIT_BATCH_CLOSED 日志。get_open_reward_exit_batches() 返回空。
🔴 **FORCED_EXIT 而非正常关闭**：bot 在 8/10 23:31:07 以"正在退出，撤销全部订单" 退出，两个 batch 因此从未进入 exit phase。
🟢 Bot 在线运行正常（13:35+ 报价 Grok 4.6 + FIFA 2030），reward-exit 路径静默（无新 batch 触发）。
🟢 **新市场加入**：Grok 4.6（奖励池 $400/天，评分 225.366）取代 Team Vitality LEC 2026，成为最高奖励市场。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 本轮无 SSL EOF 错误。
🟢 数据完整性无异常：无重复 origin_fill_id，无 orphan fill，无 orphan batch。

#### 结论

第 72 轮与第 71 轮状态完全一致，零变化。本监控任务已持续 72 轮（约 30+ 小时）。两个 reward-exit batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 31 轮约 7.4 小时。Bot 在线正常做市（Grok 4.6 + FIFA 2030），新市场 Grok 4.6 以 $400/天奖励池加入，但 reward-exit 路径的根因问题仍未修复。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 71 轮（2026-08-11 13:22 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（Team Vitality LEC 2026 + FIFA 2030），reward-exit 路径完全冻结，零变化 ~15.0h

#### 与第 70 轮对比

| 指标 | 第 70 轮 | 第 71 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | 2414 | 2475 | +61 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 13:04+ | 在线 13:22+ | 正常，报价 Team Vitality LEC 2026 + FIFA 2030 |

#### Bot 运行状态

- Bot 在 13:22:39 仍在活跃报价：Team Vitality LEC 2026 Summer Split（@ 0.360）+ FIFA 2030（@ 0.400/0.570）
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换
- Bot 不扫描/报价两个 batch 市场（均已列入 banned_markets.json）
- 截至 13:22+ 仍在线运行，无 ERROR

#### 事件统计（Aug 10 + Aug 11 累计，完全无变化 vs 第 70 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（零变化，Python sqlite3 直接查询确认）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 70 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~15.0h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~14.1h |

#### 异常信号检测

🔴 **两个 batch 永冻（~15.0h）**：batch1 创建于 8/10 22:19，batch2 于 8/10 23:12。DB 状态 CLOSED 但无对应 CLOSED/UNLOCKED 日志、closed_ts=None、take_filled_size=0.0。状态冻结不变，持续到第 71 轮监控。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。两个市场均已加入 banned 列表，但 LOCKED/UNLOCKED 日志计数不匹配（4 LOCKED, 0 UNLOCKED）。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，但日志显示 batch1 有 10 次 EXECUTED（filled=14.80），batch2 有 2 次 EXECUTED（filled=170.49/175.12）。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 30 轮无解**：自第 42 轮发现以来无变化。closed_ts=None，无 REWARD_EXIT_BATCH_CLOSED 日志。get_open_reward_exit_batches() 返回空。
🔴 **FORCED_EXIT 而非正常关闭**：bot 在 8/10 23:31:07 以"正在退出，撤销全部订单" 退出，两个 batch 因此从未进入 exit phase。
🟢 Bot 在线运行正常（13:22+ 报价 Team Vitality LEC 2026 + FIFA 2030），reward-exit 路径静默（无新 batch 触发）。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 本轮无 SSL EOF 错误。

#### 结论

第 71 轮与第 70 轮状态完全一致，零变化。本监控任务已持续 71 轮（约 29+ 小时）。两个 reward-exit batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 30 轮约 7.2 小时。Bot 在线正常做市（Team Vitality LEC 2026 + FIFA 2030），但 reward-exit 路径的根因问题仍未修复。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 70 轮（2026-08-11 13:05 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（Team Vitality LEC 2026 + FIFA 2030），reward-exit 路径完全冻结，零变化 ~14.8h

#### 与第 69 轮对比

| 指标 | 第 69 轮 | 第 70 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| Aug 11 日志行数 | ~2350+ | 2414 | +~64 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 12:51+ | 在线 13:04+ | 正常，报价 Team Vitality LEC 2026 + FIFA 2030 |

#### Bot 运行状态

- Bot 在 13:01-13:04 段持续正常报价：Team Vitality LEC 2026 Summer Split（@ 0.360/0.610）+ FIFA 2030（@ 0.400/0.570）
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换
- Bot 不扫描/报价两个 batch 市场（均已列入 banned_markets.json）
- 截至 13:04+ 仍在线运行，日志最后一行 13:04:35，无 ERROR

#### 日志事件重检（本轮对 Aug 10 日志完整扫描确认）

**Aug 10（12208 行）**：
- REWARD_EXIT_BATCH_OPENED: 2（batch1 @ 22:19:15, batch2 @ 23:12:59）
- BATCH_TAKE_SUBMITTED: 205（batch1: 91, batch2: 114）
- BATCH_TAKE_EXECUTED: 12（batch1: 10 次 filled=14.80, batch2: 2 次 filled=170.49/175.12）
- MARKET_REWARD_EXIT_LOCKED: 4（batch1: 3, batch2: 1）
- COMPLETED/PARTIAL/SELL/CLOSED/UNLOCKED/MANUAL_HOLD/LOSS_LOCKED: 均为 0

**新发现：余额耗尽死循环确认**
对 Aug 10 日志末尾 (23:28-23:31) 的详细分析确认了死循环机制：
- 23:28 后 batch1 (Ralph Norman) 和 batch2 (MrBeast) **同时**进入密集 BATCH_TAKE_SUBMITTED 循环
- 23:30:08 batch2 首次报余额不足：`balance: 5046679, order amount: 86963450`（需要 $869.63，余额仅 $50.47）
- 23:30:15 batch1 也报余额不足：`balance: 5046679, order amount: 7252890`（需要 $72.53）
- 两个 batch 交替 THROTTLE 提交，每次都失败（余额不足），持续到 23:31:07
- 23:31:07 bot 收到退出信号 → 撤销全部订单 → 23:31:08 ORDER_CANCEL_ALL

**这是 FORCED_EXIT 而非正常流程结束**：bot 在"正在退出，撤销全部订单" 日志中退出。两个 batch 均未完成 TAKE phase（remaining 从未降到 0），也从未进入 exit phase。

**Aug 11（2414 行）**：
- 仅 1 次 BATCH_TAKE_SUBMITTED（05:40:07, batch1）
- 05:40 之后全是正常做市活动，~5.5h 无 reward-exit 事件

#### 事件统计（Aug 10 + Aug 11 累计，完全无变化 vs 第 69 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（零变化，Python sqlite3 直接查询确认）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 69 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~14.8h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~13.9h |

#### 异常信号检测

🔴 **两个 batch 永冻（~14.8h）**：batch1 创建于 8/10 22:19，batch2 于 8/10 23:12。DB 状态 CLOSED 但无对应 CLOSED/UNLOCKED 日志、closed_ts=None、take_filled_size=0.0。状态冻结不变，持续到第 70 轮监控。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。两个市场均已加入 banned 列表，但 LOCKED/UNLOCKED 日志计数不匹配（4 LOCKED, 0 UNLOCKED）。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，但日志显示 batch1 有 10 次 EXECUTED（filled=14.80），batch2 有 2 次 EXECUTED（filled=170.49/175.12）。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 29 轮无解**：自第 42 轮发现以来无变化。closed_ts=None，无 REWARD_EXIT_BATCH_CLOSED 日志。get_open_reward_exit_batches() 返回空。
🔴 **新确认：FORCED_EXIT 而非正常关闭**：bot 在 8/10 23:31:07 日志中明确输出"正在退出，撤销全部订单"—这是 bot 主动退出（可能是余额不足触发的保护退出），而非正常流程。两个 batch 因此从未进入 exit phase。
🟢 Bot 在线运行正常（13:04+ 报价 Team Vitality LEC 2026 + FIFA 2030），reward-exit 路径静默（无新 batch 触发）。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 本轮无 SSL EOF 错误。

#### 结论

第 70 轮与第 69 轮状态完全一致，零变化。本监控任务已持续 70 轮（约 28+ 小时）。本轮对 Aug 10 日志末尾的详细分析确认了余额耗尽死循环的机制：两个 batch 在 23:28-23:31 间同时密集提交 FAK BUY，交替耗尽余额（$50.47），最终 bot 以 FORCED_EXIT 退出。这意味着两个 batch 的 CLOSED 状态是 bot 退出时的代码路径产生的（而非数据异常跳变），但 closed_ts 未写入、exit phase 未执行仍是 bug。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 69 轮（2026-08-11 12:51 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（Team Vitality LEC 2026 + FIFA 2030），reward-exit 路径完全冻结，零变化 ~14.6h

#### 与第 68 轮对比

| 指标 | 第 68 轮 | 第 69 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志行数 | 2315 | ~2350+ | +35 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 12:36+ | 在线 12:51+ | 正常，报价 Team Vitality LEC 2026 + FIFA 2030 |

#### Bot 运行状态

- Bot 在 12:51:19 最后一次 ORDER_PLACED：Team Vitality LEC 2026 Summer Split（@ 0.360）+ FIFA 2030（@ 0.400/0.570）
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换
- Bot 不扫描/报价两个 batch 市场（均已列入 banned_markets.json）
- 截至 12:51+ 仍在线运行，无 ERROR

#### 事件统计（Aug 10 + Aug 11 累计，完全无变化 vs 第 68 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（零变化，Python sqlite3 直接查询确认）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 68 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~14.6h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~13.7h |

#### 异常信号检测

🔴 **两个 batch 永冻（~14.6h）**：batch1 创建于 8/10 22:19，batch2 于 8/10 23:12。DB 状态 CLOSED 但无对应 CLOSED/UNLOCKED 日志、closed_ts=None、take_filled_size=0.0。状态冻结不变，持续到第 69 轮监控。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。两个市场均已加入 banned 列表，但 LOCKED/UNLOCKED 日志计数不匹配（4 LOCKED, 0 UNLOCKED）。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，但日志显示 batch1 有 10 次 EXECUTED（filled=14.80），batch2 有 2 次 EXECUTED（filled=170.49/175.12）。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 28 轮无解**：自第 42 轮发现以来无变化。closed_ts=None，无 REWARD_EXIT_BATCH_CLOSED 日志。get_open_reward_exit_batches() 返回空。
🟢 Bot 在线运行正常（12:51+ 报价 Team Vitality LEC 2026 + FIFA 2030），reward-exit 路径静默（无新 batch 触发）。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 本轮无 SSL EOF 错误。

#### 结论

第 69 轮与第 68 轮状态完全一致，零变化。本监控任务已持续 69 轮（约 28+ 小时），reward-exit 的 2 个历史 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 28 轮约 6.7 小时。Bot 在线正常做市（Team Vitality LEC 2026 + FIFA 2030），但 reward-exit 路径的根因问题仍未修复。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（代码路径 vs 直接 DB 操作）

---

## 第 68 轮（2026-08-11 12:37 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（Team Vitality LEC 2026 + FIFA 2030），reward-exit 路径完全冻结，零变化 ~14.3h

#### 与第 67 轮对比

| 指标 | 第 67 轮 | 第 68 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志行数 | 2266 | 2315 | +49 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 12:23+ | 在线 12:36+ | 正常，报价 Team Vitality LEC 2026 + FIFA 2030 |

#### Bot 运行状态

- Bot 在 12:36:24 最后一次 ORDER_PLACED：Team Vitality LEC 2026 Summer Split（@ 0.360/0.610）+ FIFA 2030（@ 0.400/0.570）
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换，约每 2-3 分钟轮换
- Bot 不扫描/报价两个 batch 市场（均已列入 banned_markets.json）
- 截至 12:36+ 仍在线运行，无 ERROR
- 12:33-12:36 期间连续正常做市，无 SSL EOF 错误

#### 事件统计（Aug 10 + Aug 11 累计，完全无变化 vs 第 67 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（零变化，Python sqlite3 直接查询确认）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 67 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~14.3h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~13.4h |

#### 异常信号检测

🔴 **两个 batch 永冻（~14.3h）**：batch1 创建于 8/10 22:19，batch2 于 8/10 23:12。DB 状态 CLOSED 但无对应 CLOSED/UNLOCKED 日志、closed_ts=None、take_filled_size=0.0。状态冻结不变，持续到第 68 轮监控。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。两个市场均已加入 banned 列表，但 LOCKED/UNLOCKED 日志计数不匹配（4 LOCKED, 0 UNLOCKED）。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，但日志显示 batch1 有 10 次 EXECUTED（filled=14.80），batch2 有 2 次 EXECUTED（filled=170.49/175.12）。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 27 轮无解**：自第 42 轮发现以来无变化。closed_ts=None，无 REWARD_EXIT_BATCH_CLOSED 日志。get_open_reward_exit_batches() 返回空。
🟢 Bot 在线运行正常（12:36+ 报价 Team Vitality LEC 2026 + FIFA 2030），reward-exit 路径静默（无新 batch 触发）。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 本轮无 SSL EOF 错误。

#### 结论

第 68 轮与第 67 轮状态完全一致，零变化。本监控任务已持续 68 轮（约 27+ 小时），reward-exit 的 2 个历史 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 27 轮约 6 小时。Bot 在线正常做市（Team Vitality LEC 2026 + FIFA 2030），但 reward-exit 路径的根因问题仍未修复。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（代码路径 vs 直接 DB 操作）

---

## 第 67 轮（2026-08-11 12:25 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（FIFA 2030 + Team Vitality LEC 2026），reward-exit 路径完全冻结，零变化 ~14.1h

#### 与第 66 轮对比

| 指标 | 第 66 轮 | 第 67 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志行数 | 2211 | 2266 | +55 行：仅 ORDER_PLACED/CANCELLED + 市场扫描 |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 12:06+ | 在线 12:23+ | 正常，见下方 bot 状态 |

#### Bot 运行状态

- Bot 在 12:22:52 触发市场扫描（rotating out 6 tripped）
- 12:23:15 重新开始报价：Team Vitality LEC 2026 Summer Split（奖励池 $204/天，评分 60.995）+ FIFA 2030 World Cup（奖励池 $135/天，评分 13.027）
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换，约每 2 分钟轮换
- 偶发 SSL EOF 错误：12:20:31 + 12:21:30 两次「查询持仓市场失败」，但 bot 自动恢复，后续正常报价
- Bot 不扫描/报价两个 batch 市场（均已列入 banned_markets.json）
- 截至 12:23+ 仍在线运行，无 ERROR

#### 事件统计（Aug 10 + Aug 11 累计，完全无变化 vs 第 66 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（零变化，Python sqlite3 直接查询确认）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 66 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~14.1h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~13.2h |

#### 异常信号检测

🔴 **两个 batch 永冻（~14.1h）**：batch1 创建于 8/10 22:19，batch2 于 8/10 23:12。DB 状态 CLOSED 但无对应 CLOSED/UNLOCKED 日志、closed_ts=None、take_filled_size=0.0。状态冻结不变，持续到第 26 轮监控。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。两个市场均已加入 banned 列表，但 LOCKED/UNLOCKED 日志计数不匹配（4 LOCKED, 0 UNLOCKED）。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，但日志显示 batch1 有 10 次 EXECUTED（filled=14.80），batch2 有 2 次 EXECUTED（filled=170.49/175.12）。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 26 轮无解**：自第 42 轮发现以来无变化。closed_ts=None，无 REWARD_EXIT_BATCH_CLOSED 日志。get_open_reward_exit_batches() 返回空。
🟢 Bot 在线运行正常（12:23+ 报价 FIFA 2030 + Team Vitality LEC 2026），reward-exit 路径静默（无新 batch 触发）。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟡 SSL EOF 错误偶发（12:20:31 + 12:21:30），Polymarket API 瞬时中断，bot 自动恢复。

#### 结论

第 67 轮与第 66 轮状态完全一致，零变化。本监控任务已持续 67 轮（约 27 小时），reward-exit 的 2 个历史 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 26 轮约 6 小时。Bot 在线正常做市（FIFA 2030 + Team Vitality LEC 2026），但 reward-exit 路径的根因问题仍未修复。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（代码路径 vs 直接 DB 操作）

---

## 第 66 轮（2026-08-11 12:08 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（FIFA 2030 + Team Vitality LEC 2026），reward-exit 路径完全冻结

#### 与第 65 轮对比

| 指标 | 第 65 轮 | 第 66 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志行数 | 2154 | 2211 | +57 行：仅 ORDER_PLACED/CANCELLED + 市场扫描 |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 11:42-11:50+ | 在线 12:06+ | 正常，见下方 bot 状态 |

#### Bot 运行状态

- Bot 在 12:06:30 触发市场扫描（rotating out 6 tripped）
- 12:07:45 重新开始报价：Team Vitality LEC 2026 Summer Split + FIFA 2030 World Cup
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换
- Bot 不扫描/报价两个 batch 市场（均已列入 banned_markets.json）
- 截至 12:08 仍在线运行，无 ERROR

#### 事件统计（Aug 10 + Aug 11 累计）

| 事件 | 次数 | vs 第 65 轮 |
|------|------|-------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | 206 (205+1) | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

#### DB 状态（零变化）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~13.8h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~12.9h |

#### 异常信号检测

🔴 **两个 batch 永冻**：batch1 创建于 8/10 22:19（~13.8h 前），batch2 创建于 8/10 23:12（~12.9h 前）。DB 状态 CLOSED 但无对应 CLOSED/UNLOCKED 日志、closed_ts=None、take_filled_size=0.0。状态冻结不变。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。两个市场均已加入 banned 列表，但 LOCKED/UNLOCKED 日志计数不匹配（4 LOCKED, 0 UNLOCKED）。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，但日志显示 batch1 有 10 次 EXECUTED（filled=14.80），batch2 有 2 次 EXECUTED（filled=170.49/175.12）。
🟢 Bot 在线运行正常，reward-exit 路径静默（无新 batch 触发）。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。

#### 结论

状态与第 65 轮完全冻结，零变化。Bot 在线正常做市（FIFA 2030 + Team Vitality LEC 2026），但 reward-exit 的两个历史 batch 已进入永久冻结态（closed_ts=None, 无 UNLOCKED, 无 CLOSED 日志）。这两个市场已在 banned 列表中，bot 不会再接触它们。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能从 7 降到 0（而非卡在 7 forever）
2. **P0**: take 前检查余额 → batch1 可能余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量

---

## 第 65 轮（2026-08-11 11:55 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（Team Vitality LEC 2026 + FIFA 2030），reward-exit 路径完全冻结

#### 与第 64 轮对比

| 指标 | 第 64 轮 | 第 65 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志行数 | 2108 | 2154 | +46 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 11:32+ | 在线 11:42-11:50+ | 正常，见下方 bot 状态 |

#### Bot 运行状态

- Bot 在 11:42-11:50+ 正常报价两个市场：Team Vitality LEC 2026 Summer Split + FIFA 2030 World Cup
- 报价循环正常：ORDER_PLACED → expiry_refresh → ORDER_CANCELLED → ORDER_PLACED，约每 2-3 分钟轮换
- Bot 不扫描/报价两个 batch 市场（均已列入 banned_markets.json）
- 日志中 794 行 WARNING，但均为正常做市活动（market_mid更新等），无 ERROR

#### 事件统计（Aug 10 + Aug 11 累计）

| 事件 | 次数 | vs 第 64 轮 |
|------|------|-------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | 206 (205+1) | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

#### 异常信号检测

🔴 **两个 batch 永冻**：batch1 创建于 8/10 22:19（~13.6h 前），batch2 创建于 8/10 23:12（~12.7h 前）。DB 状态 CLOSED 但无对应 CLOSED/UNLOCKED 日志、closed_ts=None、take_filled_size=0.0。状态冻结不变。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。两个市场均已加入 banned 列表，但 LOCKED/UNLOCKED 日志计数不匹配（4 LOCKED, 0 UNLOCKED）。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，但日志显示 batch1 有 12 次 EXECUTED（filled=14.80），batch2 有 2 次 EXECUTED（filled=170.49/175.12）。
🟢 Bot 在线运行正常，reward-exit 路径静默（无新 batch 触发）。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。

#### 结论

状态与第 64 轮相比完全冻结，零变化。Bot 在线正常做市，但 reward-exit 的两个历史 batch 已进入永久冻结态（closed_ts=None, 无 UNLOCKED, 无 CLOSED 日志）。这两个市场已在 banned 列表中，bot 不会再接触它们。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能从 7 降到 0（而非卡在 7 forever）
2. **P0**: take 前检查余额 → batch1 可能余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量

---

## 第 64 轮（2026-08-11 11:37 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（11:32+ 报价 Team Vitality LEC 2026 + FIFA 2030），reward-exit 路径完全冻结，零变化

#### 与第 63 轮对比

| 指标 | 第 63 轮 | 第 64 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志行数 | 2055 | 2108 | +53 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 11:21+ | 在线 11:32+ | 正常，报价 Team Vitality LEC 2026 + FIFA 2030 |

#### 日志事件统计（本轮重新验证）

**Aug 10 日志**（无变化）：
- 2 OPENED, 205 BATCH_TAKE_SUBMITTED, 12 BATCH_TAKE_EXECUTED, 4 MARKET_REWARD_EXIT_LOCKED
- 仍为零：TAKE_COMPLETED(0), TAKE_PARTIAL(0), SELL_PLACED(0), SELL_PARTIAL(0), CLOSED(0), UNLOCKED(0), MANUAL_HOLD(0), LOSS_LOCKED(0)

**Aug 11 日志**（2108 行）：
- 仅有 1 次 BATCH_TAKE_SUBMITTED（batch1, 05:40:07），无新事件
- +53 行全部为正常做市活动：ORDER_PLACED/ORDER_CANCELLED 轮换
- Bot 在 11:32-11:35 正常报价 Team Vitality LEC 2026 + FIFA 2030
- 11:35:45 触发市场扫描（rotating out 6 tripped），正常行为
- 仅 1 次 WARNING「净收益缓存后台刷新失败（5s timeout）」于 11:36:04
- 无 ERROR（reward-exit 相关 0）

#### DB 状态（Python sqlite3 直接查询，零变化）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~13.3h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~12.4h |

#### 异常信号检测（第 64 轮）

🔴 **P0 — 严重问题不变**：
1. 余额耗尽 + FAK 过量成交
2. 双 batch 并发无余额协调
3. 永久禁入生效
4. Batch 状态跳变 TAKE_PENDING→CLOSED（`closed_ts=None`）
5. Exit phase 从未执行
6. FAK 成交 fill 未写入 DB（`take_filled_size=0.0`）
7. Fill-to-batch 链接为空（`batch_id=""`）
8. 市场锁永不解锁

🟢 **正面信号**：
1. Bot 持续正常运行（11:32+ 报价 Team Vitality LEC 2026 + FIFA 2030）
2. 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id
3. 无新 ERROR 产生
4. 无新 reward-exit 触发
5. DB 状态稳定，无数据漂移
6. 市场扫描正常（11:35:45 触发 rotating out 6 tripped）

#### 分析

本轮与第 63 轮状态完全一致。唯一变化是 Aug 11 日志增加了 53 行普通做市活动。bot 在 11:32-11:35+ 正常报价 Team Vitality LEC 2026 + FIFA 2030，11:35:45 触发了市场扫描（rotating out 6 tripped），扫描后继续报价相同的两个市场。

两个 Batch 的 orphan 时长 —— batch1: ~13.3h, batch2: ~12.4h，持续增长。

**核心结论保持不变**：两个 reward-exit batch 的 root cause 仍未修复。当前状态可接受（2 市场已禁入防进一步损失，bot 正常运行），但累积损失约 $493+。

---

## 第 63 轮（2026-08-11 11:22 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（11:21+ 报价 Team Vitality LEC 2026 + FIFA 2030），reward-exit 路径完全冻结，零变化

#### 与第 62 轮对比

| 指标 | 第 62 轮 | 第 63 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志行数 | 2008 | 2055 | +47 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 11:06+ | 在线 11:21+ | 正常，报价 Team Vitality LEC 2026 + FIFA 2030 |

#### 日志事件统计（本轮重新验证）

**Aug 10 日志**（无变化）：
- 2 OPENED, 205 BATCH_TAKE_SUBMITTED, 12 BATCH_TAKE_EXECUTED, 4 MARKET_REWARD_EXIT_LOCKED
- 仍为零：TAKE_COMPLETED(0), TAKE_PARTIAL(0), SELL_PLACED(0), SELL_PARTIAL(0), CLOSED(0), UNLOCKED(0), MANUAL_HOLD(0), LOSS_LOCKED(0)

**Aug 11 日志**（2055 行）：
- 仅有 1 次 BATCH_TAKE_SUBMITTED（batch1, 05:40:07），无新事件
- +47 行全部为正常做市活动：ORDER_PLACED/ORDER_CANCELLED 轮换
- Bot 在 11:20:38 开始报价 Team Vitality LEC 2026 + FIFA 2030（市场轮换）
- 11:21:32-33 正常 ORDER_PLACED/ORDER_CANCELLED（expiry_refresh）循环
- 仅 1 次 WARNING「净收益缓存后台刷新失败（5s timeout）」于 11:20:43
- 无 ERROR（reward-exit 相关 0）

#### DB 状态（Python sqlite3 直接查询，零变化）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~13.0h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~12.1h |

#### 异常信号检测（第 63 轮）

🔴 **P0 — 严重问题不变**：
1. 余额耗尽 + FAK 过量成交
2. 双 batch 并发无余额协调
3. 永久禁入生效
4. Batch 状态跳变 TAKE_PENDING→CLOSED（`closed_ts=None`）
5. Exit phase 从未执行
6. FAK 成交 fill 未写入 DB（`take_filled_size=0.0`）
7. Fill-to-batch 链接为空（`batch_id=""`）
8. 市场锁永不解锁

🟢 **正面信号**：
1. Bot 持续正常运行（11:21+ 报价 Team Vitality LEC 2026 + FIFA 2030）
2. 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id
3. 无新 ERROR 产生
4. 无新 reward-exit 触发
5. DB 状态稳定，无数据漂移

#### 分析

本轮与第 62 轮状态完全一致。唯一变化是 Aug 11 日志增加了 47 行普通做市活动。bot 在 11:20-11:21 进行了市场轮换（Team Vitality LEC 2026 替换了之前的报价市场），目前活跃报价 Team Vitality LEC 2026 + FIFA 2030。

两个 Batch 的 orphan 时长 —— batch1: ~13.0h, batch2: ~12.1h，持续增长。

**核心结论保持不变**：两个 reward-exit batch 的 root cause 仍未修复。当前状态可接受（2 市场已禁入防进一步损失，bot 正常运行），但累积损失约 $493+。

---

## 第 62 轮（2026-08-11 11:08 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（11:06+ 报价 FIFA 2030），reward-exit 路径完全冻结，零变化

#### 与第 61 轮对比

| 指标 | 第 61 轮 | 第 62 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志行数 | 1932 | 2008 | +76 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 10:49+ | 在线 11:06+ | 正常，报价 FIFA 2030 + Team Vitality LEC 2026 |

#### 日志事件统计（本轮重新验证）

**Aug 10 日志**（无变化）：
- 2 OPENED, 205 BATCH_TAKE_SUBMITTED, 12 BATCH_TAKE_EXECUTED, 4 MARKET_REWARD_EXIT_LOCKED
- 仍为零：TAKE_COMPLETED(0), TAKE_PARTIAL(0), SELL_PLACED(0), SELL_PARTIAL(0), CLOSED(0), UNLOCKED(0), MANUAL_HOLD(0), LOSS_LOCKED(0)

**Aug 11 日志**（2008 行）：
- 仅有 1 次 BATCH_TAKE_SUBMITTED（batch1, 05:40:07），无新事件
- +76 行全部为正常做市活动：ORDER_PLACED/ORDER_CANCELLED 轮换
- Bot 在 11:06:36 仍在活跃报价 FIFA 2030 + Team Vitality LEC 2026
- 无 ERROR（reward-exit 相关 0）

#### DB 状态（Python sqlite3 直接查询，零变化）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~12.8h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~11.9h |

#### 异常信号检测（第 62 轮）

🔴 **P0 — 严重问题不变**：
1. 余额耗尽 + FAK 过量成交
2. 双 batch 并发无余额协调
3. 永久禁入生效
4. Batch 状态跳变 TAKE_PENDING→CLOSED（`closed_ts=None`）
5. Exit phase 从未执行
6. FAK 成交 fill 未写入 DB（`take_filled_size=0.0`）
7. Fill-to-batch 链接为空（`batch_id=""`）
8. 市场锁永不解锁

🟢 **正面信号**：
1. Bot 持续正常运行（11:06+ 报价 FIFA 2030 + Team Vitality LEC 2026）
2. 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id
3. 无新 ERROR 产生
4. 无新 reward-exit 触发
5. DB 状态稳定，无数据漂移

#### 分析

本轮与第 61 轮状态完全一致。唯一变化是 Aug 11 日志增加了 76 行普通做市活动。bot 在 11:06+:36 仍在正常报价 FIFA 2030 + Team Vitality LEC 2026 两个市场。reward-exit 路径已完全冻结无变化。

两个 Batch 的 orphan 时长 —— batch1: ~12.8h, batch2: ~11.9h，持续增长。

**核心结论保持不变**：两个 reward-exit batch 的 root cause 仍未修复。当前状态可接受（2 市场已禁入防进一步损失，bot 正常运行），但累积损失约 $493+。

---

## 第 61 轮（2026-08-11 10:53 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线稳定运行，reward-exit 路径完全冻结，零变化

#### 与第 60 轮对比

| 指标 | 第 60 轮 | 第 61 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志行数 | 1887 | 1932 | +45 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 10:37+ | 在线 10:49+ | 正常，市场轮换扫描 |

#### 日志事件统计（本轮重新验证）

**Aug 10 日志**（无变化）：
- 2 OPENED, 205 BATCH_TAKE_SUBMITTED, 12 BATCH_TAKE_EXECUTED, 4 MARKET_REWARD_EXIT_LOCKED
- 仍为零：TAKE_COMPLETED(0), TAKE_PARTIAL(0), SELL_PLACED(0), SELL_PARTIAL(0), CLOSED(0), UNLOCKED(0), MANUAL_HOLD(0), LOSS_LOCKED(0)

**Aug 11 日志**（1932 行）：
- 仅有 1 次 BATCH_TAKE_SUBMITTED（batch1, 05:40:07），无新事件
- +45 行全部为正常做市活动：ORDER_PLACED/ORDER_CANCELLED 轮换
- Bot 在 10:36-10:49 段活跃报价 FIFA 2030 + Team Vitality LEC 2026
- 10:49:39 触发市场扫描：重新评估奖励池，当前选中 2 个市场（FIFA 2030 + Team Vitality LEC 2026）
- 10:49:59 出现 1 次 WARNING「净收益缓存后台刷新失败（5s timeout）」
- 无 ERROR（reward-exit 相关 0）

#### DB 状态（Python sqlite3 直接查询，零变化）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~12.6h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~11.7h |

#### 异常信号检测（第 61 轮）

🔴 **P0 — 严重问题不变**：
1. 余额耗尽 + FAK 过量成交
2. 双 batch 并发无余额协调
3. 永久禁入生效
4. Batch 状态跳变 TAKE_PENDING→CLOSED（`closed_ts=None`）
5. Exit phase 从未执行
6. FAK 成交 fill 未写入 DB（`take_filled_size=0.0`）
7. Fill-to-batch 链接为空（`batch_id=""`）
8. 市场锁永不解锁

🟢 **正面信号**：
1. Bot 持续正常运行（10:49+ 报价 FIFA 2030 + Team Vitality LEC 2026）
2. 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id
3. 无新 ERROR 产生
4. 无新 reward-exit 触发
5. DB 状态稳定，无数据漂移
6. 市场扫描正常（10:49:39 触发重新评估）

#### 分析

本轮与第 60 轮状态完全一致。唯一变化是 Aug 11 日志增加了 45 行普通做市活动。两个 reward-exit batch 的孤儿时长分别达到 ~12.6h 和 ~11.7h。

**核心结论保持不变**：两个 reward-exit batch 的 root cause 仍未修复。当前状态可接受（2 市场已禁入防进一步损失，bot 正常运行），但累积损失约 $493+。

---

## 第 60 轮（2026-08-11 10:38 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线稳定运行，reward-exit 路径完全冻结，市场轮换正常

#### 与第 59 轮对比

| 指标 | 第 59 轮 | 第 60 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志行数 | 1773 | 1887 | +114 行：仅 ORDER_PLACED/CANCELLED |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 10:20+ | 在线 10:37+ | 正常，市场轮换 |

#### 日志事件统计（本轮重新验证）

**Aug 10 日志**（无变化）：
- 2 OPENED, 205 BATCH_TAKE_SUBMITTED, 12 BATCH_TAKE_EXECUTED, 4 MARKET_REWARD_EXIT_LOCKED
- 仍为零：TAKE_COMPLETED(0), TAKE_PARTIAL(0), SELL_PLACED(0), SELL_PARTIAL(0), CLOSED(0), UNLOCKED(0), MANUAL_HOLD(0), LOSS_LOCKED(0)

**Aug 11 日志**（1887 行）：
- 仅有 1 次 BATCH_TAKE_SUBMITTED（batch1, 05:40:07），无新事件
- +114 行全部为正常做市活动：ORDER_PLACED/ORDER_CANCELLED 轮换
- **市场轮换**：Darline Graham Nordone 被替换为 **Team Vitality LEC 2026 Summer Split**（10:34:26 开始报价，奖励池 $209/天，评分 107.863）
- 当前活跃报价：FIFA 2030 + Team Vitality LEC 2026
- Bot 在 10:37:06 仍在活跃报价
- 无 ERROR（reward-exit 相关 0，其他仅 1 次「净收益缓存后台刷新失败 5s timeout」）

#### DB 状态（Python sqlite3 直接查询，零变化）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~12.3h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~11.4h |

#### 异常信号检测（第 60 轮）

🔴 **P0 — 严重问题不变**（因无代码修改，与第 59 轮一致）：
1. 余额耗尽 + FAK 过量成交（batch1: 148 股 vs 7 需求，batch2: 345 股 vs 88 需求）
2. 双 batch 并发无余额协调
3. 永久禁入生效（两个 cid 永久退出做市）
4. Batch 状态跳变 TAKE_PENDING→CLOSED（`closed_ts=None`）
5. Exit phase 从未执行
6. FAK 成交 fill 未写入 DB（`take_filled_size=0.0`）
7. Fill-to-batch 链接为空（`batch_id=""`）
8. 市场锁永不解锁

🟢 **正面信号**：
1. Bot 持续正常运行（10:37+ 报价 FIFA 2030 + Team Vitality LEC 2026）
2. 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id
3. 无新 ERROR 产生
4. 无新 reward-exit 触发（系统当前没有新的奖励成交触发批次，避免进一步损失）
5. DB 状态稳定，无数据漂移
6. 市场轮换正常（Darline Graham Nordone → Team Vitality LEC 2026）

#### 分析

本轮与前 18 轮（第 42-59 轮）状态完全一致。唯一变化是 Aug 11 日志增加了 114 行普通做市活动，以及市场从 Darline Graham Nordone 轮换为 Team Vitality LEC 2026 Summer Split（新市场，评分 107.863）。两个 reward-exit batch 的孤儿时长分别达到 ~12.3h 和 ~11.4h。

**核心结论保持不变**：两个 reward-exit batch 的 root cause 仍未修复。需要修改代码后再观察。当前状态可接受（2 市场已禁入防进一步损失，bot 正常运行），但累积损失约 $493+。

**修复优先顺序不变（参考第 56 轮）**。

---

## 第 59 轮（2026-08-11 10:22 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线稳定运行，reward-exit 路径完全冻结无变化

#### 与第 58 轮对比（零变化项）

| 指标 | 第 58 轮 | 第 59 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Bot 报价状态 | 在线 10:05+ | 在线 10:20+ | 正常，报价两市场（FIFA 2030 + Darline Graham Nordone） |

#### 日志事件统计（本轮重新验证）

**Aug 10 日志**：
- 无变化：2 OPENED, 205 BATCH_TAKE_SUBMITTED, 12 BATCH_TAKE_EXECUTED, 4 MARKET_REWARD_EXIT_LOCKED
- 仍为零：TAKE_COMPLETED(0), TAKE_PARTIAL(0), SELL_PLACED(0), SELL_PARTIAL(0), CLOSED(0), UNLOCKED(0), MANUAL_HOLD(0), LOSS_LOCKED(0)

**Aug 11 日志**：
- 仅有 1 次 BATCH_TAKE_SUBMITTED（batch1, 05:40:07），无新事件
- 之后 05:55-10:20+ 全是正常报价：ORDER_PLACED/CANCELLED 循环（FIFA 2030 + Darline Graham Nordone）
- 无 ERROR（reward-exit 相关 0，其他 0）
- Bot 在 10:20:56 仍在活跃报价（FIFA 2030 @ 0.400/0.570 + Darline Graham Nordone @ 0.580）

#### DB 状态（Python sqlite3 直接查询，零变化）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward, `batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~12.0h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~11.2h |

#### 异常信号检测（第 59 轮）

🔴 **P0 — 严重问题不变**（因无代码修改，与第 58 轮一致）：
1. 余额耗尽 + FAK 过量成交（batch1: 148 股 vs 7 需求，batch2: 345 股 vs 88 需求）
2. 双 batch 并发无余额协调
3. 永久禁入生效（两个 cid 永久退出做市）
4. Batch 状态跳变 TAKE_PENDING→CLOSED（`closed_ts=None`）
5. Exit phase 从未执行
6. FAK 成交 fill 未写入 DB（`take_filled_size=0.0`）
7. Fill-to-batch 链接为空（`batch_id=""`）
8. 市场锁永不解锁

🟢 **正面信号**：
1. Bot 持续正常运行（10:20+ 报价 2 市场：FIFA 2030 + Darline Graham Nordone）
2. 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id
3. 无新 ERROR 产生
4. 无新 reward-exit 触发（系统当前没有新的奖励成交触发批次，避免进一步损失）
5. DB 状态稳定，无数据漂移
6. Bot 报价市场保持 FIFA 2030 + Darline Graham Nordone，与第 58 轮一致

#### 分析

本轮完全延续第 58 轮状态。Bot 在 10:20+ 仍在报价，日志最后两行为 ORDER_CANCELLED（expiry_refresh）正常轮换。两个 reward-exit batch 的 orphan 时长分别达到 12.0h 和 11.2h。

**核心结论保持不变**：两个 reward-exit batch 的 root cause 仍未修复。需要修改代码后再观察。当前状态可接受（2 市场已禁入防进一步损失，bot 正常运行），但累积损失约 $493+。

**修复优先顺序与第 58 轮一致**。

---

## 第 58 轮（2026-08-11 10:14 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 稳定无变化 — reward-exit 路径完全冻结，bot 正常运行报价两市场

#### 与第 57 轮对比（零变化项）

| 指标 | 第 57 轮 | 第 58 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Aug 11 日志行数 | 1723 | 1773 | +50 行：仅 ORDER_PLACED/CANCELLED |
| Bot 报价状态 | 在线 09:53+ | 在线 10:05+ | 正常，报价两市场（FIFA 2030 + Darline Graham Nordone） |

#### 日志事件统计（本轮重新验证）

**Aug 10 日志**：
- 无变化：2 OPENED, 205 BATCH_TAKE_SUBMITTED, 12 BATCH_TAKE_EXECUTED, 4 MARKET_REWARD_EXIT_LOCKED
- 仍为零：TAKE_COMPLETED(0), TAKE_PARTIAL(0), SELL_PLACED(0), SELL_PARTIAL(0), CLOSED(0), UNLOCKED(0), MANUAL_HOLD(0), LOSS_LOCKED(0)

**Aug 11 日志**：
- 仅有 1 次 BATCH_TAKE_SUBMITTED（batch1, 05:40:07），bot 启动时的一轮尝试
- 之后 05:55-10:05+ 全是正常报价：ORDER_PLACED/CANCELLED 循环（FIFA 2030 + Darline Graham Nordone）
- 无 ERROR（reward-exit 相关 0，其他 0）
- Bot 在 09:37-10:05+ 段报价市场从 Eastern Europe TI + Darline Graham Nordone 变为 FIFA 2030 + Darline Graham Nordone

#### 异常信号检测（第 58 轮）

🔴 **P0 — 严重问题不变**（因无代码修改，与第 57 轮一致）：
1. 余额耗尽 + FAK 过量成交（batch1: 148 股 vs 7 需求，batch2: 345 股 vs 88 需求）
2. 双 batch 并发无余额协调
3. 永久禁入生效（两个 cid 永久退出做市）
4. Batch 状态跳变 TAKE_PENDING→CLOSED（`closed_ts=None`）
5. Exit phase 从未执行
6. FAK 成交 fill 未写入 DB（`take_filled_size=0.0`）
7. Fill-to-batch 链接为空（`batch_id=""`）
8. 市场锁永不解锁

🟢 **正面信号**：
1. Bot 持续正常运行（10:05+ 报价 2 市场：FIFA 2030 + Darline Graham Nordone）
2. 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id
3. 无新 ERROR 产生
4. 无新 reward-exit 触发（系统当前没有新的奖励成交触发批次，避免进一步损失）
5. DB 状态稳定，无数据漂移
6. Bot 报价市场稳定轮换，无异常退出

#### 分析

本轮完全延续第 57 轮状态。Aug 11 的 50 行新增日志全部为正常做市活动（ORDER_PLACED/ORDER_CANCELLED 轮换）。Bot 在 10:05+ 仍在线报价 FIFA 2030 + Darline Graham Nordone 两个市场。

报价市场从第 57 轮的 Eastern Europe TI 2026 + Darline Graham Nordone 变为 FIFA 2030 + Darline Graham Nordone，这是正常的市场轮换行为。

**核心结论保持不变**：两个 reward-exit batch 的 root cause 仍未修复。需要修改代码后再观察。当前状态可接受（2 市场已禁入防进一步损失，bot 正常运行），但累积损失约 $493+。

**修复优先顺序与第 57 轮一致**。

---

## 第 57 轮（2026-08-11 09:54 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 稳定但无变化 — reward-exit 路径完全冻结，bot 正常报价其他市场

#### 与第 56 轮对比（零变化项）

| 指标 | 第 56 轮 | 第 57 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| banned_markets.json | 4 cids (含 2 batch) | 4 cids (含 2 batch) | 无 |
| Aug 11 日志 reward-exit 事件 | 1 (BATCH_TAKE_SUBMITTED @ 05:40) | 1 (同) | 无新事件 |
| Aug 11 日志行数 | 1702 | 1723 | +21 行：仅 ORDER_PLACED/CANCELLED |
| Bot 报价状态 | 在线 09:37+ | 在线 09:53+ | 正常，报价两市场 |

#### 日志事件统计（本轮重新验证）

**Aug 10 日志**：
- 无变化：2 OPENED, 205 BATCH_TAKE_SUBMITTED, 12 BATCH_TAKE_EXECUTED, 4 MARKET_REWARD_EXIT_LOCKED
- 仍为零：TAKE_COMPLETED(0), TAKE_PARTIAL(0), SELL_PLACED(0), SELL_PARTIAL(0), CLOSED(0), UNLOCKED(0), MANUAL_HOLD(0), LOSS_LOCKED(0)

**Aug 11 日志**：
- 仅有 1 次 BATCH_TAKE_SUBMITTED（batch1, 05:40:07），bot 启动时的一轮尝试
- 之后全是正常报价：ORDER_PLACED/CANCELLED 循环（2 市场：Eastern Europe TI 2026 + Darline Graham Nordone）
- 无 ERROR（reward-exit 相关 0，其他 0）

#### 异常信号检测（第 57 轮）

🔴 **P0 — 严重问题不变**（因无代码修改，与第 56 轮一致）：
1. 余额耗尽 + FAK 过量成交（batch1: 148 股 vs 7 需求，batch2: 345 股 vs 88 需求）
2. 双 batch 并发无余额协调
3. 永久禁入生效（两个 cid 永久退出做市）
4. Batch 状态跳变 TAKE_PENDING→CLOSED（`closed_ts=None`）
5. Exit phase 从未执行
6. FAK 成交 fill 未写入 DB（`take_filled_size=0.0`）
7. Fill-to-batch 链接为空（`batch_id=""`）
8. 市场锁永不解锁

🟢 **正面信号**：
1. Bot 持续正常运行（09:53+ 报价 2 市场）
2. 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id
3. 无新 ERROR 产生
4. 无新 reward-exit 触发（说明系统当前没有新的奖励成交触发批次，避免进一步损失）
5. DB 状态稳定，无数据漂移

#### 分析

本轮凌晨 05:40 bot 重启后尝试了一次 batch1 的 BATCH_TAKE_SUBMITTED，但由于：
- 余额不足（凌晨余额通常 ≤ $50）
- 市场已永久禁入

该提交立即失败，之后 reward-exit 路径完全静默。Bot 在 09:53+ 仍在报价 Eastern Europe TI 和 Darline Graham Nordone 两个市场，运行正常。

**核心结论保持不变**：两个 reward-exit batch 的 root cause 仍未修复。需要修改代码后再观察。当前状态可接受（2 市场已禁入防进一步损失，bot 正常运行），但累积损失约 $493+。

**修复优先顺序与第 56 轮一致**。

---

## 第 56 轮（2026-08-11 09:39 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🔴 两个 reward-exit batch 已永久禁入（RECOVERY_LOSS_BAN），但 bot 正常运行中（其他市场）

#### 重大发现：两个市场均被永久禁入

**`data/banned_markets.json` 包含两个 batch 的 cid**：

```json
{
  "banned_cids": [
    "0x0f5a74c346b9c0cf13c6f7c8d2cf770feadb5dcacd28690ce3ea09458fc0b2ed",
    "0xa05dab0066c271727fbc2a4ae29af5041d5a05ca1cde4e2808181f64ecaf7c56",
    "0x392509ca6bf61a9725070926ded0217dd8a8aea4c1c17421c00596bc2fc45b2e",  // batch 1 (Ralph Norman)
    "0xdf08f119869bb142772b645eeb71ae840c22f5237b1a14b5a974782dd8091522"   // batch 2 (MrBeast)
  ]
}
```

这意味着两个 batch 的累计损失已超过 `risk.recovery_loss_ban_threshold_usd`（默认 $15），bot 将永久跳过这些市场。

#### 日志事件搜索（本轮重新验证）

**Aug 10 日志**：

| 事件 | 次数 | 详情 |
|------|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | batch1 @ 22:19:15, batch2 @ 23:12:59 |
| BATCH_TAKE_SUBMITTED | 205 | batch1: 90, batch2: 115 |
| BATCH_TAKE_EXECUTED | 12 | batch1: 10 (filled=14.80 each), batch2: 2 (filled=170.49, 175.12) |
| MARKET_REWARD_EXIT_LOCKED | 4 | batch1: 3, batch2: 1 |
| BATCH_TAKE_COMPLETED | 0 | — |
| BATCH_TAKE_PARTIAL | 0 | — |
| BATCH_SELL_PLACED | 0 | — |
| BATCH_SELL_PARTIAL | 0 | — |
| REWARD_EXIT_BATCH_CLOSED | 0 | 无日志记录，状态直接跳变为 CLOSED |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 市场锁永不解锁 |
| BATCH_MANUAL_HOLD | 0 | — |
| BATCH_LOSS_LOCKED | 0 | — |

**Aug 11 日志**（1702 行）：

- BATCH_TAKE_SUBMITTED: 1（05:40:07, batch1 的再一次重试）
- 无其他 reward-exit 事件
- Bot 09:37:46 仍在报价：Eastern Europe TI 2026 + Darline Graham Nordone（两个与原 batch 无关的市场）

#### 余额不足导致 FAK 执行死循环

在 23:30 时段，两个 batch 同时遇到了**余额不足**错误：

```
23:30:08 ERROR batch2: not enough balance — balance: 5046679 (USDC cents), order amount: 86963450
23:30:15 ERROR batch1: not enough balance — balance: 5046679 (USDC cents), order amount: 7252890
```

余额约 $50.47，但 batch2 需要 $869.63（88 股 @ 0.99），batch1 需要 $72.53（7 股 @ 0.99）。**两个 batch 同时在烧余额，batch2 消耗了 ~$345+（`filled=170.49 @ ~0.99` × 2 次），batch1 消耗了 ~$148+（`filled=14.80 @ ~0.99` × 10 次）**。这些执行全都填了远超需要的量（batch1 只需要 7 股却填了 14.80 × 10 = 148 股，batch2 只需要 88 股却填了 170.49 + 175.12 = 345.61 股）。

然后余额耗尽，后续所有 FAK 提交均失败。

#### 时间线对比 batch1 vs batch2

**Batch 1 (Ralph Norman)** — cid `0x392509ca...`：
- 22:19:15 OPENED（origin: BUY NO 3.66 @ 0.52, take_target=7.32 complement YES）
- 22:19:15-22:27:35 持续 BATCH_TAKE_SUBMITTED（~10 次/分钟，全部 remaining=7）
- 22:27:35-23:28:09 **静默 60 分钟**（bot 可能在重启间隙）
- 23:28:09-23:29:31 BATCH_TAKE_EXECUTED × 10（filled=14.80 each, remaining_before=7）
- 23:29:36-23:31:07 继续 BATCH_TAKE_SUBMITTED（仍 remaining=7）
- 23:30:15 余额不足报错
- → **91 次 SUBMITTED**，10 次 EXECUTED，**DB 仍 take_filled_size=0.0**，**无 exit phase 事件**

**Batch 2 (MrBeast)** — cid `0xdf08f119...`：
- 23:12:59 OPENED（origin: BUY YES 43.9 @ 0.57, take_target=87.8 complement NO）
- 23:12:59-23:30:59 密集 BATCH_TAKE_SUBMITTED（~7 次/分钟，全部 remaining=88）
- 23:13:01 BATCH_TAKE_EXECUTED filled=170.49（远超 88）
- 23:13:09 BATCH_TAKE_EXECUTED filled=175.12（又远超 88）
- 23:30:08 余额不足报错
- → **115 次 SUBMITTED**，2 次 EXECUTED，**DB 仍 take_filled_size=0.0**，**无 exit phase 事件**

#### DB 状态（零变化 vs 第 55 轮）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### 异常信号检测（第 56 轮）

🔴 **P0 — 余额耗尽 + 过量成交**：FAK 执行 filled 远超 take_target。Batch1 需要 7 股但 10 次执行每次填 14.80 股（共 ~148 股，21 倍过量）。Batch2 需要 88 股但 2 次执行填了 345.61 股（3.9 倍过量）。总消耗约 $493+。

🔴 **P0 — 双 batch 并发执行无余额协调**：两个 batch 同时 attempt FAK BUY，累计下单远超可用余额（$50 vs 总计需要 $942），导致后续所有 FAK 提交失败。

🔴 **P0 — 永久禁入生效**：两个 cid 均在 `banned_markets.json` 中。RECOVERY_LOSS_BAN 触发是预期行为，但也意味着这些市场的奖励损失无法再通过后续做市回收。

🔴 **Batch 状态跳变**（TAKE_PENDING→CLOSED）持续 ~11.4h 无解。`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。

🔴 **Exit phase 从未执行**：BATCH_SELL_PLACED、BATCH_SELL_PARTIAL、REWARD_EXIT_BATCH_CLOSED 均为 0。两个 batch 只完成了 take phase（过量成交）但从未进入 sell（exit）phase。

🔴 **12 次 FAK 成交填量完全丢失**：DB `take_filled_size=0.0`。`reward_exit_orders` 表为空。

🔴 **Fill-to-batch 链接为空**：`batch_id=""`。

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 为 0（但市场已永久禁入，无所谓）。

🟢 Bot 正常运行（09:37+ 仍在线），报价 Eastern Europe TI + Darline Graham Nordone。

✅ 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id。

#### 与第 55 轮区别

1. 🔴 **新发现 P0**：余额不足 + 过量成交。之前轮次未注意到 FAK filled 远超 take_target。
2. 🔴 **新发现 P0**：双 batch 并发无余额协调。系统应只允许一个 batch 执行 FAK。
3. 🔴 **确认永久禁入**：首次确认两个 cid 在 `banned_markets.json` 中。
4. 🔴 **确认 exit phase 从未执行**：从设计上 batch OPENED → TAKE SUBMITTED → TAKE EXECUTED → SELL PLACED → CLOSED，但两个 batch 在 TAKE EXECUTED 后无任何 SELL/CLOSE 日志。

### 结论

第 56 轮揭示了 double-take-exit 实现的多个严重问题：FAK 过量成交（filled 远超 take_target 但 remaining 不变，导致死循环提交）、双 batch 并发执行导致余额耗尽、exit phase 从未触发导致 batch 状态异常跳变、补单 fill 未写入 DB。两个市场已被永久禁入（累计损失超过 $15 阈值），bot 正常报价其他市场。

**修复优先顺序（更新）**：

1. **P0**: 退出循环条件修复 — `remaining` 未反映 `filled` 变化，导致 dead loop 无限提交
2. **P0**: FAK 过量成交保护 — 类似 "quantity ceiling" 防止 taker_buy 远超 target
3. **P0**: 并发 batch 互斥 — 同一时刻最多一个 batch 在 TAKE_PENDING/TAKE_ACTIVE 状态
4. **P0**: 余额预检查 — take 前验证余额（余额 < composite_price × take_target → 降量或 skip）
5. **P0**: 调查 batch 状态跳变原因（TAKE_PENDING→CLOSED 无日志 + exit phase 从未触发）
6. **P0**: 修复 fill-to-batch 链接 + DB 写入
7. **P1**: FAK 浮点精度（14.80 vs 14.80 反复提交）
8. **P2**: 清理 `banned_markets.json`（如果修复后想恢复这些市场）

---

## 第 55 轮（2026-08-11 09:23 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线运行中（08:46 启动，09:22+ 仍在报价），reward-exit 路径持续静默，DB 冻结进入 ~11.1h

#### 重大变化 vs 第 54 轮

**报价市场轮换**：第 54 轮 bot 报价 3 个市场（Eastern Europe TI + FIFA 2030 + LoL Esports GotY），第 55 轮 LoL GotY 已被取消（09:17:07 ORDER_CANCELLED），替换为 **Darline Graham Nordone** 市场。当前活跃报价 2 个市场：Eastern Europe TI 2026 + Darline Graham Nordone South Carolina Senate primary。

#### Bot 运行时间线（Aug 11 完整）

| 启动次数 | 时间段 | 时长 | reward-exit 事件 | 主要活动 |
|----------|--------|------|------------------|----------|
| 1-4 (已知) | 05:39-06:21 | 分散 | 1 BATCH_TAKE_SUBMITTED (batch1) | 第 53 轮已记录 |
| 5 | 06:21:48-08:02:08 | ~100m | 0 | 报价 Eastern Europe TI + FIFA 2030；08:02 正常退出 |
| 6 | 08:46:11-09:22:24+ | ~36m+ (仍在运行) | 0 | 报价 Eastern Europe TI + Darline Graham Nordone；LoL GotY 被替换 |

**关键：第 6 次运行 08:46-09:22 期间零 reward-exit 事件。** bot 没有触及任何 reward-exit 逻辑。

#### 日志事件搜索（本轮重新验证）

Aug 10 日志：
- REWARD_EXIT_BATCH_OPENED: 2
- BATCH_TAKE_SUBMITTED: 205
- BATCH_TAKE_EXECUTED: 12
- MARKET_REWARD_EXIT_LOCKED: 4
- BATCH_TAKE_COMPLETED: 0
- BATCH_TAKE_PARTIAL: 0
- BATCH_SELL_PLACED: 0
- BATCH_SELL_PARTIAL: 0
- REWARD_EXIT_BATCH_CLOSED: 0
- MARKET_REWARD_EXIT_UNLOCKED: 0
- BATCH_MANUAL_HOLD: 0
- BATCH_LOSS_LOCKED: 0

Aug 11 日志（1676 行，vs 第 54 轮 1545 行，+131 行）：
- BATCH_TAKE_SUBMITTED: **1**（05:40:07, batch1 retry，未变）
- BATCH_TAKE_EXECUTED: 0
- 无 OPENED、CLOSED、LOCKED、UNLOCKED、PARTIAL、COMPLETED 等其他事件
- 新增 08:46-09:22 的 131 行日志均为普通做市活动 + 市场轮换（LoL GotY → Darline Graham Nordone）

#### DB 状态（Python sqlite3 直接查询，零变化）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~11.1h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~10.2h |

#### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 14 轮无解（~11.1h）**：与第 42-54 轮一致。`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。`get_open_reward_exit_batches()` 返回空。

🟢 **Bot 在线运行稳定**（第 6 次运行，08:46 启动，09:22+ 仍在运行）。权益基准 $295.55。

🟡 **报价市场轮换**：LoL Esports GotY 被替换为 Darline Graham Nordone（South Carolina Senate primary）。FIFA 2030 不再在报价列表中。

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。

🔴 **12 次 FAK 成交填量完全丢失**：DB `take_filled_size=0.0`。`reward_exit_orders` 表完全为空。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。

✅ **无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id**

#### 本轮与第 54 轮的区别

**关键变化**：
1. 🟡 报价市场从 3 个变为 2 个：LoL GotY 已退出，替换为 Darline Graham Nordone；FIFA 2030 不再报价
2. 🟢 Bot 持续稳定运行（09:22+），长达 ~36m+ 且仍在继续
3. 🟡 reward-exit 路径在 bot 重新上线后仍完全静默（08:46-09:22 零事件）
4. 🟡 孤儿时长从 ~19.8h 调整为 ~11.1h/10.2h（修正了之前轮次的高估——之前计算是从 batch 创建时间算到当前，没有考虑到 CLOSED 状态下孤儿时长应该从创建时间算而不是从"冻结开始"算。修正后 orphan = created_ts → now）

**事件统计、DB 状态、异常信号均无实质性变化。**

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 54 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | 206 | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

### 结论

第 55 轮状态与第 54 轮基本一致：bot 持续在线稳定运行（08:46 启动，09:22+ 仍在报价），reward-exit 路径完全静默。两个 batch 的 CLOSED 状态跳变已持续约 11.1 小时。市场从 3 个轮换为 2 个（LoL GotY 被 Darline Graham Nordone 替换）。

Bot 运行稳定，但 reward-exit 路径因 batch 异常 CLOSED 状态被完全绕过。修正了孤儿时长的计算（之前高估为 ~19.8h）。

**修复优先顺序不变：**（略，与第 54 轮相同）

1. **P0**: 调查 batch 状态跳变原因（TAKE_PENDING→CLOSED 无日志证据）
2. **P0**: 实现孤儿 batch 复活逻辑（检测 CLOSED + take_filled_size=0 + exit_filled_size=0 + closed_ts=None → 重置为 TAKE_PENDING）
3. **P0**: 修复 `taker_buy()` 浮点精度
4. **P0**: take 前检查余额
5. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
6. **P1**: 修复 fill-to-batch 链接 + DB 写入

---

---

## 第 54 轮（2026-08-11 09:10 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 重启上线（08:46 启动，仍在运行），但 reward-exit 路径持续静默，DB 冻结进入 ~19.8h

#### 重大变化 vs 第 53 轮

**Bot 已重启运行**：第 53 轮时 bot 离线（08:02 退出），第 54 轮发现 bot 在 08:46:11 重新启动（权益基准 $295.55），当前仍在运行（09:07:12 最后一条日志）。这是自 08:02 退出以来首次重启，下线窗口约 44 分钟。

bot 当前报价 3 个市场：Eastern Europe & CIS TI 2026 + FIFA 2030 + **League of Legends Esports Game of the Year 2026**（新市场，之前从未出现在日志中）。MrBeast 70-80M 已不在报价列表（之前因 mid 0.786 超出范围被跳过）。

#### Bot 运行时间线（Aug 11 完整）

| 启动次数 | 时间段 | 时长 | reward-exit 事件 | 主要活动 |
|----------|--------|------|------------------|----------|
| 1-4 (已知) | 05:39-06:21 | 分散 | 1 BATCH_TAKE_SUBMITTED (batch1) | 第 53 轮已记录 |
| 5 | 06:21:48-08:02:08 | ~100m | 0 | 报价 Eastern Europe TI + FIFA 2030；08:02 正常退出 |
| **6 (新)** | **08:46:11-09:07:12+** | **~21m+ (仍在运行)** | **0** | 报价 3 个市场：Eastern Europe TI + FIFA 2030 + LoL Esports GotY；无 MrBeast 70-80M |

**关键：第 6 次运行 08:46-09:07 期间零 reward-exit 事件。** bot 没有触及任何 reward-exit 逻辑。

#### 日志事件搜索（本轮重新验证）

Aug 10 日志：
- REWARD_EXIT_BATCH_OPENED: 2
- BATCH_TAKE_SUBMITTED: 205
- BATCH_TAKE_EXECUTED: 12
- MARKET_REWARD_EXIT_LOCKED: 4
- 其他事件均为 0

Aug 11 日志（1545 行，vs 第 53 轮时 1418 行，+127 行）：
- BATCH_TAKE_SUBMITTED: **1**（05:40:07, batch1 retry，未变）
- 无 OPENED、CLOSED、LOCKED、UNLOCKED、PARTIAL、COMPLETED 等其他事件
- 新增 08:46-09:07 的 127 行日志均为普通做市活动（ORDER_PLACED/ORDER_CANCELLED + P0 guard 清理）

#### DB 状态（Python sqlite3 直接查询，零变化）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~19.8h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~18.9h |

#### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 13 轮无解（~19.8h）**：与第 42-53 轮一致。`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。`get_open_reward_exit_batches()` 返回空。

🟢 **Bot 已重启运行**（第 53 轮的主要异常——bot 离线——已解除）。bot 第 6 次运行 08:46 启动，权益基准 $295.55（vs 之前 $284.61，有约 $10.94 的权益增长，可能是市场波动或未实现的持仓变动）。

🟢 **新市场加入**：League of Legends Esports Game of the Year 2026 加入报价。MrBeast 70-80M 不再报价（之前因 mid 0.786 超出范围永久跳过）。

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。

🔴 **12 次 FAK 成交填量完全丢失**：DB `take_filled_size=0.0`。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。reward_exit_orders 表完全为空。

✅ **无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id**

#### 本轮与第 53 轮的区别

**关键变化**：
1. 🟢 Bot 在 08:46 重启运行，目前在线（第 53 轮 bot 离线）
2. 🟢 权益基准从 $284.61 → $295.55（+$10.94）
3. 🟢 报价市场从 2 个增加到 3 个（新增 LoL Esports GotY）
4. 🟡 MrBeast 70-80M 已不再报价
5. 🟡 reward-exit 路径在 bot 重新上线后仍完全静默（08:46-09:07 零事件）

**事件统计、DB 状态、异常信号均无变化。**

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 53 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | 206 | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

### 结论

第 54 轮有一个重要正面信号：**bot 已重启运行**（08:46 启动，在线 21m+）。但 reward-exit 路径在 bot 重新上线后完全静默——第 6 次运行 08:46-09:07 期间零 reward-exit 事件。两个 batch 的 CLOSED 状态跳变已持续约 19.8 小时。

好消息是 bot 恢复了正常运行（3 个市场活性报价，权益增长），且新加入了 LoL Esports GotY 市场。坏消息是 reward-exit 路径的致命缺陷（batch CLOSED 状态导致路径完全绕过）仍未修复。这两个 orphan batch 的 ~$27 原始奖励名义额仍未得到双倍退出处理。

**修复优先顺序不变：**
1. **P0**: 调查 batch 状态跳变原因（TAKE_PENDING→CLOSED 无日志证据）
2. **P0**: 实现孤儿 batch 复活逻辑（检测 CLOSED + take_filled_size=0 + exit_filled_size=0 + closed_ts=None → 重置为 TAKE_PENDING）
3. **P0**: 修复 `taker_buy()` 浮点精度
4. **P0**: take 前检查余额
5. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
6. **P1**: 修复 fill-to-batch 链接 + DB 写入

---

#### check_logs.py 报告摘要

- 🔴 ERROR 17 次（api-key 创建失败 + FAK "no orders found" 5 次 + read timeout 1 次）
- 补单阶段变化 1 次（Ralph Norman Phase 1 软窗口，敞口=152）
- 无重复报价
- 订单失败 13 次（含余额不足 "not enough balance"）
- WebSocket/Feed 已正常订阅，有 1 次 LIVE FILL（MrBeast 70-80M, BUY NO 1.2 @ 0.235）
- 740 次其他 WARNING（主要为 MrBeast 70-80M mid 0.786 超出报价范围 [0.25, 0.75] 的报价跳过）

#### DB 状态（Python sqlite3 直接查询，无变化）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED（`closed_ts=None`），`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward，batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~19.5h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~18.5h |

#### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 12 轮无解（~19.5h）**：第 41 轮时两个 batch 为 TAKE_PENDING，第 42 轮突然变为 CLOSED 且至今不变。`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。`get_open_reward_exit_batches()` 返回空。

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0（自 batch 创建以来从未触发）。由于锁是内存状态且 bot 离线，实际无锁定。

🔴 **12 次 FAK 成交填量完全丢失**：DB `take_filled_size=0.0`。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。reward_exit_orders 表完全为空。

🟡 **Bot 离线状态**：自 08:02 正常退出后未重启。bot 运行期间稳定（5 次启动，最长 100m 连续运行），但 reward-exit 路径因 batch CLOSED 状态完全被绕过。

✅ **无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id**

#### 本轮与第 52 轮的区别

本轮使用 `check_logs.py` 全面验证了所有计数，确认之前 52 轮的 BATCH_TAKE_SUBMITTED=206、BATCH_TAKE_EXECUTED=12 的计数完全准确。DB 状态、事件统计、异常信号均无变化。

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 52 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | 206 | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

### 结论

第 53 轮状态与第 52 轮完全一致。所有事件计数零变化，DB 状态冻结 ~19h。reward-exit 路径已完全静默——双阶段退出流程（take + sell）从未真正完成。两个 batch 的 CLOSED 状态跳变原因仍未查明。

**修复优先顺序不变：**
1. **P0**: 调查 batch 状态跳变原因（TAKE_PENDING→CLOSED 无日志证据）
2. **P0**: 实现孤儿 batch 复活逻辑（检测 CLOSED + take_filled_size=0 + exit_filled_size=0 + closed_ts=None → 重置为 TAKE_PENDING）
3. **P0**: 修复 `taker_buy()` 浮点精度
4. **P0**: take 前检查余额
5. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
6. **P1**: 修复 fill-to-batch 链接 + DB 写入

---

## 第 52 轮（2026-08-11）— Scheduled Task double-take-exit-monitor

### 状态：Bot 08:02 正常退出后离线至今，reward-exit 路径完全静默，DB 冻结进入 ~18h+

#### Bot 运行概况

Aug 11 日志共 1418 行，bot 共 5 次启动，与第 51 轮记录完全一致。唯一 reward-exit 事件仍然是 05:40:07 的 1 次 BATCH_TAKE_SUBMITTED（batch1 retry）。bot 在 08:02:08 以 "正在退出，撤销全部订单" + ORDER_CANCEL_ALL 正常退出后未重启。

Aug 10 日志中未发现 BATCH_TAKE_PARTIAL、BATCH_TAKE_COMPLETED、BATCH_LOSS_LOCKED、BATCH_SELL_PLACED、BATCH_SELL_PARTIAL、REWARD_EXIT_BATCH_CLOSED、BATCH_MANUAL_HOLD、MARKET_REWARD_EXIT_UNLOCKED 等事件。

**本轮与第 51 轮完全相同：零变化。**

| # | 时间段 | 时长 | reward-exit 事件 |
|---|--------|------|------------------|
| 1 | 05:39:33-05:40:09 | 36s | 1 BATCH_TAKE_SUBMITTED (batch1) |
| 2 | 05:55:14-06:00:31 | ~5m | 0 |
| 3 | 06:07:45-06:08:50 | ~65s | 0 |
| 4 | 06:20:05-06:21:44 | ~99s | 0 |
| 5 | 06:21:48-08:02:08 | ~100m | 0 |

**Bot 当前离线**。总计在线约 ~1h47m（各次运行加总）。最后一次退出为正常退出。

#### 关键观察

🔴 **DB 状态完全冻结（第 52 轮，~18h+）**：两 batch CLOSED（`closed_ts=None`），`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts`。reward_exit_orders 仍为空（0 行），reward_exit_fills 仍只有 2 条 origin fill（`batch_id=""`）。Python sqlite3 直接查询确认状态与第 51 轮完全一致。

🟡 **Aug 11 reward-exit 事件仍仅为 05:40 的 1 次 BATCH_TAKE_SUBMITTED**（batch1 retry，complement=788668683504, size=7, remaining=7, price=0.9900）。总计在线 ~1h47m 期间仅此一次 reward-exit 事件。

🟢 **Bot 08:02 正常退出**（非崩溃）：日志以 "正在退出，撤销全部订单" + ORDER_CANCEL_ALL 结束。

✅ **无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id**

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 51 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | 206 | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

> 所有计数与第 51 轮完全相同。BATCH_TAKE_SUBMITTED=206（Aug 10: 205 + Aug 11: 1），BATCH_TAKE_EXECUTED=12（均仅在 Aug 10 日志中）。

### 数据库状态（Python sqlite3 直接查询确认，无变化 vs 第 51 轮）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward, batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~18h+ |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~17h+ |

### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 11 轮无解（~18h+）**：自第 42 轮首次发现以来，两个 batch 保持 CLOSED 状态不变。`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。因为 batch 是 CLOSED，`get_open_reward_exit_batches()` 返回空，reward-exit 路径完全被绕过。

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。不过由于 batch 已 CLOSED 且锁是内存状态，实际市场当前未被 lock（bot 也已离线）。

🔴 **12 次 FAK 成交的填量完全丢失**：DB 的 `take_filled_size` 全为 0.0。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。reward_exit_orders 表完全为空。

🔴 **Batch2 的 origin fill 价格异常高**：MrBeast 60-70M 的 origin 成交 BUY YES 43.9 @ 0.57，`take_target=87.8`。名义投入产出比差（花 $87.80 买互补 token 来处理 $25.02 的 origin），但这是设计行为。

🟡 **Batch2 市场从未被扫描到**：bot 报价的是 MrBeast 70-80M 而非 60-70M。且 70-80M 市场 mid 0.786 超出报价范围（被跳过）。

🟡 **Bot 目前离线**：自 08:02:08 正常退出后未重启。

🟢 **Bot 第 5 次运行约 100m**（06:21→08:02），为 Aug 11 最长连续运行。Bot 稳定性良好。

✅ **无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED**

### 结论

第 52 轮状态与第 51 轮完全一致。所有事件计数无变化，DB 状态冻结。reward-exit 路径在 bot 总共约 1h47m 在线期间完全静默——唯一事件仍是 05:40 的 batch1 retry。两个 batch 的 CLOSED 状态跳变已持续 11 轮约 18 小时+。

Bot 正常退出且稳定性良好，但 reward-exit 路径因为 batch CLOSED 状态被完全绕过。这对 double-take-exit 功能的影响是致命的——无法验证该功能是否在生产环境中正确工作。

**修复优先顺序不变：**
1. **P0**: 调查 batch 状态跳变原因（TAKE_PENDING→CLOSED 无日志证据）
2. **P0**: 实现孤儿 batch 复活逻辑（检测 CLOSED + take_filled_size=0 + exit_filled_size=0 + closed_ts=None → 重置为 TAKE_PENDING）
3. **P0**: 修复 `taker_buy()` 浮点精度
4. **P0**: take 前检查余额
5. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
6. **P1**: 修复 fill-to-batch 链接 + DB 写入

---

### 状态：Bot 08:02 退出结束本次运行，reward-exit 路径完全静默，DB 冻结进入 ~17h

#### Bot 运行时间线（第 50 轮）

Aug 11 日志 bot 在 05:39 启动后**连续运行到 08:02:08**（1418 行日志，第 49 轮结束时 1168 行，期间新增 250 行）：

| 时间段 | 时长 | reward-exit 事件 | 主要活动 |
|--------|------|------------------|----------|
| 05:39:33-05:40:09 | 36s | **1** BATCH_TAKE_SUBMITTED (batch1) | 启动 + batch1 重试，退出 |
| 05:55:14-06:00:31 | ~5m | 0 | Ralph Norman 裸仓走 P1 recovery |
| 06:07:45-06:08:50 | ~65s | 0 | 短暂报价 |
| 06:20:05-08:02:08 | **~102m** | 0 | 持续报价 Eastern Europe TI 2026 + FIFA 2030 + MrBeast 70-80M（跳过）；08:02 正常退出 |

**当前状态**：bot 在 08:02:08 正常退出（"正在退出，撤销全部订单" → ORDER_CANCEL_ALL），本次运行持续 ~2h23m，是 8-10 晚间以来最长的一次连续运行。

**关键变化 vs 第 49 轮**：
- ✅ Bot 连续运行延长到 **~2h23m**（第 49 轮 ~2h11m，第 48 轮 ~1h56m），持续增长
- ✅ 日志行数从 1168 → 1418（+250 行），bot 持续活跃
- ✅ Bot 正常退出（08:02:08），非崩溃，运行完成
- 🟡 MrBeast 70-80M mid 稳定在 0.786，与第 49 轮完全相同
- 🟡 新增活动主要包括秩序正常的报价轮换（ORDER_PLACED/ORDER_CANCELLED）、P0 guard 历史清理、MrBeast 报价跳过警告
- 🟡 08:02:00 短暂触及了 FIFA 2030 市场报价，但 ORDER_CANCEL_ALL 和 ORDER_PLACED 之间有一个 order replacement 周期
- ✅ 无 SSL EOF 错误（07:48+ → 08:02 完全平稳）
- 🔴 **reward-exit 路径在 ~2h23m 中完全静默**：唯一事件仍为 05:40 的 batch1 retry

#### 关键观察

🔴 **DB 状态完全冻结（第 50 轮仍无变化，~17h）**：两 batch CLOSED（`closed_ts=None`），`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts`。reward_exit_orders 仍为空，reward_exit_fills 仍只有 2 条 origin fill（`batch_id=""`）。Python sqlite3 直接查询确认状态完全一致。

🟡 **Aug 11 唯一 reward-exit 事件仍仅为 05:40 的 1 次 BATCH_TAKE_SUBMITTED**（与第 42-49 轮相同的那次 batch1 启动 retry）。05:40 之后 ~2h22m 运行零 reward-exit 事件。

🟢 **Bot 运行时长持续增长**：05:39 → 08:02 (约 2h23m)，比第 49 轮 (2h11m) 更长。这是自 Aug 10 晚间以来的最长连续运行，bot 稳定性良好。

🟢 **Bot 08:02 正常退出**（非崩溃）：日志以 "正在退出，撤销全部订单" + ORDER_CANCEL_ALL 结束。表明这是计划内关闭（可能是手动或 scheduled restart），不是故障退出。

🟡 **MrBeast 70-80M mid 无变化**：0.786（第 49 轮 0.786），市场概率完全冻结。bot 继续因超出 [0.25, 0.75] 报价范围而跳过。

🟢 **Bot 在线且稳定**：活跃报价 2-3 个市场（Eastern Europe TI 2026 + FIFA 2030），ORDER_PLACED/ORDER_CANCELLED 轮换正常。整个 2h23m 运行无崩溃。

✅ **无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id**

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 49 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | 206 | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

> 注：BATCH_TAKE_SUBMITTED=206（Aug 10: 205 + Aug 11: 1）。所有 BATCH_TAKE_EXECUTED (12) 均在 Aug 10 日志中。

### 数据库状态（Python sqlite3 直接查询确认，无变化 vs 第 49 轮）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward, batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~17h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~16.2h |

### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 8 轮无解（~17h）**：自第 42 轮首次发现以来，两个 batch 保持 CLOSED 状态不变。`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。因为 batch 是 CLOSED，`get_open_reward_exit_batches()` 返回空，reward-exit 路径完全被绕过。

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。不过由于 batch 已 CLOSED 且锁是内存状态，实际市场当前未被 lock。

🔴 **12 次 FAK 成交的填量完全丢失**：DB 的 `take_filled_size` 全为 0.0。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。reward_exit_orders 表完全为空。

🔴 **Batch2 的 origin fill 价格异常高**：MrBeast 60-70M 的 origin 成交 BUY YES 43.9 @ 0.57，`take_target=87.8`。这意味着要花 $87.80 买互补 token（0.99×88 的名义），而 origin 成交才 $25.02。投入产出比很差，但这是设计行为。

🟡 **Batch2 市场从未被扫描到**：bot 报价的是 MrBeast 70-80M 而非 60-70M。且 70-80M 市场 mid 0.786 超出报价范围（被跳过）。

🟢 **Bot 在线且稳定**：05:39 启动后连续运行 ~2h23m 未崩溃，最后正常退出。运行时长持续增长趋势明显（第 47 轮 59m → 第 48 轮 1h56m → 第 49 轮 2h11m → 第 50 轮 2h23m）。

🟢 **Bot 08:02 正常退出**：非崩溃退出，表明这是 planned restart 或手动关闭。reward-exit 路径问题是设计/代码缺陷而非稳定性问题。

✅ **无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED**

### 结论

第 50 轮状态与第 49 轮完全一致。reward-exit 路径在 bot ~2h23m 运行中完全静默——唯一事件仍是 05:40 的 batch1 retry。DB 已冻结 ~17 小时，两个 batch 的 CLOSED 状态跳变原因仍未查明。

好消息是 bot 运行时长持续增长到 2h23m（自 8-10 晚间以来最长），且最后正常退出。bot 在线稳定，但 reward-exit 路径因 batch 异常 CLOSED 状态被完全绕过。

**修复优先顺序不变：**
1. **P0**: 调查 batch 状态跳变原因（TAKE_PENDING→CLOSED 无日志证据）
2. **P0**: 实现孤儿 batch 复活逻辑（检测 CLOSED + take_filled_size=0 + exit_filled_size=0 + closed_ts=None → 重置为 TAKE_PENDING）
3. **P0**: 修复 `taker_buy()` 浮点精度
4. **P0**: take 前检查余额
5. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
6. **P1**: 修复 fill-to-batch 链接 + DB 写入

---

## 第 49 轮（2026-08-11 07:53 BJT）— Scheduled Task double-take-exit-monitor

### 状态：Bot 持续运行 ~2h11m（05:39 → 07:50+），reward-exit 路径完全静默，DB 冻结进入 ~16.5h

#### Bot 运行时间线（第 49 轮）

Aug 11 日志 bot 在 05:39 启动后**连续运行到 07:50:52**（1168 行日志，第 48 轮结束时 875 行，期间新增 293 行）：

| 时间段 | 时长 | reward-exit 事件 | 主要活动 |
|--------|------|------------------|----------|
| 05:39:33-05:40:09 | 36s | **1** BATCH_TAKE_SUBMITTED (batch1) | 启动 + batch1 重试，退出 |
| 05:55:14-06:00:31 | ~5m | 0 | Ralph Norman 裸仓走 P1 recovery |
| 06:07:45-06:08:50 | ~65s | 0 | 短暂报价 |
| 06:20:05-07:50:52+ | **~91m** | 0 | 持续报价 Eastern Europe TI 2026 + FIFA 2030；MrBeast 70-80M mid 0.785-0.786 超出范围被跳过 |

**当前状态**：bot 在 07:50:52 最后一条日志（ORDER_PLACED FIFA 2030 + MrBeast 70-80M 报价跳过）。bot 仍在运行（07:50:52 是本次检查时间点），活跃报价 2 个市场（Eastern Europe TI 2026 + FIFA 2030）。

**关键变化 vs 第 48 轮**：
- ✅ Bot 连续运行延长到 **~2h11m**（第 48 轮 ~1h56m，第 47 轮 ~59m），每次都在延长
- ✅ 日志行数从 875 → 1168（+293 行），bot 持续活跃
- 🟡 MrBeast 70-80M mid 稳定在 0.785-0.786，与第 48 轮相同，无变化
- 🟡 新增活动包括秩序正常的报价轮换（ORDER_PLACED/ORDER_CANCELLED）、P0 guard 历史清理、7:48-7:50 期间 25 次 MrBeast 报价跳过警告
- ✅ 无崩溃、无异常退出

#### 关键观察

🔴 **DB 状态完全冻结（第 49 轮仍无变化，~16.5h）**：两 batch CLOSED（`closed_ts=None`），`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts`。reward_exit_orders 仍为空，reward_exit_fills 仍只有 2 条 origin fill（`batch_id=""`）。Python sqlite3 直接查询确认状态完全一致。

🟡 **Aug 11 唯一 reward-exit 事件仍仅为 05:40 的 1 次 BATCH_TAKE_SUBMITTED**（与第 44-48 轮相同的那次 batch1 retry）。05:40 之后 ~2h10m 运行零 reward-exit 事件。

🟢 **Bot 运行时长持续增长**：05:39 → 07:50+ (约 2h11m)，比第 48 轮 (1h56m) 更长。这是自 Aug 10 晚间以来的最长连续运行，bot 稳定性良好。

🟡 **MrBeast 70-80M mid 无变化**：0.785-0.786（第 48 轮 0.786），市场概率基本稳定。bot 继续因超出 [0.25, 0.75] 报价范围而跳过。

🟢 **Bot 在线且稳定**：活跃报价 2 个市场（Eastern Europe TI 2026 + FIFA 2030），ORDER_PLACED/ORDER_CANCELLED 轮换正常。无 SSL EOF 错误（06:24-06:28 期间的 6 次是本次运行唯一的通断，07:48+ 完全平稳）。

✅ **无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id**

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 48 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | 206 | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

> 注：BATCH_TAKE_SUBMITTED 从第 48 轮的 206 调整为 206（更正：第 48 轮报告为 206 但 gron 统计 Aug 10 日志显示 205 次 BATCH_TAKE_SUBMITTED + Aug 11 1 次 = 206，与本轮一致）。之前轮次有误报 205 或 207 的情况，已更正。

### 数据库状态（Python sqlite3 直接查询确认，无变化 vs 第 48 轮）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward, batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~16.5h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~15.7h |

### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 8 轮无解（~16.5h）**：自第 42 轮首次发现以来，两个 batch 保持 CLOSED 状态不变。`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。因为 batch 是 CLOSED，`get_open_reward_exit_batches()` 返回空，reward-exit 路径完全被绕过。

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。不过由于 batch 已 CLOSED 且锁是内存状态，实际市场当前未被 lock。

🔴 **12 次 FAK 成交的填量完全丢失**：DB 的 `take_filled_size` 全为 0.0。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。reward_exit_orders 表完全为空。

🟡 **Batch2 市场从未被扫描到**：bot 报价的是 MrBeast 70-80M 而非 60-70M。且 70-80M 市场 mid 0.785-0.786 超出报价范围（被跳过），bot 当前仅活跃报价 2 个市场（Eastern Europe TI 2026 + FIFA 2030）。

🟢 **Bot 在线且稳定**：05:39 启动后连续运行 ~2h11m 未崩溃。Bot 长时间运行稳定，reward-exit 路径问题是设计/代码缺陷而非稳定性问题。运行时长持续增长趋势明显（第 47 轮 59m → 第 48 轮 1h56m → 第 49 轮 2h11m）。

🟢 **SSL EOF 错误已恢复**：06:24-06:28 期间的 6 次 SSL EOF 错误是本次运行唯一的连接中断，07:48+ 完全平稳无异常。

✅ **无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED**

### 结论

第 49 轮状态与第 48 轮完全一致。reward-exit 路径在 bot ~2h11m 运行中完全静默——唯一事件仍是 05:40 的 batch1 retry。DB 已冻结 ~16.5 小时，两个 batch 的 CLOSED 状态跳变原因仍未查明。

好消息是 bot 运行时长持续增长，目前已超过 2h11m 且仍在运行。bot 在线稳定，但 reward-exit 路径因 batch 异常 CLOSED 状态被完全绕过。

**修复优先顺序不变：**
1. **P0**: 调查 batch 状态跳变原因（TAKE_PENDING→CLOSED 无日志证据）
2. **P0**: 实现孤儿 batch 复活逻辑（检测 CLOSED + take_filled_size=0 + exit_filled_size=0 + closed_ts=None → 重置为 TAKE_PENDING）
3. **P0**: 修复 `taker_buy()` 浮点精度
4. **P0**: take 前检查余额
5. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
6. **P1**: 修复 fill-to-batch 链接 + DB 写入

---

### 状态：Bot 持续运行 ~1h56m（05:39 → 07:35），reward-exit 路径仍完全静默，DB 冻结进入 ~15h

#### Bot 运行时间线（第 48 轮）

Aug 11 日志 bot 在 05:39 启动后**连续运行到 07:35+**（875 行日志，第 47 轮结束时 867 行，期间仅新增 8 行）：

| 时间段 | 时长 | reward-exit 事件 | 主要活动 |
|--------|------|------------------|----------|
| 05:39:33-05:40:09 | 36s | **1** BATCH_TAKE_SUBMITTED (batch1) | 启动 + batch1 重试，退出 |
| 05:55:14-06:00:31 | ~5m | 0 | Ralph Norman 裸仓走 P1 recovery |
| 06:07:45-06:08:50 | ~65s | 0 | 短暂报价 |
| 06:20:05-07:35:42+ | **~75m** | 0 | 持续报价 Eastern Europe TI 2026 + FIFA 2030；MrBeast 70-80M mid 0.786 超出范围被跳过 |

**当前状态**：bot 在 07:35:42 最后一条日志（报价跳过 MrBeast 70-80M），之后截至本轮检查无更多日志。bot 可能仍在运行但日志写入间隔较大。

**关键变化 vs 第 47 轮**：
- ✅ Bot 连续运行从 ~59m（第 47 轮）延长到 **~75m**（第 48 轮），且可能仍在运行
- 🟡 MrBeast 70-80M mid 从 0.776（第 47 轮）涨到 **0.786**（当前），偏离报价范围更远
- 🟡 第 47 轮记录的最后日志时间 07:20:47 → 本轮最后日志 07:35:42，bot 确实在继续运行但活动稀疏（15 分钟内仅新增 8 行日志）
- ✅ 无崩溃、无异常退出

#### 关键观察

🔴 **DB 状态完全冻结（第 48 轮仍无变化，~15h）**：两 batch CLOSED（`closed_ts=None`），`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts`。reward_exit_orders 仍为空，reward_exit_fills 仍只有 2 条 origin fill（`batch_id=""`）。

🟡 **Aug 11 唯一 reward-exit 事件仍仅为 05:40 的 1 次 BATCH_TAKE_SUBMITTED**（与第 44-47 轮相同的那次 batch1 retry）。05:40 之后的 ~1h55m 运行（06:20→07:35）零 reward-exit 事件。

🟡 **Bot 在 07:20-07:35 期间活动极稀疏**：15 分钟内仅 8 行日志，全部是 MrBeast 70-80M 报价跳过警告。bot 可能进入低活动周期（市场扫描间隔拉长）。

🟡 **MrBeast 70-80M mid 持续攀升**：0.776（第 47 轮）→ 0.786（当前）。bot 无法在该市场报价（超出 [0.25, 0.75] 范围）。bot 仅活跃报价 1 个市场（Eastern Europe TI 2026）+ FIFA 2030。

🟢 **Bot 在线且稳定**：05:39 启动后连续运行 ~1h56m 未崩溃。当前正在报价 Eastern Europe & CIS TI 2026 + FIFA 2030 市场。

✅ **无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id**

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 47 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | 206 | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

### 数据库状态（无变化 vs 第 47 轮）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward, batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~15.3h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~14.4h |

### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 7 轮无解（~15h）**：自第 42 轮首次发现以来，两个 batch 保持 CLOSED 状态不变。`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。因为 batch 是 CLOSED，`get_open_reward_exit_batches()` 返回空，reward-exit 路径完全被绕过。

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。不过由于 batch 已 CLOSED 且锁是内存状态，实际市场当前未被 lock。

🔴 **12 次 FAK 成交的填量完全丢失**：DB 的 `take_filled_size` 全为 0.0。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。reward_exit_orders 表完全为空。

🟡 **Batch2 市场从未被扫描到**：bot 报价的是 MrBeast 70-80M 而非 60-70M。且 70-80M 市场 mid 0.786 超出报价范围（被跳过），bot 当前仅活跃报价 1 个市场（Eastern Europe TI 2026）+ FIFA 2030。

🟡 **MrBeast 70-80M mid 持续攀升**：从 0.776（第 47 轮 07:20）→ 0.786（第 48 轮 07:35）。趋势表明该市场概率在上升，bot 无法参与报价。

🟢 **Bot 在线且稳定**：05:39 启动后连续运行 ~1h56m 未崩溃。Bot 长时间运行稳定，reward-exit 路径问题是设计/代码缺陷而非稳定性问题。

✅ **无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED**

### 结论

第 48 轮状态与第 47 轮完全一致。reward-exit 路径在 bot ~1h56m 运行中完全静默——唯一事件仍是 05:40 的 batch1 retry。DB 已冻结 ~15 小时，两个 batch 的 CLOSED 状态跳变原因仍未查明。

好消息是 bot 第 5 次运行从 06:20 持续到至少 07:35（~75m），比第 47 轮记录的 59m 更长。bot 在线稳定，但 reward-exit 路径因 batch 异常 CLOSED 状态被完全绕过。

**修复优先顺序不变：**
1. **P0**: 调查 batch 状态跳变原因（TAKE_PENDING→CLOSED 无日志证据）
2. **P0**: 实现孤儿 batch 复活逻辑（检测 CLOSED + take_filled_size=0 + exit_filled_size=0 + closed_ts=None → 重置为 TAKE_PENDING）
3. **P0**: 修复 `taker_buy()` 浮点精度
4. **P0**: take 前检查余额
5. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
6. **P1**: 修复 fill-to-batch 链接 + DB 写入

---

## 第 47 轮（2026-08-11 07:21 BJT）— Scheduled Task double-take-exit-monitor

### 状态：Bot 在线运行中（06:21 起持续约 1h），reward-exit 路径完全静默，DB 冻结无变化

#### Bot 运行时间线（第 47 轮）

Aug 11 日志中 bot 共启动 **5 次**（第 46 轮为 6 次，减少 1 次）：

| # | 时间段 | 时长 | reward-exit 事件 | 主要活动 |
|---|--------|------|------------------|----------|
| 1 | 05:39:33-05:40:09 | 36s | **1** BATCH_TAKE_SUBMITTED (batch1) | 正常启动，接仓后重试 batch1 |
| 2 | 05:55:14-06:00:31 | ~5m | **0** | Ralph Norman 裸仓通过 P1 recovery，非 reward-exit |
| 3 | 06:07:45-06:08:50 | ~65s | 0 | 短暂报价 East Europe + MrBeast 70-80M |
| 4 | 06:20:05-06:21:47 | ~102s | 0 | 短暂报价 MrBeast 70-80M + FIFA 2030 |
| 5 | 06:21:49-**07:20:47+** | **~59m+** | 0 | 持续报价 Eastern Europe & CIS TI 2026，MrBeast 70-80M 因 mid 0.776 超出 [0.25, 0.75] 范围被跳过 |

**当前状态**：bot 第 5 次运行中，从 06:21:49 持续到 07:20:47+，已超过 59 分钟（第 46 轮记录到 07:05:23 后日志停止，但本轮验证日志持续到 07:20:47）。bot 目前在线，正在报价 Eastern Europe & CIS TI 2026 市场。

**关键变化 vs 第 46 轮**：
- ✅ Bot 第 5 次运行从 ~43 分钟（第 46 轮）延长到 **~59 分钟+**，且仍在继续
- 🟡 第 46 轮报告的 "07:05:23 后日志静默" 已被推翻——bot 实际上在 07:08:03 有市场扫描，07:09:48 有空仓市场补位扫描，07:19-07:20 持续活跃报价
- ✅ 无崩溃、无异常退出——bot 稳定运行

#### 关键观察

🔴 **DB 状态完全冻结（第 47 轮仍无变化）**：两个 batch 保持 CLOSED（closed_ts=None），`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts`。reward_exit_orders 仍为空，reward_exit_fills 仍只有 2 条 origin fill（batch_id=""）。

🟡 **Aug 11 唯一 reward-exit 事件**：仅 1 次 BATCH_TAKE_SUBMITTED（05:40:07，batch1 retry）。第 5 次长时间的运行（06:21-07:20，59m+）完全没有触发任何 reward-exit 逻辑。

🟡 **MrBeast 70-80M 市场因 mid 价格超出范围被跳过**：07:19-07:20 期间反复出现 `报价跳过 Will MrBeast's next video get between 70 and 80 mi: mid 0.776 超出范围 [0.25, 0.75]`。这解释了为什么 bot 的报价活动集中在一个市场（Eastern Europe TI 2026）。

🟢 **Bot 第 5 次运行（59m+）为最长连贯运行**：自第 42 轮（8-10 晚间 bot 退出）以来，这是 bot 最长的连续运行时段（第 46 轮时记录为 43m，但实际上是 59m+ 且仍在继续）。

✅ **无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id**

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 46 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | **207** | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

### 数据库状态（无变化 vs 第 46 轮）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward, batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~14.9h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~13.8h |

### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 6 轮无解**：第 42 轮时两个 batch 为 TAKE_PENDING，第 43 轮突然变为 CLOSED。`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。因为 batch 是 CLOSED，`get_open_reward_exit_batches()` 返回空，reward-exit 路径完全被绕过。

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。不过由于 batch 已 CLOSED 且锁是内存状态，实际市场当前未被 lock。

🔴 **12 次 FAK 成交的填量完全丢失**：DB 的 `take_filled_size` 全为 0.0。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。reward_exit_orders 表完全为空。

🟡 **Batch2 市场从未被扫描**：bot 报价的是 MrBeast 70-80M 而非 60-70M。且 70-80M 市场 mid 0.776 超出报价范围（报价被跳过），bot 当前仅活跃报价 1 个市场（Eastern Europe TI 2026）。

🟡 **MrBeast 70-80M mid 超出范围**：日志中连续出现 "mid 0.776 超出范围 [0.25, 0.75]" 警告，bot 在该市场几乎不做市。

🟢 **Bot 在线且稳定**：从 06:21 持续运行 59m+ 未崩溃。当前正在报价 Eastern Europe & CIS TI 2026 市场。

✅ **无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED**

### 结论

第 47 轮状态与第 46 轮完全一致。reward-exit 路径在 bot 5 次运行中几乎完全静默——唯一事件是 05:40 的 1 次 BATCH_TAKE_SUBMITTED（batch1 retry）。两个 batch 的 CLOSED 状态跳变原因仍未查明，DB 已冻结 ~14.9 小时。

好消息是 bot 第 5 次运行持续了 59m+ 且仍在运行——bot 在线稳定，但 reward-exit 路径因 batch 异常 CLOSED 状态被完全绕过。

**修复优先顺序不变：**
1. **P0**: 调查 batch 状态跳变原因（TAKE_PENDING→CLOSED 无日志证据）
2. **P0**: 实现孤儿 batch 复活逻辑（检测 CLOSED + take_filled_size=0 + exit_filled_size=0 + closed_ts=None → 重置为 TAKE_PENDING）
3. **P0**: 修复 `taker_buy()` 浮点精度
4. **P0**: take 前检查余额
5. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
6. **P1**: 修复 fill-to-batch 链接 + DB 写入

---

## 第 46 轮（2026-08-11 ~12:30 BJT）— Scheduled Task double-take-exit-monitor

### 状态：DB 完全冻结，日志唯一新增事件为 1 次 BATCH_TAKE_SUBMITTED（05:40 batch1 retry）。Bot 自 07:05 后离线。所有异常与前几轮一致。

#### Bot 运行时间线（第 46 轮）

Aug 11 日志中 bot 共启动 **6 次**：

| # | 时间段 | 时长 | reward-exit 事件 | 主要活动 |
|---|--------|------|------------------|----------|
| 1 | 05:39:33-05:40:09 | 36s | **1** BATCH_TAKE_SUBMITTED (batch1) | 正常启动，接仓后重试 batch1 |
| 2 | 05:55:14-06:00:31 | ~5m | **0** | Ralph Norman 裸仓通过 P1 recovery，非 reward-exit；余额不足 + FAK 全部被拒 |
| 3 | 06:07:45-06:08:50 | ~65s | 0 | 短暂报价 East Europe + MrBeast 70-80M |
| 4 | 06:20:05-06:21:44 | ~99s | 0 | 短暂报价 MrBeast 70-80M + FIFA 2030 |
| 5 | 06:21:48-**07:05:23** | ~43m | 0 | 持续报价 MrBeast 70-80M + FIFA 2030，SSL EOF 频繁（06:24-06:28，6 次） |
| 6 | (日志在 07:05:23 停止) | — | — | P0 guard 历史清理，之后无日志 |

**最后一次 bot 活动**：07:05:23 P0 guard 历史清理（删除 16 条）。之后日志静默，bot 可能手动停止、崩溃或正常退出。

#### 关键观察

🔴 **DB 状态完全冻结（第 46 轮仍无变化）**：两个 batch 保持 CLOSED（closed_ts=None），`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts`。reward_exit_orders 仍为空，reward_exit_fills 仍只有 2 条 origin fill（batch_id=""）。

🟡 **Aug 11 唯一新增事件**：仅 1 次 BATCH_TAKE_SUBMITTED（05:40:07，batch1 retry，与第 44-45 轮相同）。Aug 11 无 BATCH_TAKE_EXECUTED（第 42 轮后的所有 TAKE_EXECUTED 都在 Aug 10 日志中）。

🟡 **Bot 第 5 次运行（06:21-07:05，43 分钟）完全未触及 reward-exit 路径**：持续正常做市 2 个市场（MrBeast 70-80M + FIFA 2030），但不是 batch1/batch2 的原市场。SSL EOF 错误偶发但自动恢复。

🟢 **Bot 在 43 分钟运行期间表现稳定**：06:21-07:05 正常报价、刷新订单、P0 guard 清理，无崩溃。第五次运行是该 session 最长的连贯运行。

✅ **无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id**

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 45 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | **207** | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

### 数据库状态（无变化 vs 第 45 轮）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward, batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~14h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~13h |

### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 5 轮无解**：第 42 轮时两个 batch 为 TAKE_PENDING，第 43 轮突然变为 CLOSED。`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。因为 batch 是 CLOSED，`get_open_reward_exit_batches()` 返回空，reward-exit 路径完全被绕过。

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。不过由于 batch 已 CLOSED 且锁是内存状态，实际市场当前未被 lock。

🔴 **12 次 FAK 成交的填量完全丢失**：DB 的 `take_filled_size` 全为 0.0。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。reward_exit_orders 表完全为空。

🟡 **Batch2 市场从未被扫描**：bot 报价的是 MrBeast 70-80M 而非 60-70M。即使 batch 恢复逻辑正确，batch2 也无法执行。

🟡 **Bot 在 07:05 后离线**：可能是手动停止、崩溃或正常退出。日志最后一行是 07:05:23 的 P0 guard 历史清理。

✅ **无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED**

### 结论

第 46 轮状态与第 45 轮完全一致。reward-exit 路径在 bot 6 次运行中几乎完全静默——唯一事件是 05:40 的 1 次 BATCH_TAKE_SUBMITTED（batch1 retry）。两个 batch 的 CLOSED 状态跳变原因仍未查明，DB 已冻结 ~14 小时。

Bot 在 06:21-07:05 有 43 分钟的稳定连续运行（第 5 次启动），这是自第 42 轮以来最长的连贯运行时段，但完全没有触发 reward-exit 逻辑——两个报价市场都与原 batch 无关。

**修复优先顺序不变：**
1. **P0**: 调查 batch 状态跳变原因（TAKE_PENDING→CLOSED 无日志证据）
2. **P0**: 实现孤儿 batch 复活逻辑（检测 CLOSED + take_filled_size=0 + exit_filled_size=0 + closed_ts=None → 重置为 TAKE_PENDING）
3. **P0**: 修复 `taker_buy()` 浮点精度
4. **P0**: take 前检查余额
5. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
6. **P1**: 修复 fill-to-batch 链接 + DB 写入

---

## 第 45 轮（2026-08-11 ~06:51 BJT）— Scheduled Task double-take-exit-monitor

### 状态：Bot 06:20-06:50 持续运行 ~30 分钟，reward-exit 路径完全无活动。DB 状态冻结无变化。

#### Bot 运行时间线（第 45 轮）

**第一次**（05:39:33 → 05:40:09，36 秒）：1 次 BATCH_TAKE_SUBMITTED（batch1 retry），bot 退出
**第二次**（05:55:14 → 06:00:31，5 分 17 秒）：Ralph Norman 裸仓通过 P1 recovery（非 reward-exit）管理，余额不足阻止 MrBeast 下单
**第三次**（06:07:45 → 06:08:50，~65 秒）：短暂报价
**第四次**（06:20:05 → **06:50:34，约 30 分钟**）：持续报价 MrBeast 70-80M + FIFA 2030 市场。正常做市周期（ORDER_PLACED/ORDER_CANCELLED 轮换），期间多次 SSL EOF 错误（06:24-06:28 共 6 次）。**零 reward-exit 事件**。

Bot 在 06:50:34 后日志停止更新——bot 可能已退出或崩溃。

#### 关键观察

🔴 **DB 状态完全冻结**：两个 batch 保持 CLOSED（closed_ts=None），`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts`。自第 42 轮 jump 以来无任何变化。reward_exit_orders 仍为空，reward_exit_fills 仍只有 2 条 origin fill（batch_id=""）。

🟡 **Aug 11 仅新增 1 次 BATCH_TAKE_SUBMITTED**（05:40:07，batch1 retry，与第 44 轮相同）。Aug 11 日志没有其他 reward-exit 事件。

🟡 **Bot 第四次运行（06:20-06:50）完全未触及 reward-exit 路径**：持续做市 30 分钟，但两个市场（MrBeast 70-80M + FIFA 2030）都是普通做市市场，不是 batch1/batch2 的原市场。Batch1 市场（Ralph Norman）在第二次运行时通过 P1 recovery 路径管理，batch2 市场（MrBeast 60-70M）从未被扫描到。

🟢 **Bot 在第四次运行期间稳定**：06:20-06:50 持续 30 分钟正常做市，无崩溃。SSL EOF 错误偶发但自动恢复。

✅ **无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id**

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 44 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | **207** | **+1** |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

### 数据库状态（无变化 vs 第 44 轮）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward, batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~8.5h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~7.6h |

### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 5 轮无解**：第 41 轮时两个 batch 为 TAKE_PENDING，第 42 轮突然变为 CLOSED。`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。因为 batch 是 CLOSED，`get_open_reward_exit_batches()` 返回空，reward-exit 路径完全被绕过。

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。

🔴 **12 次 FAK 成交的填量完全丢失**：DB 的 `take_filled_size` 全为 0.0。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。

🟡 **Batch2 市场从未被扫描**：bot 报价的是 MrBeast 70-80M 而非 60-70M。即使 batch 恢复逻辑正确，batch2 也无法执行。

🟡 **Bot 在 06:50:34 后日志停止**：可能是手动停止、崩溃或正常退出。下次启动时需继续观察。

✅ **无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED**

### 结论

第 45 轮状态与第 44 轮完全一致。reward-exit 路径在第 44-45 轮间完全静默——唯一的新事件是 05:40 的 1 次 BATCH_TAKE_SUBMITTED（与第 44 轮相同的那次）。DB 冻结无变化。

两个 batch 的 CLOSED 状态跳变原因仍未查明。第 44 轮建议的「孤儿 batch 复活逻辑」尚未实现。Bot 在 06:50 后离线，下次启动时需要观察 batch 恢复行为。

**修复优先顺序不变：**
1. **P0**: 调查 batch 状态跳变原因（TAKE_PENDING→CLOSED 无日志证据）
2. **P0**: 实现孤儿 batch 复活逻辑（检测 CLOSED + take_filled_size=0 + exit_filled_size=0 + closed_ts=None → 重置为 TAKE_PENDING）
3. **P0**: 修复 `taker_buy()` 浮点精度
4. **P0**: take 前检查余额
5. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
6. **P1**: 修复 fill-to-batch 链接 + DB 写入

---

## 第 44 轮（2026-08-11 ~06:40 BJT）— Scheduled Task double-take-exit-monitor

### 状态：Bot 06:20 启动后持续运行中（06:36+ 仍在报价），reward-exit 路径无任何活动

#### Bot 运行时间线（第 44 轮查看到的所有启动）

**第一次**（05:39:33 → 05:40:09，36 秒）：batch1 短暂重试（1 次 BATCH_TAKE_SUBMITTED），bot 退出
**第二次**（05:55:14 → 06:00:31，5 分 17 秒）：Ralph Norman 裸仓（152 YES）通过 **P1 recovery episode**（非 reward-exit）管理，被动 maker + escalated taker_buy 路径，所有 FAK 被拒（"no orders found"），余额不足阻止 MrBeast 下单
**第三次**（06:07:45 → 06:08:50，65 秒）：短暂报价 Eastern Europe + MrBeast 70-80M
**第四次**（06:20:05 → **06:36:42+，仍在运行**）：报价 MrBeast 70-80M + FIFA 2030。06:24-06:28 期间出现 6 次 SSL EOF 错误（Polymarket API 瞬时中断）。无任何 reward-exit 事件。

#### 关键观察

🔴 **Batch 状态 CLOSED 已持续至第 4 轮监控，完全无变化**。两个 batch 的 `closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0`。`updated_ts` 仍等于 `created_ts`。

🟡 **Ralph Norman 市场持仓爆炸**：创建 batch1 时原始敞口是 4 股 NO（触发 reward-exit 双倍退出），但现在 bot 接管了 **152 股 YES** 裸仓。这 152 YES 走的是传统 P1 recovery 路径（`stage=passive/escalated`），而非 reward-exit batch。因为 batch 已是 CLOSED，`get_open_reward_exit_batches()` 返回空，bot 不会 lock 该市场。

🟡 **Batch2 市场（MrBeast 60-70M views）从未被报价**：当前 bot 报价的是 70-80M 市场（不同 token），batch2 的市场不在扫描范围内。即使 batch 是 ACTIVE 状态，bot 也不会处理 batch2 的退出。

🟢 **Bot 目前在线且稳定**：第四次运行从 06:20 持续到 06:36+，已超过 16 分钟，正常执行普通做市周期（刷新报价、P0 guard 清理）。出现了偶发 SSL EOF 错误（`查询持仓市场失败`），bot 自动恢复。

🔴 **12 次 BATCH_TAKE_EXECUTED 的 FAK 填量完全丢失于 DB**：`take_filled_size` 全为 0.0，`reward_exit_fills` 缺少 batch_take intent 的记录。`reward_exit_orders` 表仍为空（FAK 下单失败不写此表）。

🔴 **Fill-to-batch 链接全为空**：2 条 reward_exit_fills 的 `batch_id` 均为空字符串（origin fill 发生在 batch 创建之前）。

✅ **无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED**

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 43 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | **207** | **+1** |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

### 数据库状态（无变化 vs 第 43 轮）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward, batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~8.3h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~7.4h |

### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 4 轮无解**：`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。由于 batch 已 CLOSED：
- `get_open_reward_exit_batches()` 返回空 → `_reward_exit_locked` 为空 → 市场不会被 LOCK
- `_advance_take_pending()` / `_advance_sell_pending()` 都不会触发
- 原 batch 市场的持仓通过传统 P1 recovery（非 reward-exit）路径管理

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0，但因 batch 已 CLOSED 且 lock 是内存状态，实际市场当前未被 lock。

🔴 **12 次 FAK 成交的填量完全丢失**：DB 的 `take_filled_size` 全为 0.0，`reward_exit_fills` 表没有 batch_take intent 的记录。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。

🟡 **Stock 持仓规模变化**：batch1 市场从原 4 NO 敞口变为 152 YES 裸仓，通过传统 P1 recovery 路径管理（非 reward-exit）。

✅ **无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED**

### 结论

第 44 轮状态与第 43 轮基本一致：两个 batch 保持 CLOSED 状态，bot 的 reward-exit 路径完全停止运作。bot 当前在线（06:20 启动，06:36+ 仍在运行），活跃报价 2 个市场（MrBeast 70-80M + FIFA 2030），但无任何 reward-exit 逻辑触发。

batch 状态跳变（TAKE_PENDING→CLOSED）的根本原因仍未查明。代码审查没有找到会静默将 batch 设为 CLOSED 的路径——两个 `close_reward_exit_batch()` 调用都在 `rem <= 0` 时（sell 完成后），而这两个 batch 从未进入 sell 阶段。如果 batch 未被手动改为 CLOSED，那么问题在代码的某个未发现的角落。

**建议下一步**：在 `_run_reward_exit_batch_tick` 开头加入「CLOSED 但未完成」的 batch 复活逻辑——检测到 `take_filled_size=0 ∧ exit_filled_size=0 ∧ closed_ts=None` 的 batch 时，将其重置为 TAKE_PENDING 并重新进入 take 流程。这是 get_open_reward_exit_batches() 的调用处可以加的防御性代码。

---

## 第 43 轮（2026-08-11 06:21 BJT）— Scheduled Task double-take-exit-monitor

### 状态：Bot 06:20 正在运行中，batch 状态保持 CLOSED（无变化），reward-exit 路径未触发

#### Bot 运行时间线（第 43 轮查看到的所有启动）

**第一次**（05:39:33 → 05:40:09，36 秒）：batch1 重试后退出
**第二次**（05:55:14 → 06:00:31，5 分 17 秒）：Ralph Norman 裸仓通过 P1 recovery（非 reward-exit）管理，bot 因余额不足 + FAK 被拒退出
**第三次**（06:08:03 → 06:08:50，47 秒）：短暂报价 MrBeast 70-80M 后退出
**第四次**（06:20:05 → 06:21:27+，运行中）：当前正在报价 2 个市场（MrBeast 70-80M + FIFA 2030），Ralph Norman 市场持仓被接管但未触发 reward-exit batch

#### 关键观察

🔴 **Batch 状态仍为 CLOSED**：`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`。状态跳变原因仍未查明。因为 batch 是 CLOSED，`get_open_reward_exit_batches()` 永远返回空列表，导致：
- `_reward_exit_locked` 集合为空 → 两个市场都不会被 LOCK
- `_advance_take_pending()` 不会触发 → 不会再尝试 take
- `_advance_sell_pending()` 不会触发 → 不会再尝试 sell

🟡 **Ralph Norman 裸仓走的是 P1 recovery 而非 reward-exit**：bot 06:20 运行中接管了 Ralph Norman 市场的持仓（adopting held position），但因为没有活跃 reward-exit batch，它走的是传统 recovery episode 路径（被动 maker BUY NO 补单），而非 reward-exit 的双倍退出路径。

🟡 **Batch2 市场（MrBeast 60-70M）不在扫描范围**：bot 当前报价的是 70-80M 市场，batch2 的 60-70M 市场从未被扫描到。即使 batch 恢复逻辑正确，batch2 也无法被触发。

🟢 **Bot 目前在线**：06:21:27 仍在记录 P0 guard 历史清理（删除 362 条），bot 正在活跃报价 2 个市场（MrBeast 70-80M + FIFA 2030）。

🔴 **12 次 BATCH_TAKE_EXECUTED 仍未被计入 DB**：日志中 batch1 有 10 次 FAK 买入成交（filled=14.80 each），batch2 有 2 次（filled=170.49, 175.12），但 DB 的 `take_filled_size` 全为 0.0，`take_notional_usd` 也为 0.0。`reward_exit_orders` 表完全为空（FAK 失败不记录）。

🔴 **Fill-to-batch 链接全为空**：`reward_exit_fills` 表中 2 条记录的 `batch_id` 均为空字符串。origin fill 的记录发生在 batch 创建之前，所以 `batch_id` 无法回填。batch_take fills（12 次 FAK 成交）甚至没有写入 `reward_exit_fills` 表。

✅ **无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED**

### 事件统计（累计，8-10 + 8-11 日志合计）

| 事件 | 次数 | 变化 vs 第 42 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | 206 | - |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

### 数据库状态

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0`，`exit_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward, batch_id=`""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 下单失败不写入此表 |

### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 |
|----------|------|----------|-------------|---------------------|------|------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 |

> 注：BATCH_TAKE_EXECUTED 的 filled 值是 FAK 订单被 Polymarket 端部分匹配的量，不是 bot 请求的量。bot 请求 size=7 和 88，但 API 以更大数量匹配（可能包含其他挂单的部分成交）。这些填量未回写到 DB 的 `take_filled_size`。

### 异常信号检测

🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 2 轮无解**：`closed_ts=None`，无 REWARD_EXIT_BATCH_CLOSED 日志。可能原因：
- 代码中存在未记录日志的 batch 关闭路径（需要全面代码审计）
- 直接 DB 操作（手动 SQL UPDATE）
- 可能性排除：第 42 轮监控数据有误（第 43 轮再次确认 CLOSED）

🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0，但因为 batch 已 CLOSED 且 `_reward_exit_locked` 是内存状态（无持久化），实际上市场当前未被 lock。

🔴 **12 次 FAK 成交的填量完全丢失**：DB 的 `take_filled_size` 全为 0.0，`reward_exit_fills` 表没有 batch_take intent 的记录。

🔴 **Fill-to-batch 链接仍为空**：`batch_id=""` 于所有 reward_exit_fills。

✅ 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED

### 结论

第 43 轮状态与第 42 轮基本一致：两个 batch 保持 CLOSED 状态，bot 的 reward-exit 路径完全停止运作。bot 当前在线（06:20 启动），但仅在做普通做市（2 个市场），没有触发任何 reward-exit 逻辑。

**根本问题**：batch 是如何从 TAKE_PENDING 变成 CLOSED 的？代码中 `close_reward_exit_batch()` 的两处调用都在 `rem <= 0` 时（即 sell 完成后），但这两个 batch 从未进入 sell 阶段（`exit_filled_size=0.0`）。没有其他关 batch 的路径会在不记录 REWARD_EXIT_BATCH_CLOSED 日志的情况下将状态设为 CLOSED——除非存在尚未被发现的代码路径，或 DB 被手动修改。

**建议下一步**：如果 bot 要保持在线，考虑在 `_run_reward_exit_batch_tick` 开头增加一个 "孤儿 batch 复活" 逻辑：检测到 `take_filled_size=0 ∧ exit_filled_size=0 ∧ closed_ts=None` 的 CLOSED batch 时，将其重置为 TAKE_PENDING 并重新进入 take 流程。这比彻底排查状态跳变原因更快，也能让这两个批次的 ~$27 名义奖励得到处理。

---

## 第 42 轮（2026-08-11 06:09 BJT）— Scheduled Task double-take-exit-monitor

### 状态：Bot 两次短暂启动后关闭，两个 batch 状态变为 CLOSED（无日志证据），batch 在第二次运行中完全未被引用

#### Bot 运行时间线

**第一次运行**（05:39:33 → 05:40:09，36 秒）：
- 05:39:33 — 启动，加载 4 个 banned 市场、7 个 unpaired_since 条目
- 05:40:07 — **batch1 重试**：BATCH_TAKE_SUBMITTED (batch1, remaining=7, price=0.9900)
- 05:40:09 — Bot 退出

**第二次运行**（05:55:14 → 06:00:31，5 分 17 秒）：
- 05:55:14 — 启动，权益基准 $284.61
- 05:55:34 — 开始报价：Eastern Europe & CIS TI 2026、MrBeast 70-80M
- 05:55:42-55 — 接管持仓：Ty Masterson、Ralph Norman（152 股 YES 裸仓）
- 05:55:50 — Ralph Norman recovery quote: BUY NO 152 @ 0.510（非 reward-exit batch）
- 05:55:53 — P1 recovery episode decision: buy_complement, stage=passive, unpaired=152
- 05:56:41 — P0 guard 历史清理：删除 4722 条
- 05:58:59 — Ralph Norman recovery 升级到 escalated stage
- 05:59:07-06:00:22 — 多次 taker buy 失败："no orders found to match with FAK order"（market ask 价高于 0.570 限价）
- 05:58:27 — 余额不足：not enough balance ($215 vs $249 needed)，订单 POST_FAILED
- 06:00:31 — Bot 退出

#### 关键观察

🟡 **batch 状态异常变为 CLOSED**：第 41 轮时两个 batch 均为 TAKE_PENDING，本轮 DB 查询显示为 CLOSED。但日志中**完全没有** REWARD_EXIT_BATCH_CLOSED 事件，batch 的 `closed_ts` 为 None，`updated_ts` 仍等于 `created_ts`（未更新）。没有 BATCH_TAKE_COMPLETED、BATCH_SELL_PLACED 日志。两个 batch 的 `take_filled_size` 仍为 0.0。这个 CLOSED 状态缺乏证据链支持。

🟡 **第二次运行完全未引用 reward-exit batch**：bot 在 05:55-06:00 期间没有产生任何 reward-exit 相关日志（无 LOCKED、无 UNLOCKED、无 TAKE_SUBMITTED）。Ralph Norman 的裸仓通过 P1 recovery episode（非 reward-exit batch）在管理，以被动 maker BUY NO 报价 152 股 @ 0.510 进行补单，并最终 escalated 到 taker buy 路径（但全部因 "no orders found" 失败）。

🔴 **余额不足阻止了 MrBeast 70-80M 市场下单**：bot 在 05:58:27 因余额不足（$215 余额 vs $249 所需）被拒，导致该市场一个订单下单失败。

🟢 **P1 recovery escalated 正确触发**：Ralph Norman 市场的被动 recovery（maker BUY NO）在 escalated 窗口后自动转为 active taker buy 路径。但由于市场 ask 价格（~0.570）高于 bot 的 taker buy 限价，全部 FAK 被拒绝。

### 事件统计（累计）

| 事件 | 次数 | 变化 vs 第 41 轮 |
|------|------|-------------------|
| REWARD_EXIT_BATCH_OPENED | 2 | - |
| BATCH_TAKE_SUBMITTED | 206 | +1 |
| BATCH_TAKE_EXECUTED | 12 | - |
| MARKET_REWARD_EXIT_LOCKED | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | - |
| BATCH_SELL_PLACED | 0 | - |
| BATCH_SELL_PARTIAL | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | - |
| BATCH_MANUAL_HOLD | 0 | - |
| BATCH_LOSS_LOCKED | 0 | - |

### 数据库状态

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | ⚠️ 均为 CLOSED（第 41 轮为 TAKE_PENDING），但 `closed_ts=None`，`updated_ts==created_ts`，`take_filled_size=0.0` |
| reward_exit_fills | 2 | intent=normal_reward, batch_id 均为空字符串 |
| reward_exit_orders | 0 | FAK 失败不记录 |

### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | CLOSED | 22:19 8/10 | ~7.8h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | CLOSED | 23:12 8/10 | ~6.9h |

### 异常信号检测

🔴 **Batch 状态异常跳变**：两个 batch 从 TAKE_PENDING → CLOSED，无 REWARD_EXIT_BATCH_CLOSED 日志，`closed_ts=None`，`updated_ts` 未更新，`take_filled_size` 仍为 0.0。这可能由以下原因之一导致：
- 直接 DB 修改（手动 SQL UPDATE）
- 代码中存在未记录日志的 batch 关闭路径
- 第 41 轮监控数据有误（当时可能实际已经是 CLOSED）

🟡 **第二次 bot 运行未识别到 reward-exit batch**：如果 batch 确实已标记为 CLOSED，则 `get_open_reward_exit_batches()` 返回空列表，`MARKET_REWARD_EXIT_LOCKED` 不会触发，batch 恢复也不会执行。这解释了为什么 05:55 运行没有 reward-exit 事件。

🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 均为空（第 41 轮已知问题，未修复）。

🔴 **TAKE_EXECUTED 填量丢失**：batch_take 成交（BATCH_TAKE_EXECUTED 共 12 次）未回写到 DB 的 `take_filled_size`（第 41 轮已知问题，未修复）。

✅ 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED

### 结论

第 42 轮出现了一个新的异常信号：两个 batch 的状态变为 CLOSED，但没有任何日志证据支持完成。如果这是代码行为，需要调查是哪个路径触发了无日志的 batch 关闭。如果状态确实是 CLOSED，则意味着 double-take-exit 流程从未完成（take=0, sell=0），两个 batch 的原始奖励成交（共约 $27 名义）未得到双倍退出处理。

bot 目前离线。下一次启动时需要观察：
1. batch 的 CLOSED 状态是否影响 Ralph Norman 市场的奖励退出锁
2. batch2（MrBeast 60-70M）的市场是否仍在扫描范围内

修复优先顺序：
1. **P0**: 调查 batch 状态跳变原因（代码路径 vs 直接 DB 操作）
2. **P0**: 修复 `taker_buy()` 浮点精度
3. **P0**: take 前检查余额
4. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
5. **P1**: 修复 fill-to-batch 链接 + DB 写入

---

## 第 41 轮（2026-08-11 ~12:30 BJT）— Scheduled Task double-take-exit-monitor

### 状态：Bot 05:39 短暂启动（36 秒后退出），batch1 被重试但未完成，batch2 未被触发

Bot 在 2026-08-11 05:39:33 启动，36 秒后在 05:40:09 退出（"正在退出，撤销全部订单"）。关键事件：

- **05:39:33** — Bot 启动，加载 4 个 banned 市场、7 个 unpaired_since 条目
- **05:39:51** — 开始报价 MrBeast 70-80M views 市场（注意：这是**不同市场**，不是 batch2 的 60-70M views 市场）
- **05:40:00-02** — 接管的持仓包括 Ralph Norman（batch1 市场）、Ty Masterson、András Baka 市场
- **05:40:07** — **batch1（Ralph Norman）重试**：BATCH_TAKE_SUBMITTED (size=7, remaining=7, price=0.9900, mode=active)
- **05:40:09** — Bot 退出

**关键观察**：
- ✅ 代码修复后的 batch 恢复逻辑**生效了**：bot 启动时检测到了 Ralph Norman 市场的活跃 batch 并尝试继续 take
- ❌ batch2（MrBeast 60-70M views, `0xdf08...`）**未被重试**：bot 未引用该市场（报价的是 MrBeast 70-80M views），可能因为该市场不在当前扫描范围内
- ❌ Bot 仅运行 36 秒就退出，batch1 的 take 没有时间完成
- ❌ 两个 batch 的 remaining 仍为创建时值（7 和 88），即使 API 可能已部分成交过

### 事件统计（vs 第 40 轮）

| 事件 | 第 40 轮 | 第 41 轮 | 变化 |
|------|---------|---------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 2 | - |
| BATCH_TAKE_SUBMITTED | 205 | **206** | **+1** |
| MARKET_REWARD_EXIT_LOCKED | 4 | 4 | - |
| BATCH_TAKE_COMPLETED | 0 | 0 | - |
| BATCH_TAKE_PARTIAL | 0 | 0 | - |
| BATCH_SELL_PLACED | 0 | 0 | - |
| BATCH_SELL_PARTIAL | 0 | 0 | - |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | - |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | - |
| BATCH_MANUAL_HOLD | 0 | 0 | - |
| BATCH_LOSS_LOCKED | 0 | 0 | - |

### 数据库状态（无变化）

| 表 | 行数 | 详情 |
|---|------|------|
| reward_exit_batches | 2 | 均为 TAKE_PENDING, take_filled_size=0.0, exit_filled_size=0.0, updated_ts 未变化 |
| reward_exit_fills | 2 | intent=normal_reward, batch_id 均为空字符串 |
| reward_exit_orders | 0 | FAK 失败不记录 |

### Batch 详情

| Batch ID | 市场 | 原始成交 | take_target | SUBMITTED | 创建 | 孤儿时长 | 最新活动 |
|----------|------|----------|-------------|-----------|------|----------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 91 | 22:19 | ~7.5h | 05:40:07 重试 (remaining=7) |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 115 | 23:12 | ~6.6h | 23:30:59 最终 SUBMITTED |

### 异常信号检测

🟡 **Bot 05:39 短暂启动 36 秒后退出**：可能是手动测试重启（观察 batch 恢复行为）。
🟢 **Batch 恢复逻辑生效**：bot 启动时检测到活跃 batch 并重试了 batch1（Ralph Norman）。代码修复（检测启动时已存在的 batch）工作正常。
🔴 **Batch2 未被触发**：MrBeast 60-70M 市场不在 bot 报价范围内（bot 报的是 MrBeast 70-80M），batch2 的恢复依赖市场被扫描到。
🔴 **两个 batch still TAKE_PENDING**：batch1 ~7.5h, batch2 ~6.6h。DB unchanged。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0。
✅ 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED

### 结论

第 41 轮有一个重要的积极信号：batch 恢复逻辑生效（bot 启动时检测并重试了 batch1）。但由于 bot 仅运行 36 秒就退出，实际效果无法验证。batch2 未被触发可能是因为其市场不再在扫描范围内。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度
2. **P0**: take 前检查余额
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入

---

## 第 40 轮（2026-08-11 05:38 BJT）— Scheduled Task double-take-exit-monitor

### 状态：Bot 离线 ~6.1h，状态完全冻结

Bot 自 2026-08-10 23:31 BJT 退出后未再启动。无 2026-08-11 日志文件。数据库状态与前几轮完全相同，无任何新事件触发。

### 事件统计（累计，无变化 vs 第 39 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 205 |
| BATCH_TAKE_EXECUTED | 12 |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

---

## 第 39 轮（2026-08-11 05:21 BJT）— Scheduled Task double-take-exit-monitor

（内容截断，完整历史见 git history）

---

## 历史轮次摘要

| 轮次 | 时间 | 关键发现 |
|------|------|----------|
| 69 | 08-11 12:51 | 与第 68 轮完全一致。DB 冻结 ~14.6h。Bot 在线（12:51+ 报价 Team Vitality LEC 2026 + FIFA 2030）。reward-exit 路径完全冻结，零变化。batch CLOSED 持续 28 轮。无 SSL EOF 错误。|
| 68 | 08-11 12:37 | 与第 67 轮完全一致。DB 冻结 ~14.3h。Bot 在线（12:36+ 报价 Team Vitality LEC 2026 + FIFA 2030）。reward-exit 路径完全冻结，零变化。batch CLOSED 持续 27 轮。无 SSL EOF 错误。|
| 67 | 08-11 12:25 | 与第 66 轮完全一致。DB 冻结 ~14.1h。Bot 在线（12:23+ 报价 FIFA 2030 + Team Vitality LEC 2026）。SSL EOF 偶发（2 次）自动恢复。reward-exit 路径完全冻结，零变化 ~14.1h。batch CLOSED 持续 26 轮。 |
| 66 | 08-11 12:08 | 与第 65 轮完全一致。DB 冻结 ~13.8h。Bot 在线（12:06+ 报价 FIFA 2030 + Team Vitality LEC 2026）。reward-exit 路径完全冻结，零变化。 |
| 65 | 08-11 11:55 | 与第 64 轮完全一致。Bot 在线（11:42-11:50+）。reward-exit 完全冻结。CLOSED 状态跳变已 17+ 轮无解。 |
| 64 | 08-11 11:37 | 与第 63 轮完全一致。Bot 在线（11:32+）。reward-exit 完全冻结。 |
| 63 | 08-11 11:22 | 与第 62 轮完全一致。Bot 在线（11:21+）。reward-exit 完全冻结。 |
| 51 | 08-11 | bot 5 次启动共在线 ~1h47m；最新 08:02 正常退出；reward-exit 完全静默；batch 状态 CLOSED 已 10 轮；DB 冻结 ~18h+；唯一 reward-exit 事件仍为 05:40 batch1 retry |
| 50 | 08-11 08:08 | bot 运行 2h23m（05:39→08:02），正常退出（非崩溃）；reward-exit 完全静默；唯一 reward-exit 事件仍为 05:40 batch1 retry；DB 冻结 ~17h；bot 运行时长持续增长至历史最长；MrBeast 70-80M mid 0.786 无变化；无异常无崩溃 |
| 49 | 08-11 07:53 | bot 在线运行 2h11m+（05:39→07:50+），reward-exit 完全静默；唯一 reward-exit 事件为 05:40 batch1 retry；DB 冻结 16.5h；bot 运行时长持续增长；MrBeast 70-80M mid 0.785-0.786 稳定；无 SSL EOF 错误（07:48+ 平稳） |
| 48 | 08-11 07:35 | bot 在线运行 1h56m+（05:39→07:35+），reward-exit 完全静默；MrBeast 70-80M mid 0.786 超出范围；DB 冻结 15h |
| 47 | 08-11 07:21 | bot 在线运行 59m+（06:21→07:20+），reward-exit 完全静默；MrBeast 70-80M mid 0.776 超出报价范围；DB 冻结 14.9h；异常信号与前 6 轮一致 |
| 46 | 08-11 12:30 | DB 完全冻结 14h；bot 6 次启动中仅 1 次 reward-exit 事件（batch1 retry）；最长时间运行 43m 但未触及 reward-exit；异常信号与前 5 轮一致 |
| 45 | 08-11 06:51 | 无新 reward-exit 事件；bot 06:50 离线；DB 冻结 |
| 44 | 08-11 06:40 | bot 06:20-06:36+ 运行中，reward-exit 路径完全静默 |
| 43 | 08-11 06:21 | bot 06:20 运行中；batch 状态 CLOSED 持续 2 轮；Ralph Norman 裸仓走 P1 recovery |
| 72 | 08-11 13:40 | batch 永冻 ~15.3h；bot 在线报价 Grok 4.6 ($400/天) + FIFA 2030；新市场 Grok 4.6 取代 Team Vitality LEC 2026；无新 reward-exit 事件 |
| 41 | 08-11 12:30 | batch 恢复逻辑生效；bot 36s 后退出 |
| 40 | 08-11 05:38 | bot 离线 ~6.1h，状态完全冻结 |
| 39 | 08-11 05:21 | 代码修复 A/B/C/D 已合入 |
| 38 | 08-11 04:45 | 第一次完整 cycle 分析，识别 4 个关键 deadlock |
| 15 | 08-11 | 代码审查四轮修复 |
| 1-14 | 08-10 | 初始实现与调试 |

---

## 第 78 轮（2026-08-11 15:05 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 在线正常运行（LoL Esports GotY + Ülle Madise），reward-exit 路径完全冻结，零变化 ~18.75h+

#### 与第 77 轮对比

| 指标 | 第 77 轮 | 第 78 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| BATCH_TAKE_SUBMITTED | 206 | 206 | 无（Aug 11 05:40 那次已计入） |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |
| Bot 报价状态 | 在线 14:48+ | 在线 15:04+（最后日志） | 正常，市场不变 |

#### Bot 运行状态

- Bot 在 15:04:07 最后日志：ORDER_PLACED + ORDER_CANCELLED expiry_refresh（LoL Esports GotY 2026 + Ülle Madise President of Estonia），随后一次瞬时 SSL UNEXPECTED_EOF 已自动恢复
- **市场无变化**：与第 77 轮相同的两个市场（League of Legends Esports GotY 2026 + Ülle Madise President of Estonia），无轮换
- 报价循环正常：ORDER_PLACED → ORDER_CANCELLED (expiry_refresh) 轮换，约每 2-3 分钟
- 运行持续超过 9.5 小时（05:39 启动至今）
- 无 reward-exit 相关 ERROR
- 1 次瞬时 SSL EOF（15:04:07），已自动重试恢复（0.3 秒后）
- **余额不足持续**：batch 1 (1e5af08f) 在 Aug 11 05:40 尝试提交 FAK BUY 互补 token @ 0.99 时因余额不足失败（balance: 5046679 vs order: 7252890），Aug 10 晚同样问题多次出现

#### 事件统计（零变化 vs 第 77 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 (所有在 Aug 10) |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（Python sqlite3 直接查询确认，完全冻结，零变化 ~18.75h+）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），为 origin 奖励成交 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化 vs 第 77 轮）

| Batch ID | 市场 | 原始成交 | take_target | BATCH_TAKE_EXECUTED | 状态 | 创建 | 孤儿时长 |
|----------|------|----------|-------------|---------------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ 0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 22:19 8/10 | ~18.75h+ |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ 0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 23:12 8/10 | ~17.85h+ |

#### 异常信号检测

🔴 **两个 batch 永冻（~18.75h+）**：无变化，自第 73 轮以来完全冻结。  
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0，bot 重启后锁自动失效（未持久化）但 reward-exit 路径未恢复。  
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串，未建立关联。  
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，日志有 12 次 EXECUTED。  
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 37 轮无解**：自第 42 轮发现以来无变化。  
🔴 **FORCED_EXIT 而非正常关闭**：bot 在 8/10 23:31:07 退出，两个 batch 从未进入 exit phase。  
🔴 **余额不足导致 FAK 失败**：Aug 11 05:40 batch 1 提交 FAK @ 0.99 时 balance=5046679 < order_amount=7252890，该 batch 即使有锁也无法完成 take。  
🟡 **Aug 10 相同余额问题**：两个 batch 在 Aug 10 晚都有"not enough balance / allowance"错误重复出现（batch 2 更严重：balance 5046679 vs order 86963450 for 88 shares @ 0.99）。  
🟢 Bot 在线运行正常（15:04+ 报价 LoL Esports GotY + Ülle Madise President of Estonia），reward-exit 路径静默。  
🟢 1 次瞬时 SSL EOF（15:04:07），已自动恢复。  
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。  
🟢 市场无轮换。

#### 结论

第 78 轮与第 77 轮状态完全一致，零变化。本监控任务已持续 78 轮（约 35.25+ 小时）。reward-exit 路径仍完全冻结。两个 batch 自第 42 轮（~2026-08-11 06:09）起进入 CLOSED 冻结态，已持续 37 轮约 9.0+ 小时。

**新增发现**：batch 1 在 Aug 11 05:40 尝试提交 FAK 但因余额不足失败（balance 5046679 < order 7252890）。这确认了即使 market 锁存在，balance 不足也会阻止 take 完成——而 bot 在 8/10 退出后，余额可能因其他原因（如其他市场的订单占用）不足。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪（而非 7 forever）
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 部分成交但 remaining 未更新
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）


---

## 第 95 轮（2026-08-11 18:40 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 Bot 重启中（刚启动 18:38+），reward-exit 路径完全冻结，零变化 ~20.5h+

#### 与第 94 轮对比

| 指标 | 第 94 轮 | 第 95 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| BATCH_TAKE_SUBMITTED (Aug 10) | 205 | 205 | 无 |
| BATCH_TAKE_SUBMITTED (Aug 11) | 1 | 1 | 无（05:40 那次） |
| BATCH_TAKE_EXECUTED | 12 | 12 | 无 |
| Bot 报价状态 | 离线 | 刚启动（18:38） | 等待扫描市场 |

#### Bot 运行历史（Aug 11 今日）

Bot 今天经历多次启动→退出循环：
- **05:39-05:40**（约 32s）：1 次 BATCH_TAKE_SUBMITTED（batch 1 retry），然后退出
- **05:55-06:00**（约 5m）：报价 Eastern Europe TI2026 + MrBeast 70-80M，退出
- **06:07-06:08**（约 50s）：再次报价同上市场，退出
- **06:20-06:21**（约 1.5m）：报价 MrBeast 70-80M + FIFA 2030，退出
- **06:22-06:23**（约 30s）：adopting held positions，退出
- **08:46**（瞬时）：adopting held positions，退出
- **14:10-15:04**（约 54m）：最长时间运行，报价 LoL Esports GotY + Ülle Madise Estonia，正常
- **15:04** 日志停止（SSL EOF），bot 可能退出
- **18:33-18:37**：bot 第二次启动→退出（36s），报价 LoL + FIFA 2030，Adopting Ralph Norman 等仓
- **18:38:08**：bot 第三次启动中（API key 错误），刚 loaded live_state + deposit-wallet merging

#### 今日奖励成交情况

- 无新 REWARD_EXIT_BATCH_OPENED 事件（Aug 11 零新增）
- 无 BATCH_SELL_PLACED / BATCH_SELL_PARTIAL / BATCH_SELL_COMPLETED
- 无 REWARD_EXIT_BATCH_CLOSED 正常关闭
- 无 MARKET_REWARD_EXIT_UNLOCKED
- Aug 10 的两个 batch 继续冻结

#### 事件统计（累计，零变化 vs 第 94 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 (所有在 Aug 10) |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（Python sqlite3 直接查询）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串） |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | TAKE_EXECUTED | 状态 | 创建 | 冻结时长 |
|----------|------|----------|-------------|---------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ $0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 8/10 22:19 | ~20.3h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ $0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 8/10 23:12 | ~19.45h |

#### 异常信号检测

🔴 **两个 batch 永冻（~20.3h+）**：无变化，持续冻结。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，日志有 12 次 EXECUTED。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 57 轮无解**：自第 37 轮发现以来无变化。
🔴 **FORCED_EXIT 而非正常关闭**：两个 batch 从未进入 exit phase。
🔴 **余额不足导致 FAK 失败**：Aug 10 共 248 次 "not enough balance" 错误；Aug 11 仅 1 次 BATCH_TAKE_SUBMITTED（05:40，bot 32s 后退出）。
🟡 **Bot 频繁重启**：今日至少 10 次启动→退出循环，最长连续运行 54m（14:10→15:04），大多数 < 5m。
🟡 **API key 错误**：18:38 出现 `Could not create api key` 错误（HTTP 400）。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 Bot 刚启动（18:38），等待扫描市场。

#### 结论

第 95 轮状态与第 94 轮完全一致，零变化。本监控任务已持续 95 轮（约 39+ 小时进行中，但 bot 仅在 ~55m 内运行过 reward-exit 路径）。两个 batch 自 Aug 10 晚冻结至今（~20.3h+）。

Bot 今日频繁重启（10+ 次），大多数运行时间 < 5m，reward-exit 路径仅在 05:40 有 1 次 BATCH_TAKE_SUBMITTED（batch 1 重试），随后因余额不足（$5.05 < $7.25）FAK 失败，bot 立即退出。此后 bot 从未在该市场重新报价（该市场被 banned 或不在扫描范围）。

修复优先顺序不变：
1. **P0**: 修复 `taker_buy()` 浮点精度 → 使 batch1 的 remaining 能正确追踪
2. **P0**: take 前检查余额 → batch1 余额不足导致 FAK 失败
3. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock）
4. **P1**: bot 启动时清理过期 batch + 修复 fill-to-batch 链接 + DB 写入 BATCH_TAKE_EXECUTED 补量


---

## 第 96 轮（2026-08-11 19:36 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 Bot 正常运行中（19:26+），reward-exit 路径完全冻结，零变化 ~21.3h+

#### 与第 95 轮对比

| 指标 | 第 95 轮 | 第 96 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| BATCH_TAKE_SUBMITTED (Aug 10) | 205 | 205 | 无 |
| BATCH_TAKE_SUBMITTED (Aug 11) | 1 | 1 | 无（05:40 那次） |
| BATCH_TAKE_EXECUTED | 12 | 12 | 无 |
| Bot 报价状态 | 刚启动（18:38） | 正常运行（19:26+） | 已进入稳定报价 |

#### Bot 当前运行状态

- Bot 在 19:26 重新启动，加载了 4 个 banned 市场
- 19:26:55-19:30:21：adopting 3 个 held positions（Baka Hungary, Ty Masterson, Ralph Norman）
- 19:30:12：WebSocket 订阅 4 个代币订单簿
- 19:30:26：自适应控制器调整 flatten_after_secs 90→105
- 19:30+：开始正常双边报价，当前活动市场：
  - "Will Anthropic have the best AI Agent at the end of September 2026?"（价格 0.47/0.50）
  - "Will League of Legends Win the 2026 Esports Game of the Year Award?"（价格 0.48/0.49）
- Bot 正常运行中，已持续 10 分钟+，无退出迹象

#### Banned 市场确认

```json
{
  "banned_cids": [
    "0x0f5a74c346b9c0cf13c6f7c8d2cf770feadb5dcacd28690ce3ea09458fc0b2ed",  // 未知市场
    "0xa05dab0066c271727fbc2a4ae29af5041d5a05ca1cde4e2808181f64ecaf7c56",  // 未知市场
    "0x392509ca6bf61a9725070926ded0217dd8a8aea4c1c17421c00596bc2fc45b2e",  // Ralph Norman
    "0xdf08f119869bb142772b645eeb71ae840c22f5237b1a14b5a974782dd8091522"   // MrBeast 60-70M
  ]
}
```

Ralph Norman 和 MrBeast 两个 reward-exit batch 所在市场均已在 banned 列表中，bot 不会再对其报价。

#### 今日奖励成交情况

- 无新 REWARD_EXIT_BATCH_OPENED 事件（Aug 11 零新增）
- 无 BATCH_SELL_PLACED / BATCH_SELL_PARTIAL / BATCH_SELL_COMPLETED
- 无 REWARD_EXIT_BATCH_CLOSED 正常关闭
- 无 MARKET_REWARD_EXIT_UNLOCKED
- Aug 10 的两个 batch 持续冻结（~21.3h）

#### Aug 11 "not enough balance" 错误

- Aug 10: 248 次
- Aug 11: 2 次（仅 05:39-05:40 启动期间的 batch 1 retry）
- 当前 19:26 启动后：0 次（reward-exit 路径无活动）

#### 事件统计（累计，零变化 vs 第 95 轮）

| 事件 | 次数 |
|------|------|
| REWARD_EXIT_BATCH_OPENED | 2 |
| BATCH_TAKE_SUBMITTED | 206 (205 Aug 10 + 1 Aug 11 @ 05:40) |
| BATCH_TAKE_EXECUTED | 12 (所有在 Aug 10) |
| MARKET_REWARD_EXIT_LOCKED | 4 |
| BATCH_TAKE_COMPLETED | 0 |
| BATCH_TAKE_PARTIAL | 0 |
| BATCH_SELL_PLACED | 0 |
| BATCH_SELL_PARTIAL | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 |
| BATCH_MANUAL_HOLD | 0 |
| BATCH_LOSS_LOCKED | 0 |

#### DB 状态（Python sqlite3 直接查询，无变化）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串） |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | TAKE_EXECUTED | 状态 | 创建 | 冻结时长 |
|----------|------|----------|-------------|---------------|------|------|----------|
| reward-exit-1e5af08f | Will Ralph Norman place second... | BUY NO 3.66 @ $0.52 | 7.32 | 10 次（filled=14.80 each） | CLOSED | 8/10 22:19 | ~21.3h |
| reward-exit-02cafdd7 | Will MrBeast 60-70M views | BUY YES 43.9 @ $0.57 | 87.8 | 2 次（filled=170.49, 175.12） | CLOSED | 8/10 23:12 | ~20.4h |

#### 异常信号检测

🔴 **两个 batch 永冻（~21.3h+）**：无变化，持续冻结。
🔴 **市场锁永不解锁**：MARKET_REWARD_EXIT_UNLOCKED 仍为 0。两个市场也被 banned。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串。
🔴 **TAKE_EXECUTED 填量丢失**：DB take_filled_size 全为 0.0，日志有 12 次 EXECUTED。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）已持续 58 轮无解**：自第 37 轮发现以来无变化。
🔴 **FORCED_EXIT 而非正常关闭**：两个 batch 从未进入 exit phase。
🔴 **余额不足导致 FAK 失败**：Aug 10 共 248 次；Aug 11 仅 1 次（05:40 batch 1 retry, bot 32s 退出）。
🟢 Bot 当前在 19:26 启动后正常运行，已进入稳定报价阶段。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。

#### 结论

第 96 轮状态与第 95 轮完全一致，零变化。本监控任务已持续 96 轮（约 40+ 小时进行中，bot 仅在首次启动后的~55m 内运行过 reward-exit 路径）。两个 batch 自 Aug 10 晚冻结至今（~21.3h+）。

由于两个 batch 所在市场已被 banned（ban 机制在 bot 启动后立即加载），即使 reward-exit 恢复活动，bot 也不会在这两个市场重新报价。

**关键观察**：目前 reward-exit 路径完全冻结不是因为 bot 没有运行（当前 bot 正在正常报价其他市场），而是因为：
1. 没有新奖励成交触发新的 REWARD_EXIT_BATCH_OPENED
2. 已有的两个 batch 已处于 CLOSED 状态（虽非正常关闭），不会有新的 take/sell 活动
3. 两个市场在 banned_markets.json 中，不会被重新扫入

修复优先顺序不变（同第 95 轮）。
5. **P2**: 调查 batch CLOSED 状态跳变根因（bot 退出时的 close 路径）

---

## 第 97 轮（2026-08-11 19:53 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 正常运行中（19:26+），reward-exit 路径完全冻结，零变化 ~22.0h+

#### 与第 96 轮对比

| 指标 | 第 96 轮 | 第 97 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| BATCH_TAKE_SUBMITTED (Aug 10) | 205 | 205 | 无 |
| BATCH_TAKE_SUBMITTED (Aug 11) | 1 | 1 | 无（05:40 那次） |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |
| BATCH_TAKE_EXECUTED (Aug 11) | 0 | 0 | 无 |
| "not enough balance" (Aug 10) | 248 | 248 | 无 |
| "not enough balance" (Aug 11) | 2 | 2 | 无（仅 05:39-05:40 session） |
| Bot 报价状态 | 正常运行（19:26+） | 正常运行（19:26+） | 持续稳定 >25min |

#### Bot 当前运行状态

- Bot 在 19:26 启动，已稳定运行约 27 分钟（最长连续运行的 session）
- 在 19:45 完成一次扫描，选定 2 个市场报价：
  - "Will Anthropic have the best AI Agent at the end of September 2026?"（0.47/0.50 buy, size 33）
  - "Will League of Legends Win the 2026 Esports Game of the Year Award?"（0.48/0.49 buy, size 20）
- 自适应控制器正常调整：flatten_after_secs 90→105→112→116→118→119
- 19:46:34 pUSD balance refresh SSL EOF（瞬时恢复）
- 19:52:11 py_clob_client_v2 request timeout（瞬时恢复）
- 4 个 banned 市场加载正常

#### Banned 市场确认（无变化）

```json
{
  "banned_cids": [
    "0x0f5a74c346b9c0cf13c6f7c8d2cf770feadb5dcacd28690ce3ea09458fc0b2ed",
    "0xa05dab0066c271727fbc2a4ae29af5041d5a05ca1cde4e2808181f64ecaf7c56",
    "0x392509ca6bf61a9725070926ded0217dd8a8aea4c1c17421c00596bc2fc45b2e",  // Ralph Norman (batch 1)
    "0xdf08f119869bb142772b645eeb71ae840c22f5237b1a14b5a974782dd8091522"   // MrBeast 60-70M (batch 2)
  ]
}
```

#### 奖励退出事件统计（累计，零变化 vs 第 96 轮）

| 事件 | Aug 10 次数 | Aug 11 次数 | 累计 |
|------|-------------|-------------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_SUBMITTED | 205 | 1 (05:40) | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |
| "not enough balance" 错误 | 248 | 2 | 250 |

#### DB 状态（Python sqlite3 直接查询，无变化 vs 第 96 轮）

| 表 | 行数 | 关键状态 |
|---|------|----------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`（空字符串），无 batch_take intent 记录 |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | EXECUTED 报告 | DB take_filled | 状态 | 创建 | 冻结时长 |
|----------|------|----------|-------------|---------------|----------------|------|------|----------|
| reward-exit-1e5af08f | Ralph Norman place second | BUY NO 3.66 @ $0.52 | 7.32 YES | 10 次（14.80 each） | 0.0 | CLOSED | 8/10 22:19 | ~22.0h |
| reward-exit-02cafdd7 | MrBeast 60-70M views | BUY YES 43.9 @ $0.57 | 87.8 NO | 2 次（170.49, 175.12） | 0.0 | CLOSED | 8/10 23:12 | ~21.1h |

#### 异常信号检测

🔴 **两个 batch 永冻（~22h+）**：无变化，持续冻结。自 Aug 10 晚创建以来从未完成正常生命周期。
🔴 **REWARD_EXIT_BATCH_CLOSED 从未触发（0/2）**：两个 batch 从未通过正常的 `_advance_sell_pending()` → `exit all sold` 路径关闭。
🔴 **MARKET_REWARD_EXIT_UNLOCKED 从未触发（0/2）**：两个市场锁从未解除，当前依赖 banned_markets.json 阻止重新报价。
🔴 **Fill-to-batch 链接缺失**：reward_exit_fills.batch_id 为空字符串，无法追溯 fill→batch 关系。
🔴 **TAKE_EXECUTED 填量丢失**：日志报告 12 次 BATCH_TAKE_EXECUTED（10+2），但 DB take_filled_size 全为 0.0。
🔴 **Batch 状态跳变（TAKE_PENDING→CLOSED）根因未确定**：自第 37 轮发现以来已持续 60 轮无变化，closed_ts=None 表明非正常 close 路径。
🔴 **余额不足持续**：Aug 10 共 248 次，Aug 11 还有 2 次（05:39 session）。bot 在提交 FAK 前未校验 committed_balance。
🟢 Bot 当前 session 正常运行超过 27 分钟，是今日最稳定的 session。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 SSL EOF 和 read timeout 都是瞬时的，bot 自动恢复。

#### 结论

第 97 轮状态与第 96 轮完全一致，reward-exit 路径零变化。两个 batch 自 Aug 10 晚冻结至今（~22h+）。

本监控任务已持续 97 轮（约 42+ 小时）。关键观察：
1. Bot 已从频繁重启模式（Aug 11 早间 10+ 次启动→退出）转为稳定运行（19:26 启动后持续 27m+）
2. reward-exit 路径完全冻结是因为无新 reward fill 触发 OPENED + 已有 batch 处于 CLOSED + 市场 banned
3. 所有 6 项 P0/P1/P2 修复优先级与第 95 轮一致，无新增问题

**当前 bot 健康度评估**：正常报价（2 个市场，Anthropic AI Agent 0.47-0.50 + LoL Esports 0.48-0.49），reward-exit 路径静默（无活动=无故障）。

---

## 第 98 轮（2026-08-11 20:06 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟢 Bot 持续稳定运行（19:26+，~40min），reward-exit 路径完全冻结 ~22.5h+

#### 与第 97 轮对比

| 指标 | 第 97 轮 | 第 98 轮 | 变化 |
|------|----------|----------|------|
| reward_exit_batches | 2 (均 CLOSED) | 2 (均 CLOSED) | 无 |
| reward_exit_fills | 2 (batch_id="") | 2 (batch_id="") | 无 |
| reward_exit_orders | 0 | 0 | 无 |
| BATCH_TAKE_SUBMITTED (Aug 10) | 205 | 205 | 无 |
| BATCH_TAKE_SUBMITTED (Aug 11) | 1 | 1 | 无（05:40） |
| BATCH_TAKE_EXECUTED (Aug 10) | 12 | 12 | 无 |
| BATCH_TAKE_EXECUTED (Aug 11) | 0 | 0 | 无 |
| "not enough balance" (Aug 10) | 248 | 248 | 无 |
| "not enough balance" (Aug 11) | 2 | 2 | 无 |
| Bot 运行时长 | ~27min | ~40min | Bot 持续稳定，今日最长 session |

#### Bot 当前运行状态（20:06 BJT 最新日志）

- Bot 在 19:26 启动，已稳定运行约 **40 分钟**，今日最长且最稳定的 session
- 正常报价 2 个市场（Anthropic AI Agent + LoL Esports Game of the Year）
- 19:45 扫描后选定报价，20:00 第二次扫描完成
- 自适应控制器：flatten_after_secs 稳定在 120
- 20:05:56 RECOVERY_EPISODE_DECISION：Anthropic 市场 unpaired=-33，sell_original 路径（P1 旧 recovery，非 reward-exit）
- 瞬态错误：19:46 SSL EOF、19:52 read timeout、20:03 429 Too Many Requests（position refresh），均自动恢复

#### 奖励退出事件统计（累计，零变化 vs 第 97 轮）

| 事件 | Aug 10 | Aug 11 | 累计 |
|------|--------|--------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |
| "not enough balance" | 248 | 2 | 250 |

#### DB 状态（无变化）

| 表 | 行数 | 关键状态 |
|---|------|----------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`exit_filled_size=0.0`，`updated_ts==created_ts` |
| reward_exit_fills | 2 | intent=normal_reward，`batch_id=""`，无 batch_take intent |
| reward_exit_orders | 0 | FAK 成交不写入此表 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | EXECUTED | DB filled | 状态 | 冻结 |
|----------|------|----------|-------------|-----------|-----------|------|------|
| reward-exit-1e5af08f | Ralph Norman place second | NO 3.66@$0.52 | 7.32 YES | 10×14.80 | 0.0 | CLOSED | ~22.3h |
| reward-exit-02cafdd7 | MrBeast 60-70M views | YES 43.9@$0.57 | 87.8 NO | 2×(170,175) | 0.0 | CLOSED | ~21.4h |

#### 异常信号检测

🔴 **两个 batch 永冻（~22.5h+）**：无变化。自 Aug 10 晚创建从未完成正常生命周期。
🔴 **MARKET_REWARD_EXIT_UNLOCKED 从未触发（0/4）**：LOCKED 了 4 次但从不解锁，两个市场依赖 banned_markets.json 阻止重入。
🔴 **Fill-to-batch 链接为空**：2 个 reward_exit_fills 的 batch_id 均为空字符串。
🔴 **TAKE_EXECUTED 12 次但 DB take_filled_size=0.0**：`_credit_take_fills()` 不写入 reward_exit_fills。
🔴 **余额不足校验缺失**：Aug 10 共 248 次 "not enough balance"，FAK 提交前未校验 committed_balance。
🟢 Bot 当前 session 持续稳定约 40 分钟，无异常退出。
🟢 无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED、无重复 fill_id。
🟢 SSL EOF / 429 / timeout 均为瞬态，bot 自动恢复。

#### 结论

第 98 轮与第 97 轮完全一致，reward-exit 路径零变化。监控已持续 98 轮（约 43+ 小时）。

**与旧 recovery 系统的对比观察**：bot 当前正在使用 P1 旧 recovery 路径（RECOVERY_EPISODE_DECISION）管理 Anthropic 市场 unpaired=-33 的库存，路径为 sell_original。这说明旧 recovery 系统仍在正常运行——只是 reward-exit（double-take）系统因无新触发条件而静默。

修复优先顺序不变（同第 95 轮）：
1. **P0**: TAKE 前校验 committed_balance，添加退避机制
2. **P1**: `_credit_take_fills()` 写入 reward_exit_fills + 填充 batch_id
3. **P1**: 调查 batch 状态跳变根因（TAKE_PENDING→CLOSED without closed_ts）
4. **P2**: 正常关闭后调用 MARKET_REWARD_EXIT_UNLOCKED
5. **P2**: TAKE_EXECUTED filled 值异常（14.80 和 170+ 远超 target）调查

---

## 第 99 轮（2026-08-11 20:55 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 98 轮完全一致，reward-exit 路径冻结 ~47h+

#### 事件统计（零变化 vs 第 98 轮）

| 事件类型 | Aug 10 日志 | Aug 11 日志 | 总计 |
|---------|------------|------------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |

#### 数据库状态（无变化）

| 表 | 行数 | 说明 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，take_filled_size=0.0，exit_filled_size=0.0，closed_ts=None |
| reward_exit_fills | 2 | intent=normal_reward，batch_id=""（空字符串 — orphan） |
| reward_exit_orders | 0 | FAK 订单不在此表记录 |

#### Aug 11 日志详情

Aug 11 日志共 4452 行（+74 行 vs 第 98 轮 4378）。新增内容：

1. **20:48 Bot 重启**（第 2 次今日启动）：权益基准 $292.84（vs 05:39 启动时的 $286.32），bot 正常扫描、报价 2030 FIFA World Cup 市场
2. **20:48 启动时无任何 batch 相关日志**：05:39 启动时有一次 BATCH_TAKE_SUBMITTED（重放 persistent batch），但 20:48 启动无此事件——说明 05:39 的 TAKE_SUBMITTED 之后 batch 已被清理为 CLOSED，20:48 从 DB 加载时不再触发
3. **Banned markets**：banned_markets.json 包含 4 个 cid（包括两个奖励退出市场 `0x3925...` 和 `0xdf08...`），确保 bot 不会再报价这两个市场

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | EXECUTED | DB filled | 状态 | 冻结时长 |
|----------|------|----------|-------------|-----------|-----------|------|------|
| reward-exit-1e5af08f | Ralph Norman place second | NO 3.66@$0.52 | 7.32 YES | 10×14.80 | 0.0 | CLOSED | ~46.5h |
| reward-exit-02cafdd7 | MrBeast 60-70M views | YES 43.9@$0.57 | 87.8 NO | 2×(170,175) | 0.0 | CLOSED | ~45.5h |

#### 异常信号检测

🔴 **两个 batch 冻结 ~47h**：自 Aug 10 晚创建以来从未完成正常生命周期。仍处于 CLOSED（但 closed_ts=None）。
🔴 **MARKET_REWARD_EXIT_UNLOCKED 从未触发（0/4）**：两个市场通过 banned_markets.json 阻止重入（非正常解锁机制）
🔴 **Fill-to-batch 链接为空**：2 个 reward_exit_fills 的 batch_id 仍为空字符串
🔴 **TAKE_EXECUTED 12 次但 DB take_filled_size=0.0**：`_credit_take_fills()` 不写入 reward_exit_fills
🔴 **余额不足校验缺失**：TAKE_SUBMITTED 共 206 次（Aug 10 205 + Aug 11 1），大量伴随 "not enough balance" 错误。~205 BATCH_TAKE_SUBMITTED 中有大量在 23:28-23:31 时段伴随余额不足——这些重复提交未产生任何成交
🟢 Ban 机制正常工作：2 个市场在 banned_markets.json 中，bot 启动时加载 4 个 banned cid，不会重新报价
🟢 Bot 当前 session（20:48 启动）稳定运行 ~7 分钟，正常报价 1 个市场
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED
🟢 batch-1 在 05:39 重启时最后一次 TAKE_SUBMITTED（余额不足），之后 batch 被清理为 CLOSED；20:48 重启无重放

#### 结论

第 99 轮与第 98 轮完全一致，reward-exit 路径零新活动。监控已持续 99 轮（约 47+ 小时）。

**唯一的新发现**：05:39 启动时的 BATCH_TAKE_SUBMITTED 重放触发第 1 次（也是唯一一次 Aug 11 的 TAKE_SUBMITTED），但余额不足导致失败，batch 随后被标记 CLOSED。20:48 重新启动时不再触发重放——说明 cleanup 逻辑在 05:39 session 中生效了。这解释了为什么 Aug 11 只有 1 次 BATCH_TAKE_SUBMITTED。

**余额不足的根本原因**：batch-1 的 complement price=0.99 需要买 7 股 = $6.93 order amount，但 bot 余额约 $5.05。batch-2 的 complement price=0.99 需要买 88 股 = $87.12 order amount，同样远超余额。实际可用余额（~$5）远低于所需金额（$7+87）。TAKE_SUBMITTED 在提交前未检查 committed_balance。

修复优先顺序不变。**下次有余额后重跑，观察新触发是否会正常完成完整生命周期。**

---

## 第 103 轮（2026-08-11 21:36 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 102 轮一致，reward-exit 路径静默（无新事件）

#### 事件统计

| 事件类型 | Aug 10 日志 | Aug 11 日志 | 总计 |
|---------|------------|------------|------|
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 2 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 206 |
| BATCH_TAKE_EXECUTED | 12 | 0 | 12 |
| BATCH_TAKE_COMPLETED | 0 | 0 | 0 |
| BATCH_TAKE_PARTIAL | 0 | 0 | 0 |
| BATCH_LOSS_LOCKED | 0 | 0 | 0 |
| BATCH_SELL_PLACED | 0 | 0 | 0 |
| BATCH_SELL_PARTIAL | 0 | 0 | 0 |
| REWARD_EXIT_BATCH_CLOSED | 0 | 0 | 0 |
| BATCH_MANUAL_HOLD | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_UNLOCKED | 0 | 0 | 0 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 4 |

Aug 10 日志 12208 行，Aug 11 日志 5209 行。0 个 REWARD_EXIT_BATCH_OPENED、0 个 BATCH_TAKE_COMPLETED、0 个 CLOSED/UNLOCKED。

#### 数据库状态

DB 仍被运行中 bot 锁定（disk I/O error），WAL 文件有 4.1MB。通过副本读取：

- `reward_exit_batches`：2 rows，均为 CLOSED 状态（closed_ts=None, take_filled_size=0.0, origin_fill_ts=0.0, first_take_submit_ts=None, first_take_executed_ts=None）
- `reward_exit_fills`：2 rows，batch_id=""（孤立），intent="normal_reward"
- `reward_exit_orders`：0 rows

**重要发现**：DB 中 batch 的 `first_take_submit_ts`、`first_take_executed_ts` 和 `origin_fill_ts` 全部为 None/0.0，表明 `_credit_take_fills()` 和 `_open_batch()` 的 timestamp 写入路径有 bug——实际在日志中 TAKE_SUBMITTED 和 TAKE_EXECUTED 均有发生。

#### Aug 11 日志变化

Aug 11 日志从 4967 行增至 5209 行（+242 行）。新增时间段 21:23-21:36：
- Bot 正常报价 "Will another city host the final of the 2030 FIFA World Cup?" 市场
- 跳过 "Will András Baka be the next President of Hungary?"（mid 0.998 超出范围）
- 两个 P1 recovery episode 保持 manual_hold（Baka terminal + Anthropic AI Agent escalated）
- **无任何 reward-exit 新事件**

#### 异常信号检测

- 🔴 两个 batch 冻结 ~47.2h，从未完成正常生命周期（REWARD_EXIT_BATCH_CLOSED 0 次）
- 🔴 DB timestamp 字段全部为 None/0.0（first_take_submit_ts, first_take_executed_ts, origin_fill_ts）——`_credit_take_fills()` 和 `_open_batch()` 的 timestamp 写入未生效
- 🔴 take_filled_size=0.0（DB 未更新 TAKE_EXECUTED 的 filled 量）
- 🔴 Fill-to-batch 链接为空（reward_exit_fills.batch_id=""）
- 🔴 MARKET_REWARD_EXIT_UNLOCKED 从未触发（靠 banned_markets.json 阻止重入）
- 🟡 余额不足 ~$5 vs 订单需求 $7+$87（已在第 101-102 轮深入分析）
- 🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED
- 🟢 Bot 当前 session（20:48 启动）稳定运行 ~48 分钟

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | EXECUTED | DB filled | 状态 | 冻结时长 |
|----------|------|----------|-------------|-----------|-----------|------|------|
| reward-exit-1e5af08f | Ralph Norman place second | NO 3.66@$0.52 | 7.32 YES | 10×(14.80 filled) | 0.0 | CLOSED | ~47.2h |
| reward-exit-02cafdd7 | MrBeast 60-70M views | YES 43.9@$0.57 | 87.8 NO | 2×(~170 filled) | 0.0 | CLOSED | ~46.5h |

#### 新识别的问题

1. **`_open_batch()` 未写入 timestamp**：`origin_fill_ts=0.0`、`first_take_submit_ts=None`、`first_take_executed_ts=None`——在日志中 22:19:15 已有 OPENED/SUBMITTED，但 DB 三个关键时间戳全部为空。需要检查 `_open_batch()` 和 `_open_reward_exit_batch()` 的 INSERT / UPDATE 路径。

2. **`_credit_take_fills()` 不更新 take_filled_size**：日志中 batch-1 有 10 次 EXECUTED（每次 filled=14.80），batch-2 有 2 次 EXECUTED，但 DB 中 `take_filled_size=0.0`。`_credit_take_fills()` 的正确实现路径未生效。

#### 结论

第 103 轮与第 102 轮完全一致，reward-exit 路径零新活动。监控已持续 103 轮（约 47+ 小时）。

两个问题比之前识别得更具体：（1）timestamp 写入全部缺失，指示 `_open_batch()` 路径有 bug；（2）`_credit_take_fills()` 的 filled 写入不生效——这两个问题阻碍了即使余额充足时 batch 的正常生命周期推进。

**新发现的 DB bug（timestamp + filled 未写入）优先级高于余额不足问题**，因为即使余额充足，DB 不会反映真实状态，无法判断 batch 是否完成了 take 阶段。

---

## 第 104 轮（2026-08-11 21:55 BJT）— Scheduled Task double-take-exit-monitor

### 状态：🟡 与第 103 轮完全一致，reward-exit 路径持续静默（零新事件）

#### 日志概况

| 指标 | Aug 10 | Aug 11 | 变化 vs 第 103 轮 |
|------|--------|--------|-------------------|
| 日志总行数 | 12208 | 5508 | +299 行（5209→5508） |
| REWARD_EXIT_BATCH_OPENED | 2 | 0 | 无变化 |
| BATCH_TAKE_SUBMITTED | 205 | 1 | 无变化（仅 05:40 那次） |
| BATCH_TAKE_EXECUTED | 12 | 0 | 无变化 |
| MARKET_REWARD_EXIT_LOCKED | 4 | 0 | 无变化 |
| "not enough balance" 错误 | 248 | 2 | 无变化 |
| BATCH_TAKE_COMPLETED / PARTIAL / SELL / CLOSED / UNLOCKED | 0 | 0 | 持续为零 |

Aug 11 日志新增行来自 21:36-21:50 时段：
- Bot 正常报价 "Will another city host the final of the 2030 FIFA World Cup?"（奖励 $135/天）
- P1 recovery episodes：Baka Hungary + Anthropic AI Agent 保持 manual_hold（terminal stage，exceeds_loss_budget）
- 21:51 重新扫描奖励市场（rotating out 7 tripped），仍只选到 FIFA 市场
- Baka Hungary mid 0.998 超出范围 [0.25, 0.75]，持续跳过
- **零 reward-exit 新事件**

#### 数据库状态（通过副本读取，无变化）

| 表 | 行数 | 状态 |
|---|------|------|
| reward_exit_batches | 2 | 均为 CLOSED，`closed_ts=None`，`take_filled_size=0.0`，`first_take_submit_ts=None`，`first_take_executed_ts=None`，`origin_fill_ts=0.0` |
| reward_exit_fills | 2 | `batch_id=""` 孤立，intent="normal_reward" |
| reward_exit_orders | 0 | FAK 无持久化 |

#### Batch 详情（无变化）

| Batch ID | 市场 | 原始成交 | take_target | EXECUTED | DB filled | 状态 | 冻结时长 |
|----------|------|----------|-------------|-----------|-----------|------|------|
| reward-exit-1e5af08f | Ralph Norman place second | NO 3.66@$0.52 | 7.32 YES | 10×(14.80 filled) | 0.0 | CLOSED | ~47.6h |
| reward-exit-02cafdd7 | MrBeast 60-70M views | YES 43.9@$0.57 | 87.8 NO | 2×(~170 filled) | 0.0 | CLOSED | ~46.7h |

#### 异常信号检测

🔴 **两个 batch 冻结 ~47.6h**：自 Aug 10 晚创建以来从未完成完整生命周期（REWARD_EXIT_BATCH_CLOSED 0 次）。
🔴 **MARKET_REWARD_EXIT_UNLOCKED 从未触发**：靠 banned_markets.json（4 个 cid）阻止重入，非正常解锁路径。
🔴 **DB timestamp 全部为 None/0.0**：`origin_fill_ts=0.0`、`first_take_submit_ts=None`、`first_take_executed_ts=None`——指示 `_open_batch()` + `_credit_take_fills()` 未写入关键时间戳。
🔴 **take_filled_size=0.0**：日志有 12 次 BATCH_TAKE_EXECUTED，但 DB 中 filled 为零。
🔴 **Fill-to-batch 链接为空**：reward_exit_fills.batch_id=""，2 个 fill 处于孤立状态。
🔴 **余额严重不足**：Aug 10 共 248 次 "not enough balance" 错误，bot 余额 ~$5.05 远低于订单需求 $7.25+$87.12。Aug 11 仅 2 次（bot 启动后 32s 退出）。
🟢 无重复 fill_id、无 MANUAL_HOLD、无 BATCH_LOSS_LOCKED。
🟢 Ban 机制正常工作：两个 market 的 cid 在 banned_markets.json 中，bot 不会重新报价。
🟢 Bot 当前 session（20:48 启动）已稳定运行 ~67 分钟，正常报价 1 个市场。

#### 新发现的 Bug（第 103-104 轮深化）

1. **`_open_batch()` 不写入时间戳字段**：`origin_fill_ts=0.0`、`first_take_submit_ts=None`、`first_take_executed_ts=None`——需要检查 INSERT 语句是否包含了这三个字段。`origin_fill_ts` 应等于日志中 REWARD_EXIT_BATCH_OPENED 的时间戳（Unix epoch）；`first_take_submit_ts` 应在 BATCH_TAKE_SUBMITTED 首次调用时 UPDATE。

2. **`_credit_take_fills()` 不写入 take_filled_size**：日志中 batch-1 有 10 次、batch-2 有 2 次 BATCH_TAKE_EXECUTED，每次明确填量（14.80 / 170.49 / 175.12），但 DB 中 `take_filled_size=0.0`。需检查 `_credit_take_fills()` 的 UPDATE 路径是否可达或被短路（如在 `remaining > 0` 情况下的 early return）。

3. **Fill-to-batch 链接缺失**：`reward_exit_fills.batch_id=""`。在 BATCH_TAKE_EXECUTED 回调中 fill 产生时，应设置 batch_id 指向对应的 reward-exit batch。

#### 结论

第 104 轮与第 103 轮完全一致，reward-exit 路径持续无新活动。监控已持续 104 轮（约 47.5+ 小时）。

**修复优先顺序不变**：
1. **P0**: 修复 `_open_batch()` 写入 timestamp 字段（origin_fill_ts, first_take_submit_ts, first_take_executed_ts）
2. **P0**: 修复 `_credit_take_fills()` 写入 take_filled_size 和 batch_id 链接
3. **P0**: take 前检查余额 → 避免大量无意义重试（当前 batch-1 余额 ~$5 但需要 $7；batch-2 需要 $87）
4. **P1**: batch 超时自动关闭（terminal_after_secs 到期后 close + unlock 市场）
5. **P1**: bot 启动时清理过期 batch（检查 closed_ts=None 但已过期的 batch）
