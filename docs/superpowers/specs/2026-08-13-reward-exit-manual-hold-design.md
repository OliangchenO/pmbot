# Reward Exit Manual Hold Design

当一个奖励退出批次确认补齐 2 倍互补仓后，机器人撤销该 CID 的现有机器人订单，并将批次转为 `MANUAL_HOLD`。批次保留其 `market_name`，作为持久化的可读手动接管记录。

`MANUAL_HOLD` CID 不参与报价、撤单、take、SELL 或通用库存恢复。每个 tick 使用交易所刷新后的仓位检查；仅当 YES、NO 两侧的最大持仓均小于 5 股时，关闭手动接管批次并恢复市场自动管理。

撤单失败时不转入手动接管，避免声称人工接管却留下机器人订单。未运行的新代码不会影响实盘订单。
