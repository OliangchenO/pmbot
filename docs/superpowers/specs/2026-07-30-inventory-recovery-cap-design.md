# 库存恢复报价成本上限设计

## 目标

当已有未配对库存时，持续保留一张不会使配对亏损的互补方向被动买单。

## 规则

- 保持现有的互补方向、未配对数量上限和未知成本基准时拒绝报价。
- 若理论互补买价高于由成本基准、费用和 tick 推导出的 `pair_cap`，将价格钳制为 `pair_cap`，而非删除该报价。
- 这样订单协调器会保留同价旧单，或将更高的旧单替换为安全价格；不改变强制对冲的吃单价格上限。

## 验证

新增测试覆盖超出上限的理论价被钳制为上限、方向和数量仍受库存约束。只运行本地 pytest，不启动 live 交易。
